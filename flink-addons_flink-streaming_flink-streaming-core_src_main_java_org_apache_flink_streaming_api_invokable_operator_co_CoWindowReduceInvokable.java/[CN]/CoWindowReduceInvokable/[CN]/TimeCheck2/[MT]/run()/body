{
  while (true) {
    try {
      Thread.sleep(slideSize2);
    }
 catch (    InterruptedException e) {
    }
    if (isRunning) {
      checkWindowEnd2(System.currentTimeMillis(),window2);
    }
 else {
      break;
    }
  }
}
