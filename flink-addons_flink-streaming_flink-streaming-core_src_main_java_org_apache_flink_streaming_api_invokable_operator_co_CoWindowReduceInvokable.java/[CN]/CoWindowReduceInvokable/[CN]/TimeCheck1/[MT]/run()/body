{
  while (true) {
    try {
      Thread.sleep(slideSize1);
    }
 catch (    InterruptedException e) {
    }
    if (isRunning) {
      checkWindowEnd1(System.currentTimeMillis(),window1);
    }
 else {
      break;
    }
  }
}
