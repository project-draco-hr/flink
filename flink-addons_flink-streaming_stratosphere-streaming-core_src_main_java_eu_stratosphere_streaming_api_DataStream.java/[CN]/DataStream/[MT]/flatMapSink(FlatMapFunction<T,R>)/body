{
  DataStream<R> rS=environment.addFunction("flatMap",this.copy(),flatMapper,new FlatMapInvokable<T,R>(flatMapper));
  environment.addIterationSink("flatMap",this.copy(),flatMapper,new FlatMapInvokable<T,R>(flatMapper),rS.getId());
  return rS;
}
