{
  TypeVariable<?>[] typeParameters=type.getType().getTypeParameters();
  if (type.getParameters().length < typeParameters.length)   return null;
  for (int index=0; index < typeParameters.length; index++)   if (typeVar.equals(typeParameters[index]))   return type.getParameters()[index];
  return null;
}
