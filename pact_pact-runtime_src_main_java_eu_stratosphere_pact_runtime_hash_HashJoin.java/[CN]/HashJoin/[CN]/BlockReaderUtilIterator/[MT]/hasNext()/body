{
  if (this.next == null) {
    KeyValuePair<K,V> pair=new KeyValuePair<K,V>(this.keySerialization.newInstance(),this.valueSerialization.newInstance());
    if (next(pair)) {
      this.next=pair;
      return true;
    }
 else {
      return false;
    }
  }
 else {
    return true;
  }
}
