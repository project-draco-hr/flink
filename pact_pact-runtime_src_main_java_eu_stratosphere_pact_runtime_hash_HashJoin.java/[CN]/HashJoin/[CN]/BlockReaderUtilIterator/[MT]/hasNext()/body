{
  if (this.next == null) {
    PactRecord rec=new PactRecord();
    if (next(rec)) {
      this.next=rec;
      return true;
    }
 else {
      return false;
    }
  }
 else {
    return true;
  }
}
