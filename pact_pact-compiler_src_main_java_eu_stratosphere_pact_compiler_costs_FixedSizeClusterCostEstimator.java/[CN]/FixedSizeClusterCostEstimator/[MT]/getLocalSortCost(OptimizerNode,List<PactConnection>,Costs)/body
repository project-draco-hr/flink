{
  costs.setNetworkCost(0);
  long s=0;
  for (  PactConnection c : input)   s+=c.getSourcePact().getEstimatedOutputSize() * c.getReplicationFactor();
  costs.setSecondaryStorageCost(s < 0 ? -1 : 2 * s);
}
