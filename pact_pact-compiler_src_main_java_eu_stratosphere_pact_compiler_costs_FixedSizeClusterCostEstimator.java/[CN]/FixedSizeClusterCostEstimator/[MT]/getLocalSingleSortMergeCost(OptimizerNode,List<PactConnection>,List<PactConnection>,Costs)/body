{
  costs.setNetworkCost(0);
  long s1=0;
  for (  PactConnection c : unsortedInput)   s1+=c.getSourcePact().getEstimatedOutputSize() * c.getReplicationFactor();
  costs.setSecondaryStorageCost(s1 < 0 ? -1 : 2 * s1);
}
