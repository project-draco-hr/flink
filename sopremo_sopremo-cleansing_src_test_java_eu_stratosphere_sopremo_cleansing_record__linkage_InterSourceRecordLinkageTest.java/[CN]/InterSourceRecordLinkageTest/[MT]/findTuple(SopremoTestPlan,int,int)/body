{
  for (  PactJsonObject object : this.inputs.get(sourceIndex))   if (((IntNode)this.idProjections[sourceIndex].evaluate(object.getValue(),testPlan.getEvaluationContext())).getIntValue() == id)   return object;
  throw new IllegalStateException();
}
