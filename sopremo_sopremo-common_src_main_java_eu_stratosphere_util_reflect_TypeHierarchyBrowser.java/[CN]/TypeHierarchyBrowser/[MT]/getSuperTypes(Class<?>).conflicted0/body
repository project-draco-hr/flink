{
  final Class<?> superclass=startClass.getSuperclass();
  if (superclass == null)   return Arrays.asList(startClass.getInterfaces());
  return new ConcatenatingIterable<Class<?>>(Arrays.asList(startClass.getInterfaces()),Collections.singleton(startClass.getSuperclass()));
}
