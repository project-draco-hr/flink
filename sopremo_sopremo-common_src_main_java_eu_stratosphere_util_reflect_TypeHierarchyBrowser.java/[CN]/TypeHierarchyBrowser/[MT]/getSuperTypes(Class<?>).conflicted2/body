{
  final Class<?> superclass=startClass.getSuperclass();
  if (superclass == null)   return Arrays.asList(startClass.getInterfaces());
  return new ConcatenatingIterable<Class<?>>(Collections.singleton(startClass.getSuperclass()),Arrays.asList(startClass.getInterfaces()));
}
