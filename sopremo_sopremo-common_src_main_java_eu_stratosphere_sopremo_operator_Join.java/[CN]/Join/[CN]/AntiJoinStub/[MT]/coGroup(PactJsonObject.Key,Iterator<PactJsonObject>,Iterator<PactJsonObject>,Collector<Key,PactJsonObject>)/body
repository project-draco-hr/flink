{
  if (!values2.hasNext())   while (values1.hasNext()) {
    JsonNode result=this.getTransformation().evaluate(JsonUtils.asArray(values1.next().getValue()),this.getContext());
    out.collect(PactNull.getInstance(),new PactJsonObject(result));
  }
}
