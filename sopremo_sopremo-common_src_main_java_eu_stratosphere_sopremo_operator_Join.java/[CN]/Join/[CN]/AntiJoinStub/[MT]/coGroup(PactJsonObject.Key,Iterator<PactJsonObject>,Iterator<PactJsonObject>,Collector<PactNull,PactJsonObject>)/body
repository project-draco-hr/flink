{
  if (!values2.hasNext())   while (values1.hasNext()) {
    JsonNode result=this.transformation.evaluate(JsonUtils.asArray(values1.next().getValue()));
    out.collect(PactNull.getInstance(),new PactJsonObject(result));
  }
}
