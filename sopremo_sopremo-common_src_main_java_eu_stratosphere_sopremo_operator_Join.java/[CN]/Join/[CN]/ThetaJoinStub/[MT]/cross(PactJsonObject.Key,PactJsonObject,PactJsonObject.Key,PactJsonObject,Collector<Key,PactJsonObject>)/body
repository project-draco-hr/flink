{
  JsonNode inputPair=JsonUtils.asArray(value1.getValue(),value2.getValue());
  if (this.comparison.evaluate(inputPair,getContext()) == BooleanNode.TRUE)   out.collect(PactNull.getInstance(),new PactJsonObject(this.getTransformation().evaluate(inputPair,getContext())));
}
