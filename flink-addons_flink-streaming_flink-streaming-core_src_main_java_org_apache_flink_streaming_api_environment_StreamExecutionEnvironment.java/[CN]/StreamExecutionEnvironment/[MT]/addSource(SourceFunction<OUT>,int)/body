{
  DataStreamSource<OUT> returnStream=new DataStreamSource<OUT>(this,"source");
  try {
    jobGraphBuilder.addSource(returnStream.getId(),new SourceInvokable<OUT>(function),new FunctionTypeWrapper<OUT>(function,SourceFunction.class,0),"source",SerializationUtils.serialize(function),parallelism);
  }
 catch (  SerializationException e) {
    throw new RuntimeException("Cannot serialize SourceFunction");
  }
  return returnStream;
}
