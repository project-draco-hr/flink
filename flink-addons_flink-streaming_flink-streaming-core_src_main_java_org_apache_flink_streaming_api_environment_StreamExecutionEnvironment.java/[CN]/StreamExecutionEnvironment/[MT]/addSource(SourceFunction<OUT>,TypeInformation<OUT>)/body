{
  DataStreamSource<OUT> returnStream=new DataStreamSource<OUT>(this,"source",outTypeInfo);
  jobGraphBuilder.addStreamVertex(returnStream.getId(),new SourceInvokable<OUT>(function),null,outTypeInfo,"source",1);
  return returnStream;
}
