{
  @SuppressWarnings("unchecked") TypeComparator<K> c1=(TypeComparator<K>)comp1;
  @SuppressWarnings("unchecked") TypeComparator<K> c2=(TypeComparator<K>)comp2;
  return new TupleLeadingFieldPairComparator<K,T1,T2>(c1,c2);
}
