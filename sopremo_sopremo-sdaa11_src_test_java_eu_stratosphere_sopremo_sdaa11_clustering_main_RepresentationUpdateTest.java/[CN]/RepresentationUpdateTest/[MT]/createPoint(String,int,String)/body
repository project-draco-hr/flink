{
  final ObjectNode node=new ObjectNode();
  final IArrayNode valuesNode=new ArrayNode();
  JsonUtil2.copyStrings(valuesNode,values);
  PointNodes.write(node,new TextNode(key),valuesNode,new IntNode(rowsum));
  return node;
}
