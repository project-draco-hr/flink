{
  if (this.numLeft > 0) {
    this.numLeft--;
    Record result=new Record(2);
    result.setField(0,this.key);
    result.setField(1,this.value);
    return result;
  }
 else {
    return null;
  }
}
