{
  if (this.exception != null)   throw new RuntimeException("The combiner failed due to an exception.",this.exception.getCause() == null ? this.exception : this.exception.getCause());
  this.inputCollector.collect(record);
}
