{
  if (this.getMappingSize() == 0)   return null;
  Mapping mapping=this.getMapping(0);
  if (mapping instanceof ValueAssignment)   return ((ValueAssignment)mapping).getTransformation();
  return null;
}
