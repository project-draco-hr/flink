{
  if (this.getTarget() != NO_TARGET)   builder.append(getTarget()).append("=");
  builder.append("[");
  for (int index=0; index < this.mappings.size(); index++) {
    if (index > 0)     builder.append(", ");
    mappings.get(index).toString(builder);
  }
  builder.append("]");
}
