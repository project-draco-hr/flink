{
  if (this == IDENTITY)   return node;
  ObjectNode transformedNode=OBJECT_MAPPER.createObjectNode();
  for (  Mapping mapping : this.mappings)   transformedNode.put(mapping.getTarget(),mapping.evaluate(node,context));
  return transformedNode;
}
