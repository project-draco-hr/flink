{
  StreamGroupedReduce<OUT> operator=new StreamGroupedReduce<OUT>(clean(aggregate),keySelector);
  SingleOutputStreamOperator<OUT,?> returnStream=transform("Grouped Aggregation",getType(),operator);
  return returnStream;
}
