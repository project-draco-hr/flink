{
  if (!(o instanceof Pair<?,?>))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to N_Pair.");
  int result=this.first.compareTo(((Pair<?,?>)o).first);
  if (result == 0)   result=this.second.compareTo(((Pair<?,?>)o).second);
  return result;
}
