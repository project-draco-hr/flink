{
  if (inTypeParameter1 != -1) {
    inTupleTypeInfo1=(TupleTypeInfo)TypeExtractor.createTypeInfo(functionSuperClass,function.getClass(),inTypeParameter1,null,null);
  }
  if (inTypeParameter2 != -1) {
    inTupleTypeInfo2=(TupleTypeInfo)TypeExtractor.createTypeInfo(functionSuperClass,function.getClass(),inTypeParameter2,null,null);
  }
  if (outTypeParameter != -1) {
    outTupleTypeInfo=(TupleTypeInfo)TypeExtractor.createTypeInfo(functionSuperClass,function.getClass(),outTypeParameter,null,null);
  }
}
