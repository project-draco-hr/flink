{
  this.format=format;
  @SuppressWarnings({"unchecked","rawtypes"}) GenericDataSourceBase<String,?> source=new GenericDataSourceBase(format,new OperatorInformation<String>(typeInfo),format.toString());
  formatWrapper=source.getUserCodeWrapper();
  this.serializerFactory=createSerializer(typeInfo);
}
