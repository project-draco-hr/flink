{
  final ArrayList<BooleanExpression> booleans=new ArrayList<BooleanExpression>(expressions.size());
  for (  EvaluationExpression evaluationExpression : expressions)   booleans.add(ensureBooleanExpression(evaluationExpression));
  return booleans;
}
