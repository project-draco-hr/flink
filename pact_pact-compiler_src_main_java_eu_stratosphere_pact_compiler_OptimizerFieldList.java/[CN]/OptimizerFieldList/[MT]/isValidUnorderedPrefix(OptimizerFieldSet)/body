{
  if (set.size() > size()) {
    return false;
  }
  List<ColumnWithType> list=get();
  for (int i=0; i < set.size(); i++) {
    if (!set.contains(list.get(i))) {
      return false;
    }
  }
  return true;
}
