{
  this.config=config;
  this.taskName=taskName;
  this.collector=output;
  @SuppressWarnings("unchecked") final GenericMapper<IT,OT> mapper=RegularPactTask.instantiateUserCode(config,userCodeClassLoader,GenericMapper.class);
  this.mapper=mapper;
  mapper.setRuntimeContext(getRuntimeContext(parent,taskName));
}
