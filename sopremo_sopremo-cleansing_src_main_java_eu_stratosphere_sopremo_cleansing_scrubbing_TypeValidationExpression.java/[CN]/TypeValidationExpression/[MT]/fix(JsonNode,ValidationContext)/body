{
  try {
    if (value.isTextual())     return LenientParser.INSTANCE.parse(value.getTextValue(),this.type,LenientParser.ELIMINATE_NOISE);
    return TypeCoercer.INSTANCE.coerce(value,this.type);
  }
 catch (  final Exception e) {
    return super.fix(value,context);
  }
}
