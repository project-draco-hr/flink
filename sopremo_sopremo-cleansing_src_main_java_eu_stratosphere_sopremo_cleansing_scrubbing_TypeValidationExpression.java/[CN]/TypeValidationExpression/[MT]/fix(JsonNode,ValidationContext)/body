{
  try {
    if (value.isTextual())     return LenientParser.INSTANCE.parse((TextNode)value,this.type,LenientParser.ELIMINATE_NOISE);
    return TypeCoercer.INSTANCE.coerce(value,this.type);
  }
 catch (  final Exception e) {
    return super.fix(value,context);
  }
}
