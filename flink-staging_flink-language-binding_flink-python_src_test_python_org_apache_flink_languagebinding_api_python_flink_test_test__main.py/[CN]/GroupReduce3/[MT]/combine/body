def combine(self, iterator, collector):
    if iterator.has_next():
        v1 = iterator.next()
    if iterator.has_next():
        v2 = iterator.next()
    if (v1[0] < v2[0]):
        collector.collect(v1)
    else:
        collector.collect(v2)
