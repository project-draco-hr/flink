{
  if (childConditions.size() == 1)   return valueOf(childConditions.get(0));
  return new ConditionalExpression(combination,childConditions.toArray(new BooleanExpression[childConditions.size()]));
}
