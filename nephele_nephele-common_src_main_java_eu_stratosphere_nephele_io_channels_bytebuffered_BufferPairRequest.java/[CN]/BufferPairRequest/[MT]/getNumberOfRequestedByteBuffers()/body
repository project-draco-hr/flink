{
  if (this.compressedDataBufferSize > 0 && this.uncompressedDataBufferSize > 0) {
    return 2;
  }
  if (this.compressedDataBufferSize > 0 || this.uncompressedDataBufferSize > 0) {
    return 1;
  }
  return 0;
}
