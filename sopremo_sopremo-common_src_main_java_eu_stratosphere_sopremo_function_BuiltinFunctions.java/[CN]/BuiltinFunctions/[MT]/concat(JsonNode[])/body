{
  StringBuilder builder=new StringBuilder();
  for (  JsonNode jsonNode : params)   builder.append(jsonNode.isTextual() ? jsonNode.getTextValue() : jsonNode);
  return OBJECT_MAPPER.valueToTree(builder);
}
