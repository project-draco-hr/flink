{
  this.print(appendable,new NodePrinter<Node>(){
    String widthString="%-" + width + "s";
    @Override public String toString(    Node node){
      return String.format(this.widthString,node.toString());
    }
  }
,width);
}
