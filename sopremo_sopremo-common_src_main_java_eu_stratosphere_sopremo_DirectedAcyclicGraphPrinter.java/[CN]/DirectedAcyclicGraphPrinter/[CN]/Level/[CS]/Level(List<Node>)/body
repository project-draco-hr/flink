{
  this.levelNodes.addAll(nodes);
  for (  Node node : nodes) {
    ArrayList<Object> links=new ArrayList<Object>();
    for (    Object connectedNode : DirectedAcyclicGraphPrinter.this.navigator.getConnectedNodes(node))     links.add(connectedNode);
    this.outgoings.put(node,links);
  }
}
