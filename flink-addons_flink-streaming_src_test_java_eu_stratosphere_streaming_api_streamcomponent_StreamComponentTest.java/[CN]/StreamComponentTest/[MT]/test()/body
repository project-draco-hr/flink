{
  assertEquals(200,data.keySet().size());
  for (  Integer k : data.keySet()) {
    if (k < 0) {
      assertEquals((Integer)(k - 1),data.get(k));
    }
 else {
      assertEquals((Integer)(k + 1),data.get(k));
    }
  }
}
