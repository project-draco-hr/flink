{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  DataStream<Tuple2<Integer,Integer>> oneTask=context.addSource(new MySource()).map(new MyTask()).addSink(new MySink());
  context.execute();
}
