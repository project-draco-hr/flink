{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  DataStream<Tuple2<Integer,Integer>> oneTask=context.addSource(new MySource(),SOURCE_PARALELISM).map(new MyTask(),PARALELISM).addSink(new MySink());
  context.execute();
}
