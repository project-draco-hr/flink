{
  super(reducer);
  this.keySelector=keySelector;
  valueTypeInformation=typeInformation;
  keyTypeInformation=(TypeInformation<Object>)TypeExtractor.getKeySelectorTypes(keySelector,typeInformation);
}
