{
  if (this.getInvalidToken() == null)   return super.getMessage();
  return String.format("%s: %s @ (%d, %d)",super.getMessage(),this.getInvalidToken().getText(),this.getLine(),this.getCharIndex());
}
