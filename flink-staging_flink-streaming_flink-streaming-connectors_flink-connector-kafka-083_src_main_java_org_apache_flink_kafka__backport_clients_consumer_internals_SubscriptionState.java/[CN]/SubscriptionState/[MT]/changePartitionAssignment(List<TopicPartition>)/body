{
  for (  TopicPartition tp : assignments)   if (!this.subscribedTopics.contains(tp.topic()))   throw new IllegalArgumentException("Assigned partition " + tp + " for non-subscribed topic.");
  this.clearAssignment();
  this.assignedPartitions.addAll(assignments);
  this.needsPartitionAssignment=false;
}
