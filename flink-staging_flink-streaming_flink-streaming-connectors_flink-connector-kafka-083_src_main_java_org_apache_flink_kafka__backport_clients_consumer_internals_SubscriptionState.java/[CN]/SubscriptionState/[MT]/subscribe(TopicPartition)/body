{
  if (this.subscribedTopics.size() > 0)   throw new IllegalStateException("Subcription to topics and partitions are mutually exclusive");
  this.subscribedPartitions.add(tp);
  this.assignedPartitions.add(tp);
}
