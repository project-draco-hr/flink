{
  if (!subscribedPartitions.contains(partition))   throw new IllegalStateException("Partition " + partition + " was never subscribed to.");
  subscribedPartitions.remove(partition);
  clearPartition(partition);
}
