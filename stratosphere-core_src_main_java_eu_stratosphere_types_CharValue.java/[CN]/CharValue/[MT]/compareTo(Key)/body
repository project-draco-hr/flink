{
  if (!(o instanceof CharValue))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to PactChar!");
  final int other=((CharValue)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
