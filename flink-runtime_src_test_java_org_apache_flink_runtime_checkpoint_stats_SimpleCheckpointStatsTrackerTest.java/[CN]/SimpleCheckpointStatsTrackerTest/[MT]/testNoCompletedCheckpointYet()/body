{
  CheckpointStatsTracker tracker=new SimpleCheckpointStatsTracker(0,Collections.<ExecutionJobVertex>emptyList());
  assertFalse(tracker.getJobStats().isDefined());
  assertFalse(tracker.getOperatorStats(new JobVertexID()).isDefined());
}
