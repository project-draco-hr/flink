{
  String tempDirString=GlobalConfiguration.getString(ConfigConstants.TASK_MANAGER_TMP_DIR_KEY,ConfigConstants.DEFAULT_TASK_MANAGER_TMP_PATH);
  String[] tempDirs=tempDirString.split(",|" + File.pathSeparator);
  tempDir=tempDirs[rnd.nextInt(tempDirs.length)];
  createSpillingChannel();
}
