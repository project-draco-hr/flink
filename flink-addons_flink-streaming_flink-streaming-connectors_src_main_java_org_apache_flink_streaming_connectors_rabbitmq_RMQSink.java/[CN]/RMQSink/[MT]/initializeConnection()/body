{
  factory=new ConnectionFactory();
  factory.setHost(hostName);
  try {
    connection=factory.newConnection();
    channel=connection.createChannel();
  }
 catch (  IOException e) {
    new RuntimeException("Cannot create RMQ connection with " + queueName + " at "+ hostName,e);
  }
  initDone=true;
}
