{
  Output source=input == null ? null : input.getSource();
  EvaluationExpression keyProjection=this.idProjections.get(source);
  if (keyProjection == null)   keyProjection=EvaluationExpression.SAME_VALUE;
  return keyProjection;
}
