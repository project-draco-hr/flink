{
  if (this.currentSource.hasNext()) {
    return true;
  }
 else {
    if (this.nextSources.size() > 0) {
      this.currentSource=this.nextSources.remove(0);
      return hasNext();
    }
 else {
      return false;
    }
  }
}
