{
  final IntValue subtaskIndex=new IntValue(getEnvironment().getIndexInSubtaskGroup());
  try {
    for (int i=0; i < numberOfTimesToSend; i++) {
      writer.emit(subtaskIndex);
    }
    writer.flush();
  }
  finally {
    writer.clearBuffers();
  }
}
