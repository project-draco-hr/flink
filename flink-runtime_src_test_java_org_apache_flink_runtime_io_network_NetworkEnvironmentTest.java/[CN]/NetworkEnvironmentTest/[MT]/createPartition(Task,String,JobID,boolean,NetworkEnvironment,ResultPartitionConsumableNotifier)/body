{
  return new ResultPartition(name,owningTask,jobId,new ResultPartitionID(),ResultPartitionType.PIPELINED,eagerlyDeployConsumers,1,env.getResultPartitionManager(),resultPartitionConsumableNotifier,mock(IOManager.class),env.getDefaultIOMode());
}
