{
  return new ResultPartition(name,jobId,new ResultPartitionID(),ResultPartitionType.PIPELINED,eagerlyDeployConsumers,1,env.getPartitionManager(),env.getPartitionConsumableNotifier(),mock(IOManager.class),env.getDefaultIOMode());
}
