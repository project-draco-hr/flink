{
  WindowReduceInvokable<OUT> invokable;
  if (isGrouped) {
    invokable=new GroupedWindowReduceInvokable<OUT>(reducer,batchSize,slideSize,keySelector,timeStamp);
  }
 else {
    invokable=new WindowReduceInvokable<OUT>(reducer,batchSize,slideSize,timeStamp);
  }
  return invokable;
}
