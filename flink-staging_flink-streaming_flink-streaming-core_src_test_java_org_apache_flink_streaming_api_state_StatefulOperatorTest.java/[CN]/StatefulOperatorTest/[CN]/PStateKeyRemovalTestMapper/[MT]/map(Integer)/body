{
  if (value == 0) {
    seen.update(null);
  }
 else {
    Boolean s=seen.value();
    if (s) {
      seen.update(null);
    }
 else {
      seen.update(true);
    }
  }
  return value.toString();
}
