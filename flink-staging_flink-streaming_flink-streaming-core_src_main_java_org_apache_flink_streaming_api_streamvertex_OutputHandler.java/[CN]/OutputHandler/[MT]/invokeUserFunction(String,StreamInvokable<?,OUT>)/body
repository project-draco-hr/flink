{
  if (LOG.isDebugEnabled()) {
    LOG.debug("{} {} invoked with instance id {}",componentTypeName,vertex.getName(),vertex.getInstanceID());
  }
  try {
    vertex.invokeUserFunction(userInvokable);
  }
 catch (  Exception e) {
    flushOutputs();
    throw new RuntimeException(e);
  }
  if (LOG.isDebugEnabled()) {
    LOG.debug("{} {} invoke finished instance id {}",componentTypeName,vertex.getName(),vertex.getInstanceID());
  }
  flushOutputs();
}
