{
  while (true) {
    line=br.readLine();
    if (line == null) {
      break;
    }
    if (line != "") {
      line=line.replaceAll("[\\-\\+\\.\\^:,]","");
      outRecord.setString(0,line);
      outRecord.setLong(1,timestamp);
      timestamp++;
      emit(outRecord);
    }
  }
}
