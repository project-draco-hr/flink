{
  timestamp=0L;
  line=br.readLine().replaceAll("[\\-\\+\\.\\^:,]","");
  while (line != null) {
    if (line != "") {
      outRecord.setString(0,line);
      outRecord.setLong(1,timestamp);
      emit(outRecord);
    }
    line=br.readLine();
    timestamp++;
  }
}
