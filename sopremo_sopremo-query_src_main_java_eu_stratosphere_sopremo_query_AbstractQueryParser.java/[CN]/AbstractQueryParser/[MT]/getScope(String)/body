{
  if (packageName == null)   return this;
  ParsingScope scope=this.packageManager.getPackageInfo(packageName);
  if (scope == null)   throw new QueryParserException("Unknown package " + packageName);
  return scope;
}
