{
  for (  File file : dir.listFiles())   if (file.isDirectory())   this.importFromProject(classPath,file);
 else   if (file.getName().endsWith(".class") && !file.getName().contains("$"))   this.importFromFile(classPath,file);
}
