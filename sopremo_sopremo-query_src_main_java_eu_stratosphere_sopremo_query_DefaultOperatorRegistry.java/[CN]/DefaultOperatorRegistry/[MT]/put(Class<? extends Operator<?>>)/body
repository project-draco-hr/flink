{
  String name=this.getName(operatorClass);
  if (this.get(name) != null)   throw new IllegalStateException("Duplicate operator " + name);
  this.put(name,new OperatorInfo(operatorClass,name,this.propertyNameChooser));
}
