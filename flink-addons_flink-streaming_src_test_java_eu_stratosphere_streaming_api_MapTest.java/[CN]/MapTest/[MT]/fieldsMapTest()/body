{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  fillFromCollectionFieldsSet();
  DataStream<Tuple1<Integer>> dataStream=env.fromCollection(fromCollectionFields).partitionBy(0).map(new MyFieldsMap(),3).addSink(new MyFieldsSink());
  env.execute();
  assertTrue(allInOne);
}
