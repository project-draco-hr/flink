{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=env.addSource(new MySource(),1).partitionBy(0).map(new MyMap(),3).addSink(new MyFieldsSink());
  env.execute();
  assertEquals(10,fieldsResult);
}
