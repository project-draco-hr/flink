{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=context.addSource(new MySource(),2).partitionBy(0).map(new MyMap(),3).broadcast().addSink(new MyGraphSink(),2);
  context.execute();
  assertEquals(40,graphResult);
}
