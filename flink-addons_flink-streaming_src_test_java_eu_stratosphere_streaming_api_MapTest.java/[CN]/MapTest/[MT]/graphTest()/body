{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=context.addSource(new MySource(),2).partitionBy(0).map(new MyMap(),3).addSink(new MyGraphSink());
  context.execute();
  assertEquals(20,graphResult);
}
