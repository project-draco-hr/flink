{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=env.addSource(new MySource(),1).map(new MyMap(),3).addSink(new MyShufflesSink());
  env.execute();
  assertEquals(10,shuffleResult);
}
