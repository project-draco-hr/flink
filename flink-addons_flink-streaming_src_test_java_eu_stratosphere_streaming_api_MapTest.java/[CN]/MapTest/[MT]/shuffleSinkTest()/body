{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=context.addSource(new MySource(),1).map(new MyMap(),3).addSink(new MyShufflesSink());
  context.execute();
  assertEquals(10,shuffleResult);
}
