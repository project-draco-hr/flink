{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=env.addSource(new MyDiffFieldsSource(),1).partitionBy(0).map(new MyDiffFieldsMap(),3).addSink(new MyDiffFieldsSink());
  env.execute();
  assertTrue(threeInAll);
  assertEquals(9,diffFieldsResult);
}
