{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  DataStream<Tuple1<Integer>> dataStream=context.addSource(new MyDiffFieldsSource(),1).partitionBy(0).map(new MyDiffFieldsMap(),3).addSink(new MyDiffFieldsSink());
  context.execute();
  assertTrue(threeInAll);
  assertEquals(9,diffFieldsResult);
}
