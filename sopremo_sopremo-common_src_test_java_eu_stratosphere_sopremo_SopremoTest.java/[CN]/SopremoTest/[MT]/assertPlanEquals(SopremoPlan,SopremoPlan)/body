{
  final List<Operator<?>> unmatchingOperators=expectedPlan.getUnmatchingOperators(actualPlan);
  if (!unmatchingOperators.isEmpty())   Assert.failNotEquals(String.format("At least two operators do not match "),unmatchingOperators.get(0),unmatchingOperators.get(1));
}
