{
  ArraySchema schema=new ArraySchema();
  schema.setHeadSize(5);
  PactRecord record=schema.jsonToRecord(new ArrayNode(IntNode.valueOf(0),IntNode.valueOf(1),IntNode.valueOf(2)),null);
  this.node=new LazyArrayNode(record,schema);
}
