{
  Tuple v=(Tuple)value;
  try {
    K key=v.getFieldNotNull(pos);
    tuple.f0=key;
    tuple.f1=value;
  }
 catch (  NullFieldException nfex) {
    throw new NullKeyFieldException(nfex);
  }
  return tuple;
}
