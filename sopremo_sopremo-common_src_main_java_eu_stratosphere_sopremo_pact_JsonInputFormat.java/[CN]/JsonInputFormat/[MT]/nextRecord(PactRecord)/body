{
  if (!this.end) {
    final PactRecord result=this.schema.jsonToRecord(this.parser.readValueAsTree(),record,null);
    if (result != record)     result.copyTo(record);
    this.checkEnd();
    return true;
  }
  return false;
}
