{
  if (filePath == null)   throw new IllegalArgumentException("File path may not be null.");
  if (filePath.isEmpty()) {
    setFilePath(new Path());
    return;
  }
  setFilePath(new Path(filePath));
}
