{
  int index=this.schema.hasMapping(fieldName);
  if (fieldInSchema(index)) {
    this.record.setField(index,value);
  }
 else {
    ((IObjectNode)SopremoUtil.unwrap(this.record.getField(this.schema.getMappingSize() + 1,JsonNodeWrapper.class))).put(fieldName,value);
  }
  return this;
}
