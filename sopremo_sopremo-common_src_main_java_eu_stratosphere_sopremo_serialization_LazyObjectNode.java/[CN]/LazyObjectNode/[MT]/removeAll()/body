{
  for (int i=0; i < this.schema.getMappingSize(); i++) {
    this.record.setField(i,NullNode.getInstance());
  }
  ((IObjectNode)SopremoUtil.unwrap(this.record.getField(this.schema.getMappingSize() + 1,JsonNodeWrapper.class))).removeAll();
  return this;
}
