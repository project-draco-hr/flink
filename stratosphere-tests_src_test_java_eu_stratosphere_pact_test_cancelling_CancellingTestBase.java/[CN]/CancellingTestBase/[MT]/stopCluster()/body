{
  LOG.info("######################### stopping cluster config #########################");
  cluster.stopCluster();
  ClusterProviderPool.removeInstance(Constants.DEFAULT_TEST_CONFIG);
  FileSystem.closeAll();
  System.gc();
}
