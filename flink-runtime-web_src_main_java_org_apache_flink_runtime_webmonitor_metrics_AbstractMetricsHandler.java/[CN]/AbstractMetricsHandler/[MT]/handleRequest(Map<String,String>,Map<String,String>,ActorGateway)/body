{
  fetcher.update();
  String requestedMetricsList=queryParams.get("get");
  return requestedMetricsList != null ? getMetricsValues(pathParams,requestedMetricsList) : getAvailableMetricsList(pathParams);
}
