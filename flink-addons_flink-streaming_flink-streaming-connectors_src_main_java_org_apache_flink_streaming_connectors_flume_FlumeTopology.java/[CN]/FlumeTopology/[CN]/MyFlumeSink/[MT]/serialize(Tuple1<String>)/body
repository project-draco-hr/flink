{
  if (tuple.f0.equals("q")) {
    try {
      sendAndClose();
    }
 catch (    Exception e) {
      new RuntimeException("Error while closing RMQ connection with " + port + " at "+ host,e);
    }
  }
  return SerializationUtils.serialize((String)tuple.getField(0));
}
