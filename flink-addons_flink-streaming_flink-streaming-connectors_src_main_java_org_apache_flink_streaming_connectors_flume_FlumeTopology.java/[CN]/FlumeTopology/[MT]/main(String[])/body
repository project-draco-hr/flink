{
  StreamExecutionEnvironment env=StreamExecutionEnvironment.createLocalEnvironment(1);
  @SuppressWarnings("unused") DataStream<String> dataStream1=env.addSource(new FlumeSource<String>("localhost",41414,new SimpleStringScheme())).addSink(new FlumeSink<String>("localhost",42424,new StringToByteSerializer()));
  env.execute();
}
