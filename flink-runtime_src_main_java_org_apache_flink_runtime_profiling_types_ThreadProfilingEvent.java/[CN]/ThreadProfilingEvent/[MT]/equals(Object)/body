{
  if (obj instanceof ThreadProfilingEvent) {
    final ThreadProfilingEvent other=(ThreadProfilingEvent)obj;
    return this.userTime == other.userTime && this.systemTime == other.systemTime && this.blockedTime == other.blockedTime && this.waitedTime == other.waitedTime && super.equals(obj);
  }
 else {
    return false;
  }
}
