{
  final List<String> result=getTestFlatMapOperator(udf).executeOnCollections(input,new RuntimeUDFContext("Test UDF",4,0));
  Assert.assertEquals(input.size(),result.size());
  for (int i=0; i < input.size(); i++) {
    Assert.assertEquals(input.get(i),result.get(i));
  }
}
