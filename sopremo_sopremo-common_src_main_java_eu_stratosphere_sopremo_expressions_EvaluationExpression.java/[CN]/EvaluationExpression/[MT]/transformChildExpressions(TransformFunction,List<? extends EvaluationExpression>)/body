{
  final List<EvaluationExpression> children=new ArrayList<EvaluationExpression>(children2);
  for (int index=0; index < children.size(); index++)   children.set(index,children.get(index).transformRecursively(function));
  return children;
}
