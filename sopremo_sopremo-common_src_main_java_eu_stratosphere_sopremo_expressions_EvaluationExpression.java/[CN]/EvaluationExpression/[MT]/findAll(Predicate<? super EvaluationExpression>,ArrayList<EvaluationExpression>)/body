{
  if (predicate.isTrue(this))   expressions.add(this);
  if (this instanceof ExpressionParent)   for (  EvaluationExpression child : ((ExpressionParent)this))   child.findAll(predicate,expressions);
}
