{
  final Reference<T> ref=new Reference<T>();
  this.transformRecursively(new TransformFunction(){
    @Override public EvaluationExpression call(    EvaluationExpression evaluationExpression){
      if (ref.getValue() == null && evaluableClass.isInstance(evaluationExpression))       ref.setValue((T)evaluationExpression);
      return evaluationExpression;
    }
  }
);
  return ref.getValue();
}
