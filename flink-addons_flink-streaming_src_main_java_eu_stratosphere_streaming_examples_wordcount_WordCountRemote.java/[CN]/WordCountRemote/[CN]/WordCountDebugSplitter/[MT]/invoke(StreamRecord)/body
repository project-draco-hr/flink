{
  i++;
  if (i % 10000 == 0) {
    System.out.println("Splitter: " + i + "\t time: "+ (System.currentTimeMillis() - time)+ " ms");
    time=System.currentTimeMillis();
  }
  words=record.getString(0).split(" ");
  for (  String word : words) {
    outputRecord.setString(0,word);
    emit(outputRecord);
  }
}
