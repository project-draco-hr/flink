{
  Pragma pragma=(Pragma)expr;
  if (pragma.numChildren() != 1) {
    System.err.println("Pragma was not applied, but cannot be removed...");
    return false;
  }
  System.err.println("Pragma was ignored: " + pragma);
  pragma.replaceInParent(pragma.child(0));
  return true;
}
