{
  if (number >= 0 && number < this.numberOfTrainingFiles)   return trainingFiles[number];
 else {
    System.err.println("Number " + number + " for Trainingfile not valid!");
    return null;
  }
}
