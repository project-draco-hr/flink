{
  final String key=os.getKey();
  if (key.isEmpty()) {
    return false;
  }
  if (key.charAt(key.length() - 1) == S3_DIRECTORY_SEPARATOR && os.getSize() == 0L) {
    return true;
  }
  return false;
}
