{
  Iterable<EvaluationExpression> keyExpressions=CollectionUtil.mergeUnique(new ConversionIterable<Operator<?>,Iterable<? extends EvaluationExpression>>(getReachableNodes()){
    @Override protected Iterable<? extends EvaluationExpression> convert(    Operator<?> inputObject){
      return inputObject.getKeyExpressions();
    }
  }
);
  return schemaFactory.create(keyExpressions);
}
