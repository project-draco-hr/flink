{
  final Iterator<IJsonNode> iterator=((ArrayNode)node).iterator();
  if (!iterator.hasNext())   return ZERO;
  INumericNode sum=(INumericNode)iterator.next();
  for (; iterator.hasNext(); )   sum=ArithmeticExpression.ArithmeticOperator.ADDITION.evaluate(sum,(INumericNode)iterator.next(),sum);
  return sum;
}
