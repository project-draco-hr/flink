{
  final List<IJsonNode> nodes=new ArrayList<IJsonNode>();
  for (  final IJsonNode jsonNode : (IArrayNode)node)   nodes.add(jsonNode);
  Collections.sort(nodes);
  final ArrayNode arrayNode=new ArrayNode();
  arrayNode.addAll(nodes);
  return arrayNode;
}
