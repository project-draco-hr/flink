{
  List<Method> methods=ReflectUtil.getMethods(String.class,null,Modifier.PUBLIC,~Modifier.STATIC);
  for (  Method method : methods)   try {
    if (method.getDeclaringClass() == String.class)     registry.register(method);
  }
 catch (  Exception e) {
  }
}
