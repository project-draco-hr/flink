{
  final String[] split=input.getTextValue().split(splitString.getTextValue());
  final ArrayNode splitNode=new ArrayNode();
  for (  final String string : split)   splitNode.add(TextNode.valueOf(string));
  return splitNode;
}
