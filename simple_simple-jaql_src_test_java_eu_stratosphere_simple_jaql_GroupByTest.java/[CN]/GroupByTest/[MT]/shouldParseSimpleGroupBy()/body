{
  Transformation transformation=new Transformation();
  transformation.addMapping(new ValueAssignment(new Function("count",new Input(0))));
  assertParseResult(new Aggregation(transformation,Aggregation.NO_GROUPING,this.employeeSource()),this.employeeJaql() + "employees -> group into count($);");
}
