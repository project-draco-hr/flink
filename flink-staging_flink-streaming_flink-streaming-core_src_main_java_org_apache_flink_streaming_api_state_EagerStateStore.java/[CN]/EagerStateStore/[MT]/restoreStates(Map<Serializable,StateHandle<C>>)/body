{
  for (  Entry<Serializable,StateHandle<C>> snapshotEntry : snapshots.entrySet()) {
    fetchedState.put(snapshotEntry.getKey(),checkpointer.restoreState(snapshotEntry.getValue().getState()));
  }
}
