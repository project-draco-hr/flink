{
  getReader(0).subscribeToEvent(new EventListener(){
    @Override public void eventOccurred(    AbstractTaskEvent event){
      try {
        callback.execute((E)event);
      }
 catch (      Exception e) {
        e.printStackTrace(System.out);
      }
    }
  }
,eventClass);
}
