{
  List<JsonNode> nodes=new ArrayList<JsonNode>();
  for (  JsonNode jsonNode : node)   nodes.add(jsonNode);
  Collections.sort(nodes,JsonNodeComparator.INSTANCE);
  ArrayNode arrayNode=new ArrayNode(null);
  arrayNode.addAll(nodes);
  return arrayNode;
}
