{
  try {
    out.collect(new Tuple5<Long,Integer,String,String,String>(getLong(value,"id"),getInt(value,"entities.hashtags[0].indices[1]"),getString(value,"lang"),getString(value,"text"),getString(value,"user.name")));
  }
 catch (  JSONException e) {
    if (LOG.isErrorEnabled()) {
      LOG.error("Field not found");
    }
  }
}
