{
  if (!isRunning) {
    LOG.debug("snapshotState() called on closed source");
    return null;
  }
  return new Tuple3<>(splitsToFwdOrderedAscByModTime,currentSplitsToFwd,globalModificationTime);
}
