{
  if (log.isInfoEnabled()) {
    log.info(formatLogString("sending " + WorkerDoneEvent.class.getSimpleName() + " with count ["+ event.aggregate()+ "] to sync"));
  }
  getSyncOutput().publishEvent(event);
}
