{
  while (true) {
    int next=recordIterator.next(reuse1,reuse2);
    if (next == 0) {
      break;
    }
 else     if (next == 1) {
      initialize1();
      handleStream1();
    }
 else {
      initialize2();
      handleStream2();
    }
  }
}
