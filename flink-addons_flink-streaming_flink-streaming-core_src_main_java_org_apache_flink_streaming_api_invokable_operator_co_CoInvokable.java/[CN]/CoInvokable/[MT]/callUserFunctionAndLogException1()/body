{
  try {
    callUserFunction1();
  }
 catch (  Exception e) {
    if (LOG.isErrorEnabled()) {
      LOG.error(String.format("Calling user function failed due to: %s",StringUtils.stringifyException(e)));
    }
  }
}
