{
  if (inputs.size() <= 1)   return new GroupProjection(this.expression,inputs.get(0));
  UnionAll union=new UnionAll(inputs);
  return new GroupProjection(new PathExpression(new AggregationExpression(new ArrayUnion()),this.expression),union);
}
