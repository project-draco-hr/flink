{
  this.topics=checkNotNull(topics);
  checkArgument(topics.size() > 0,"You have to define at least one topic.");
  this.deserializer=checkNotNull(deserializer,"valueDeserializer");
  TypeInformation<Tuple2<KafkaTopicPartition,Long>> typeInfo=TypeInformation.of(new TypeHint<Tuple2<KafkaTopicPartition,Long>>(){
  }
);
}
