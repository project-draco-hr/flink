{
  KafkaPartitionState info=partitionState.get(partDescriptor);
  if (info == null) {
    throw new RuntimeException("Unknown Partition: Topic=" + partDescriptor.getTopic() + " Partition="+ partDescriptor.getPartition());
  }
  return info.isActive();
}
