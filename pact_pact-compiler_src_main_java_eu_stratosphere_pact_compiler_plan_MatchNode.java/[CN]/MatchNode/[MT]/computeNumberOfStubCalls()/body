{
  long processedKeys=computeNumberOfProcessedKeys();
  if (processedKeys == -1)   return -1;
  double stubCallsPerKey=computeStubCallsPerProcessedKey();
  if (stubCallsPerKey == -1)   return -1;
  return (long)(processedKeys * stubCallsPerKey);
}
