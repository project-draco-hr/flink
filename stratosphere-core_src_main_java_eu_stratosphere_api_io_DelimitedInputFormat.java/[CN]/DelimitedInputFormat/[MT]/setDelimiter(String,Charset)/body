{
  if (delimiter == null)   throw new IllegalArgumentException("Delimiter must not be null");
  if (charset == null)   throw new IllegalArgumentException("Charset must not be null");
  this.delimiter=delimiter.getBytes(charset);
}
