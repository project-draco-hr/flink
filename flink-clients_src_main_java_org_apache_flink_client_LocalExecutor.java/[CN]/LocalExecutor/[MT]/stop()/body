{
synchronized (this.lock) {
    if (this.flink != null) {
      this.flink.stop();
      this.flink=null;
    }
 else {
      throw new IllegalStateException("The local executor was not started.");
    }
  }
}
