{
  JsonNode result=new LazyArrayProjection(new ObjectAccess("fieldName")).evaluate(createStreamArray(createObjectNode("fieldName",1),createObjectNode("fieldName",2),createObjectNode("fieldName",3)),context);
  Assert.assertEquals(createStreamArray(1,2,3),result);
}
