{
  final SopremoModule elementaryPlan=this.asElementaryOperators();
  return CollectionUtil.mergeUnique(new ConversionIterable<Operator<?>,Iterable<? extends EvaluationExpression>>(elementaryPlan.getReachableNodes()){
    @Override protected Iterable<? extends EvaluationExpression> convert(    Operator<?> inputObject){
      return inputObject.getKeyExpressions();
    }
  }
);
}
