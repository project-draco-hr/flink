{
  JsonNode result=transformation.evaluate(JsonUtils.asArray(new StreamArray(new UnwrappingIterator(values1)),new StreamArray(new UnwrappingIterator(values2))));
  out.collect(PactNull.getInstance(),new PactJsonObject(result));
}
