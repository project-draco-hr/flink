{
  final Iterator<JsonNode> transformedJsons=transformation.evaluate(new UnwrappingIterator(values1),new UnwrappingIterator(values2));
  while (transformedJsons.hasNext())   out.collect(PactNull.getInstance(),new PactJsonObject(transformedJsons.next()));
}
