{
  JsonNode result=this.getTransformation().evaluate(JsonUtils.asArray(new StreamArray(new UnwrappingIterator(values1)),new StreamArray(new UnwrappingIterator(values2))),this.getContext());
  out.collect(PactNull.getInstance(),new PactJsonObject(result));
}
