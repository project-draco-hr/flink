{
  StringBuilder builder=new StringBuilder(this.getName());
  if (this.grouping != null)   builder.append(" on ").append(this.grouping);
  if (this.getTransformation() != Transformation.IDENTITY)   builder.append(" to ").append(this.getTransformation());
  return builder.toString();
}
