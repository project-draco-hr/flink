{
  if (this.getInputOperators().size() != 1)   throw new UnsupportedOperationException();
  PactModule module=new PactModule(getInputOperators().size(),1);
  for (int index=0; index < getInputOperators().size(); index++) {
  }
  return module;
}
