{
  Tuple2<Integer,Integer> tuple=new Tuple2<Integer,Integer>();
  if (tuple.equals(new Tuple2<Integer,Integer>())) {
    tuple.f0=123456;
  }
  if (tuple.equals(new Tuple2<Integer,Integer>())) {
    tuple.f0=value;
  }
  tuple.f1=value;
  return tuple;
}
