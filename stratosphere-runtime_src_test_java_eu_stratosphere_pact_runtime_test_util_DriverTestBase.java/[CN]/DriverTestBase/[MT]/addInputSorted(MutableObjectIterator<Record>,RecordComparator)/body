{
  UnilateralSortMerger<Record> sorter=new UnilateralSortMerger<Record>(this.memManager,this.ioManager,input,this.owner,RecordSerializerFactory.get(),comp,this.perSortMem,32,0.8f);
  this.sorters.add(sorter);
  this.inputs.add(null);
}
