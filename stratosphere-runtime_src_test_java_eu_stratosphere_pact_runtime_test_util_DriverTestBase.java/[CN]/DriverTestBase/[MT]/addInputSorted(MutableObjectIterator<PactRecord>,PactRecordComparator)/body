{
  UnilateralSortMerger<PactRecord> sorter=new UnilateralSortMerger<PactRecord>(this.memManager,this.ioManager,input,this.owner,PactRecordSerializer.get(),comp,this.perSortMem,32,0.8f);
  this.sorters.add(sorter);
  this.inputs.add(null);
}
