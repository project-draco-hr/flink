{
  if (this.positionInSegment < this.segmentSize) {
    this.currentSegment.put(this.positionInSegment++,(byte)v);
  }
 else {
    nextSegment();
    this.currentSegment.put(this.positionInSegment++,(byte)v);
  }
}
