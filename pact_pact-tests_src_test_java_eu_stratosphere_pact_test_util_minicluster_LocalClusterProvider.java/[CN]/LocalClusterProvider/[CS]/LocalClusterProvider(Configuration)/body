{
  super(config);
  this.numTaskTrackers=Integer.parseInt(config.getString("LocalClusterProvider#numTaskTrackers","-1"));
  if (numTaskTrackers == -1) {
    throw new Exception("Number of task trackers was not specified");
  }
}
