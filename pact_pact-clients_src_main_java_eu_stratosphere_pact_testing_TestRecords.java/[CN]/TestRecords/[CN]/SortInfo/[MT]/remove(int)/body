{
  for (int index=0; index < this.sortKeys.size(); index++)   if (this.sortKeys.get(index) == removeIndex) {
    this.sortKeys.remove(index);
    this.keyClasses.remove(index);
    this.comparators.remove(index);
  }
}
