{
  PactRecord record=new PactRecord(values.length);
  for (int index=0; index < values.length; index++)   record.setField(index,values[index]);
  this.records.add(record);
  this.setEmpty(false);
  return this;
}
