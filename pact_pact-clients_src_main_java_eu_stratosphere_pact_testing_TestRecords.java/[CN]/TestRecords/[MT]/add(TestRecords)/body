{
  if (records.isEmpty())   this.setEmpty();
 else {
    for (    final PactRecord record : records)     this.records.add(record);
    this.setEmpty(false);
    records.close();
  }
  return this;
}
