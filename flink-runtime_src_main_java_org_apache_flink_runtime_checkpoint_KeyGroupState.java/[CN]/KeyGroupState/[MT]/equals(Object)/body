{
  if (obj instanceof KeyGroupState) {
    KeyGroupState other=(KeyGroupState)obj;
    return keyGroupState.equals(other.keyGroupState) && stateSize == other.stateSize && duration == other.duration;
  }
 else {
    return false;
  }
}
