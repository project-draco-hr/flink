{
  final Iterator<?> inputIterator=iterable.iterator();
  for (int input=0; input < numberOfElements && inputIterator.hasNext(); input++)   builder.append(inputIterator.next()).append(' ');
  if (inputIterator.hasNext())   builder.append("...");
}
