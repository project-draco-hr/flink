{
  JsonNode inputPair=JsonUtils.asArray(value1.getValue(),value2.getValue());
  if (this.comparison.evaluate(inputPair,this.getContext()) == BooleanNode.TRUE)   out.collect(PactNull.getInstance(),new PactJsonObject(this.getTransformation().evaluate(inputPair,this.getContext())));
}
