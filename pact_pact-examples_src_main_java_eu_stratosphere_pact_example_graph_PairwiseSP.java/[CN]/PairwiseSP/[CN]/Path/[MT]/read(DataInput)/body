{
  fromNode=in.readUTF();
  toNode=in.readUTF();
  length=in.readInt();
  hopCnt=in.readInt();
  if (hopCnt < 8) {
    hopsList=new String[8];
  }
 else {
    hopsList=new String[hopCnt * 2];
  }
  for (int i=0; i < hopCnt; i++) {
    hopsList[i]=in.readUTF();
  }
}
