{
  return new AbstractCollection<IJsonNode>(){
    @Override public Iterator<IJsonNode> iterator(){
      return this.iterator();
    }
    @Override public int size(){
      return AbstractArrayNode.this.size();
    }
  }
;
}
