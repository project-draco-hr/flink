{
  if (set1 == null || set2 == null) {
    throw new IllegalArgumentException();
  }
  HashSet<Object> set=new HashSet<Object>(set1);
  for (  Object o : set2) {
    if (!set.remove(o)) {
      return false;
    }
  }
  return set.isEmpty();
}
