{
  GroupedReduceInvokable<OUT> invokable=new GroupedReduceInvokable<OUT>(aggregate,keyPosition);
  SingleOutputStreamOperator<OUT,?> returnStream=addFunction("groupReduce",aggregate,outTypeWrapper,outTypeWrapper,invokable);
  return returnStream;
}
