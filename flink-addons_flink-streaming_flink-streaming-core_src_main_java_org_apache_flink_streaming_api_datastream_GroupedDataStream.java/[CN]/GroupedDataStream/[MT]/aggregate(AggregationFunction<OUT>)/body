{
  GroupedReduceInvokable<OUT> invokable=new GroupedReduceInvokable<OUT>(aggregate,keySelector);
  SingleOutputStreamOperator<OUT,?> returnStream=addFunction("groupReduce",aggregate,typeInfo,typeInfo,invokable);
  return returnStream;
}
