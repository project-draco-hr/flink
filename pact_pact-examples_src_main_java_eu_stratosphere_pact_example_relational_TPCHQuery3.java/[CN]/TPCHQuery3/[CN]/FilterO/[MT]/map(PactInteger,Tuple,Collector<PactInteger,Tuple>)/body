{
  try {
    if (Integer.parseInt(value.getStringValueAt(4).substring(0,4)) > this.YEAR_FILTER && value.getStringValueAt(2).equals("F") && value.getStringValueAt(5).startsWith(this.PRIO_FILTER)) {
      value.project(129);
      out.collect(oKey,value);
    }
  }
 catch (  final StringIndexOutOfBoundsException e) {
    LOGGER.error(e);
  }
catch (  final Exception ex) {
    LOGGER.error(ex);
  }
}
