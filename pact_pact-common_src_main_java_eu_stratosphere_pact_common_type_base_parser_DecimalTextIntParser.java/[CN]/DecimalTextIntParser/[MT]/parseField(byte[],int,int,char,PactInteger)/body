{
  int val=0;
  for (int i=startPos; i < length; i++) {
    if (bytes[i] == delim) {
      field.setValue(val);
      return i + 1;
    }
    if (bytes[i] < 48 || bytes[i] > 57) {
      return -1;
    }
    val*=10;
    val+=bytes[i] - 48;
  }
  field.setValue(val);
  return length;
}
