{
  if (negative) {
    if (val >= Integer.MIN_VALUE) {
      field.setValue((int)-val);
    }
 else {
      return -1;
    }
  }
 else {
    if (val <= Integer.MAX_VALUE) {
      field.setValue((int)val);
    }
 else {
      return -1;
    }
  }
  return position;
}
