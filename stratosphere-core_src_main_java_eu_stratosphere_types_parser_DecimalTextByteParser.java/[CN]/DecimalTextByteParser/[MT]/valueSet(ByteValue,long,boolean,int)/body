{
  if (negative) {
    if (val >= Byte.MIN_VALUE) {
      field.setValue((byte)-val);
    }
 else {
      return -1;
    }
  }
 else {
    if (val <= Byte.MAX_VALUE) {
      field.setValue((byte)val);
    }
 else {
      return -1;
    }
  }
  return position;
}
