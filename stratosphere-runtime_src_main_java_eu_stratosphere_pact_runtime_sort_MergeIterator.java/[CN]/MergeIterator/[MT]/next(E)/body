{
  if (this.heap.size() > 0) {
    final HeadStream<E> top=this.heap.peek();
    reuse=this.serializer.copy(top.getHead(),reuse);
    if (!top.nextHead()) {
      this.heap.poll();
    }
 else {
      this.heap.adjustTop();
    }
    return reuse;
  }
 else {
    return null;
  }
}
