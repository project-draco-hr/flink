{
  if (negative) {
    if (val >= Short.MIN_VALUE) {
      field.setValue((short)-val);
    }
 else {
      return -1;
    }
  }
 else {
    if (val <= Short.MAX_VALUE) {
      field.setValue((short)val);
    }
 else {
      return -1;
    }
  }
  return position;
}
