{
  if (characteristic == TimeCharacteristic.EventTime || characteristic == TimeCharacteristic.IngestionTime) {
    return this;
  }
 else {
    throw new InvalidProgramException("Cannot use EventTime policy in a dataflow that runs on " + characteristic);
  }
}
