{
  final SopremoModule module=new SopremoModule(this.operator.getName() + "_" + this.outputIndex,1,1);
  this.operator.withInputs(module.getInputs());
  module.getOutput(0).setInput(0,this.operator.getOutput(this.outputIndex));
  return module.asElementary();
}
