{
  try {
    for (    RecordWriterOutput<?> streamOutput : getOutputs()) {
      streamOutput.close();
    }
  }
  finally {
    for (    RecordWriterOutput<?> output : getOutputs()) {
      output.clearBuffers();
    }
  }
}
