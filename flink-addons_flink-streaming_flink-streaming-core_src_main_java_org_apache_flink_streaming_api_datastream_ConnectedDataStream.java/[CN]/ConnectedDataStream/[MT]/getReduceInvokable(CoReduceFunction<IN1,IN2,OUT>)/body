{
  CoReduceInvokable<IN1,IN2,OUT> invokable;
  if (isGrouped) {
    invokable=new CoGroupedReduceInvokable<IN1,IN2,OUT>(coReducer,keyPosition1,keyPosition2);
  }
 else {
    invokable=new CoReduceInvokable<IN1,IN2,OUT>(coReducer);
  }
  return invokable;
}
