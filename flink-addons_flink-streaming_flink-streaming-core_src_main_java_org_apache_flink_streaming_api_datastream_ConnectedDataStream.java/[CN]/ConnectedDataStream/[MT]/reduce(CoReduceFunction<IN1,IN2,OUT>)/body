{
  TypeInformation<OUT> outTypeInfo=TypeExtractor.createTypeInfo(CoReduceFunction.class,coReducer.getClass(),2,null,null);
  return addCoFunction("coReduce",coReducer,outTypeInfo,getReduceInvokable(coReducer));
}
