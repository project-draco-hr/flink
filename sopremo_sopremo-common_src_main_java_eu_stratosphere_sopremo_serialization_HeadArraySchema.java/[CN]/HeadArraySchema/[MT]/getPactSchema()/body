{
  @SuppressWarnings("unchecked") Class<? extends Value>[] schema=new Class[this.getHeadSize() + 1];
  for (int i=0; i <= this.getHeadSize(); i++)   schema[i]=JsonNodeWrapper.class;
  return schema;
}
