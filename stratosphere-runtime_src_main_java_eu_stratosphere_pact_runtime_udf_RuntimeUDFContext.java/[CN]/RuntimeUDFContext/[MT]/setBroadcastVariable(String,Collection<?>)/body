{
  if (this.broadcastVars.containsKey(name)) {
    throw new IllegalArgumentException("The broadcast variable '" + name + "' already exists and cannot be added.");
  }
  this.broadcastVars.put(name,value);
}
