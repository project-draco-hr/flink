{
  PactString line=record.getField(0,PactString.class);
  Matcher matcher=WORD_PATTERN.matcher(line.getValue());
  while (matcher.find())   out.collect(new PactRecord(new PactString(matcher.group(0).toLowerCase()),new PactInteger(1)));
}
