{
  DataStream<R> returnStream=new DataStreamSource<R>(environment,"iterationSource",null,null,true);
  jobGraphBuilder.addIterationHead(returnStream.getId(),this.getId(),iterationID,degreeOfParallelism,waitTime);
  return this.copy();
}
