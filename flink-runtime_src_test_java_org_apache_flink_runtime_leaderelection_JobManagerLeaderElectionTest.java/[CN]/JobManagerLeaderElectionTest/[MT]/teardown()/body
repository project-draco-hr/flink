{
  if (actorSystem != null) {
    JavaTestKit.shutdownActorSystem(actorSystem);
  }
  if (testingServer != null) {
    testingServer.stop();
  }
  if (executor != null) {
    executor.shutdownNow();
  }
}
