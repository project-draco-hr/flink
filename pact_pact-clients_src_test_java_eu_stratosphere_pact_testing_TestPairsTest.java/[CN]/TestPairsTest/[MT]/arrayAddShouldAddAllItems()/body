{
  AssertUtil.assertIteratorEquals("should be empty in the beginning",Arrays.asList().iterator(),this.pairs.iterator());
  this.pairs.add(new KeyValuePair<Key,Value>(new PactInteger(1),new PactString("test1")),new KeyValuePair<Key,Value>(new PactInteger(2),new PactString("test2")));
  AssertUtil.assertIteratorEquals("should contain two element after batch adding two items",Arrays.asList(new KeyValuePair<Key,Value>(new PactInteger(1),new PactString("test1")),new KeyValuePair<Key,Value>(new PactInteger(2),new PactString("test2"))).iterator(),this.pairs.iterator());
}
