{
  if (list1.size() + list2.size() == 0)   return 0;
  final int intersectionSize=intersectionSize(list1,list2,comparator);
  return MAX_DISTANCE * (list1.size() + list2.size() - 2 * intersectionSize) / (list1.size() + list2.size() - intersectionSize);
}
