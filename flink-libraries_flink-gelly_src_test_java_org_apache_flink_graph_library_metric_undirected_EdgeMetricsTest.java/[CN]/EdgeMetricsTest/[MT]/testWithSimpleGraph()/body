{
  Result expectedResult=new Result(6,7,2,6,13,4,1,3,6);
  Result edgeMetrics=new EdgeMetrics<IntValue,NullValue,NullValue>().run(undirectedSimpleGraph).execute();
  assertEquals(expectedResult,edgeMetrics);
}
