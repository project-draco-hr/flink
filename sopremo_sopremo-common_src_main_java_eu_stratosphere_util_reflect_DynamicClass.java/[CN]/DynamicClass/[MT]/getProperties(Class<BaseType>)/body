{
  return new FilteringIterable(this.getProperties(),new Predicate<DynamicProperty>(){
    @Override public boolean isTrue(    DynamicProperty property){
      return baseType.isAssignableFrom(property.getType());
    }
  }
);
}
