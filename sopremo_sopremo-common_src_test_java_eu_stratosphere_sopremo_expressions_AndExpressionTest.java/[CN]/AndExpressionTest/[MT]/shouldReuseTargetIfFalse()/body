{
  IJsonNode target=BooleanNode.TRUE;
  final IJsonNode result=new AndExpression(BooleanExpressionTest.TRUE,BooleanExpressionTest.FALSE).evaluate(IntNode.valueOf(42),target,this.context);
  Assert.assertEquals(BooleanNode.FALSE,result);
  Assert.assertSame(target,result);
}
