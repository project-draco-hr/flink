{
  IJsonNode target=BooleanNode.FALSE;
  final IJsonNode result=new AndExpression(BooleanExpressionTest.TRUE,BooleanExpressionTest.TRUE).evaluate(IntNode.valueOf(42),target,this.context);
  Assert.assertEquals(BooleanNode.TRUE,result);
  Assert.assertSame(target,result);
}
