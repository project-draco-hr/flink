{
  List<Parser> parserList=parsers.get(type);
  if (parserList == null)   parsers.put(type,parserList=new ArrayList<Parser>());
  int pos=Collections.binarySearch(parserList,parser,ParserComparator);
  if (pos < 0)   parserList.add(-pos - 1,parser);
 else   parserList.set(pos,parser);
}
