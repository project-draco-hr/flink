{
  if (cachedError != null) {
    return cachedError.getClass().getName() + ": " + cachedError.getMessage();
  }
  if (serializedError == null) {
    return "(null)";
  }
 else {
    return "(serialized)";
  }
}
