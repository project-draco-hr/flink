{
  Map<Class<? extends IJsonNode>,Coercer<?,?>> toCoercers=this.coercers.get(to);
  if (toCoercers == null)   this.coercers.put(to,toCoercers=new IdentityHashMap<Class<? extends IJsonNode>,Coercer<?,?>>());
  toCoercers.put(from,coercer);
}
