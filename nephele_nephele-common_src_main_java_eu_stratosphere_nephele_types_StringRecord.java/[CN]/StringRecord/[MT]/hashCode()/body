{
  int h=this.hash;
  if (h == 0 && this.length > 0) {
    int off=0;
    byte val[]=this.bytes;
    int len=this.length;
    for (int i=0; i < len; i++) {
      h=31 * h + val[off++];
    }
    this.hash=h;
  }
  return h;
}
