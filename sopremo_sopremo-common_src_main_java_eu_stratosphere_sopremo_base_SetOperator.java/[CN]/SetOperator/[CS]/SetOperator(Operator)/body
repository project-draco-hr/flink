{
  super(EvaluableExpression.IDENTITY,inputs);
  this.setKeyExtractors=new EvaluableExpression[inputs.length];
  for (int index=0; index < this.setKeyExtractors.length; index++)   this.setKeyExtractors[index]=new InputSelection(index);
}
