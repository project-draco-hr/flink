{
  Tuple value=new Tuple();
  value.addAttribute(custkey);
  value.addAttribute(name);
  value.addAttribute(address);
  value.addAttribute(nationkey);
  value.addAttribute(phone);
  value.addAttribute(acctbal);
  value.addAttribute("mktsegment");
  value.addAttribute(comment);
  return value;
}
