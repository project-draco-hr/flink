{
  if (method.getReturnType() != Void.TYPE || method.getParameterTypes()[0] != this.returnValue.getClass())   throw new IllegalArgumentException("Can't overload functions with different return types");
  super.addSignature(method);
}
