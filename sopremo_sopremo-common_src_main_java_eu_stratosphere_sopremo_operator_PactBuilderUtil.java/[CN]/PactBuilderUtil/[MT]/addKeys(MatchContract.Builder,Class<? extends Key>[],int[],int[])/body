{
  Preconditions.checkArgument(keyClasses.length == keyIndices1.length && keyClasses.length == keyIndices2.length,"Lenght of keyClasses and keyIndices must match.");
  for (int i=0; i < keyClasses.length; ++i) {
    builder.keyField(keyClasses[i],keyIndices1[i],keyIndices2[i]);
  }
}
