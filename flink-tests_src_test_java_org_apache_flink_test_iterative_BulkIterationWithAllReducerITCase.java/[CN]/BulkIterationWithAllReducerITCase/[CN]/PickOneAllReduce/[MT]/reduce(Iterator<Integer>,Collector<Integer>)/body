{
  if (bcValue == null) {
    return;
  }
  final int x=bcValue;
  while (records.hasNext()) {
    int y=records.next();
    if (y > x) {
      out.collect(y);
      return;
    }
  }
  out.collect(bcValue);
}
