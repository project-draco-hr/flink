{
  final Envelope envelope=new Envelope(SENDER_HINT_SEQUENCE_NUMBER,originalEnvelope.getJobID(),originalEnvelope.getSource());
  final SenderHintEvent senderEvent=new SenderHintEvent(source,remoteReceiver);
  envelope.serializeEventList(Arrays.asList(senderEvent));
  return envelope;
}
