{
  final Projection keyExtractor=new Projection().withKeyTransformation(this.partitionKey).withInputs(this.getInput(0));
  return SopremoModule.valueOf(this.getName(),new IntraSourceComparison(this.similarityCondition,this.resultProjection,this.idProjection,keyExtractor));
}
