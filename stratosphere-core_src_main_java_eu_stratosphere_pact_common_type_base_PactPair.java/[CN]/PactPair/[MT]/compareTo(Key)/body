{
  if (!(o instanceof PactPair<?,?>))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to N_Pair.");
  int result=this.first.compareTo(((PactPair<?,?>)o).first);
  if (result == 0)   result=this.second.compareTo(((PactPair<?,?>)o).second);
  return result;
}
