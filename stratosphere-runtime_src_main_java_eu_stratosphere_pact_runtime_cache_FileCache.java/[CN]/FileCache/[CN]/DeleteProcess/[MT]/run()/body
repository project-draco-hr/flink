{
synchronized (active) {
    if (active.get(new ImmutablePair(jobID,name))) {
      return;
    }
  }
  Path tmp=getTempDir(jobID,name);
  try {
    if (lfs.exists(tmp)) {
      lfs.delete(tmp,true);
    }
  }
 catch (  IOException e1) {
    e1.printStackTrace();
  }
}
