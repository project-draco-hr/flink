{
  @SuppressWarnings("unchecked") BinarySparseMatrix matrix=(BinarySparseMatrix)genMatrix;
  for (  final JsonNode row : matrix.getRows())   for (  final JsonNode column : matrix.get(row))   if (row.compareTo(column) < 0)   out.collect(key,JsonUtil.asArray(row,column));
}
