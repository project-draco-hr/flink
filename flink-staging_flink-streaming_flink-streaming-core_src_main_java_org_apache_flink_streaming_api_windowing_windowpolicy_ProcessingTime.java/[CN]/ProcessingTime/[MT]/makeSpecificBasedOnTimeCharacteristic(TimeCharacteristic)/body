{
  if (characteristic == TimeCharacteristic.ProcessingTime) {
    return this;
  }
 else {
    throw new InvalidProgramException("Cannot use ProcessingTime policy in a dataflow that runs on " + characteristic);
  }
}
