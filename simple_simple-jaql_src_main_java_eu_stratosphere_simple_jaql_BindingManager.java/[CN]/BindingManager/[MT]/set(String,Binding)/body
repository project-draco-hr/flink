{
  System.out.println("set " + name + " -> "+ binding+ " with transformation "+ binding.getTransformed().getClass().getSimpleName()+ " replacing "+ getAll().get(name));
  bindings.getLast().put(name,binding);
}
