{
  final StringBuilder builder=new StringBuilder("[\n");
  for (  final JsonNode row : this.getRows())   builder.append("[").append(row).append(": ").append(this.get(row)).append("]\n");
  return builder.append("]").toString();
}
