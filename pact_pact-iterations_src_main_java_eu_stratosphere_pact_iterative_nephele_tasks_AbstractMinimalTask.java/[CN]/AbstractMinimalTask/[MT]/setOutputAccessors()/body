{
  int numOutputs=this.config.getNumOutputs();
  for (int i=0; i < numOutputs; i++) {
    ChannelSelector selector=output.getWriters().get(i).getOutputGate().getChannelSelector();
    ((OutputEmitterV2)selector).setTypeAccessor((TypeAccessorsV2<Value>)outputAccessors[i]);
  }
}
