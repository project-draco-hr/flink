{
  final Class<?> contract=STUB_CONTRACTS.get(stubClass);
  if (contract == null && stubClass != Stub.class)   return getContractClass((Class<? extends Stub<?,?>>)stubClass.getSuperclass());
  return (Class<? extends Contract>)contract;
}
