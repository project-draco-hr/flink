{
synchronized (this.startStopLock) {
    if (jobManager != null) {
      jobManager.shutdown();
      jobManager=null;
    }
    if (runner != null) {
      runner.interrupt();
      runner.join();
      runner=null;
    }
  }
}
