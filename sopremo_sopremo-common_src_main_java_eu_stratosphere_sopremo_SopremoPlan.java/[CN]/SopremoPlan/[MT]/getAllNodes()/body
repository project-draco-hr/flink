{
  final List<Operator> nodes=new ArrayList<Operator>();
  DependencyAwareDAGTraverser.INSTANCE.traverse(this.sinks,new OperatorNavigator(),new DAGTraverseListener<Operator>(){
    @Override public void nodeTraversed(    Operator node){
      nodes.add(node);
    }
  }
);
  return nodes;
}
