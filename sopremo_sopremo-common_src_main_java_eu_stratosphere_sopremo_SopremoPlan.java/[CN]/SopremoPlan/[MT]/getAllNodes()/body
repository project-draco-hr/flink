{
  final List<Operator> nodes=new ArrayList<Operator>();
  DependencyAwareGraphTraverser.INSTANCE.traverse(this.sinks,new OperatorNavigator(),new GraphTraverseListener<Operator>(){
    @Override public void nodeTraversed(    Operator node){
      nodes.add(node);
    }
  }
);
  return nodes;
}
