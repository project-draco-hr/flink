{
  long sumStateSize=0;
  if (operatorStateHandles != null) {
    for (    T state : operatorStateHandles) {
      if (state != null) {
        sumStateSize+=state.getStateSize();
      }
    }
  }
  return sumStateSize;
}
