{
  T result=null;
  for (  T v : values) {
    if (result == null) {
      result=v;
    }
 else {
      result=wrappedFunction.reduce(result,v);
    }
  }
  if (result != null) {
    out.collect(result);
  }
}
