{
  if (message instanceof JobManagerMessages.LookupConnectionInformation) {
    getSender().tell(new JobManagerMessages.ConnectionInformation(ConnectionInfoLookupResponse.createReceiverFoundAndReady(receiverID)),getSelf());
  }
 else {
    super.onReceive(message);
  }
}
