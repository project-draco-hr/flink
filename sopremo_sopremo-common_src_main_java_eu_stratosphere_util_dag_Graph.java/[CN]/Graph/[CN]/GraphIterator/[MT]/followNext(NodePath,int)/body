{
  NodePath incoming=path.getIncoming();
  if (incoming == null)   return this.noMoreElements();
  if (nextIndex < incoming.getOutgoingCount())   return incoming.followConnection(nextIndex);
  return this.followNext(incoming,incoming.getIndex() + 1);
}
