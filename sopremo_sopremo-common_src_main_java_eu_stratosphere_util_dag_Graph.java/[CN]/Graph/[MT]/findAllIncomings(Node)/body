{
  return new FilteringIterable<Graph<Node>.NodePath>(this,new Predicate<Graph<Node>.NodePath>(){
    @Override public boolean isTrue(    Graph<Node>.NodePath param){
      for (      Node outgoing : param.getOutgoings())       if (outgoing == node)       return true;
      return false;
    }
  }
);
}
