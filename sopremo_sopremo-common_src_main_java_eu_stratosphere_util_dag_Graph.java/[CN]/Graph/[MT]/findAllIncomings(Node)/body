{
  return new FilteringIterable<Graph<Node>.NodePath>(this,new Predicate<Graph<Node>.NodePath>(){
    @Override public boolean isTrue(    final Graph<Node>.NodePath param){
      for (      final Node outgoing : param.getOutgoings())       if (outgoing == node)       return true;
      return false;
    }
  }
);
}
