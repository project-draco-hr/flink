{
  return new ZooKeeperCompletedCheckpointStore(maxNumberOfCheckpointsToRetain,userLoader,ZooKeeper.createClient(),CheckpointsPath,new StateStorageHelper<CompletedCheckpoint>(){
    @Override public StateHandle<CompletedCheckpoint> store(    CompletedCheckpoint state) throws Exception {
      return new LocalStateHandle<>(state);
    }
  }
);
}
