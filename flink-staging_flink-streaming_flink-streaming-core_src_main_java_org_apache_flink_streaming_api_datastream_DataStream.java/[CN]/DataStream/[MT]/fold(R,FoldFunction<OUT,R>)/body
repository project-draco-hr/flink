{
  TypeInformation<R> outType=TypeExtractor.getFoldReturnTypes(clean(folder),getType(),Utils.getCallLocationName(),true);
  return transform("Fold",outType,new StreamFold<OUT,R>(clean(folder),initialValue,outType));
}
