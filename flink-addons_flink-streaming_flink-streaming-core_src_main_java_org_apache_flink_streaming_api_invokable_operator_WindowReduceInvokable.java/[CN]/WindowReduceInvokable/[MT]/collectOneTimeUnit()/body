{
  ArrayList<IN> list;
  list=new ArrayList<IN>();
  long startTime=System.currentTimeMillis();
  do {
    list.add(reuse.getObject());
    resetReuse();
  }
 while ((reuse=loadNextRecord()) != null && System.currentTimeMillis() - startTime < timeUnitInMillis);
  state.pushBack(list);
}
