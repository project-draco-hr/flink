{
  while (true) {
    try {
      Thread.sleep(slideSize);
    }
 catch (    InterruptedException e) {
    }
    if (isRunning) {
      window.checkBatchEnd(System.currentTimeMillis());
    }
 else {
      break;
    }
  }
}
