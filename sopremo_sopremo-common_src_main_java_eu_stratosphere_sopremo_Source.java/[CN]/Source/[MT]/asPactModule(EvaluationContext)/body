{
  if (this.type == PersistenceType.ADHOC)   throw new UnsupportedOperationException();
  final PactModule pactModule=new PactModule(this.toString(),0,1);
  final DataSourceContract<PactJsonObject.Key,PactJsonObject> contract=new DataSourceContract<PactJsonObject.Key,PactJsonObject>(JsonInputFormat.class,this.inputName,this.inputName);
  pactModule.getOutput(0).setInput(contract);
  return pactModule;
}
