{
  TypeInformation<IN> inTupleTypeInfo=configuration.getTypeInfoIn1(streamVertex.userClassLoader);
  if (inTupleTypeInfo != null) {
    inputSerializer=new StreamRecordSerializer<IN>(inTupleTypeInfo);
  }
}
