{
  Iterator<Edge> it=this.iterator();
  StringBuilder sb=new StringBuilder("[ ");
  while (it.hasNext()) {
    sb.append("( " + it.next().toString() + " ), ");
  }
  sb.append(" ]");
  return sb.toString();
}
