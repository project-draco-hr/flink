{
  Iterator<Entry<Class<? extends InputType>,Handler>> iterator=handlers.entrySet().iterator();
  for (; iterator.hasNext(); ) {
    if (iterator.next().getValue() == type)     iterator.remove();
  }
}
