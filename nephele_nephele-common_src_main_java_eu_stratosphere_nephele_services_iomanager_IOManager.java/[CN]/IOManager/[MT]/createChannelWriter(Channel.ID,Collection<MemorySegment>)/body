{
  if (isClosed) {
    throw new IllegalStateException("IO-Manger is closed.");
  }
  return new ChannelWriter(channelID,writer.requestQueue,IOManager.createBuffer(Buffer.Type.OUTPUT,freeSegments),false);
}
