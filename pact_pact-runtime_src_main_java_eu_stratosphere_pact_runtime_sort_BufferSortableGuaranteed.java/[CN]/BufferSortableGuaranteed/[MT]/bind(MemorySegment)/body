{
  if (super.bind(memory)) {
    outputView=new HeapStackDataOutputView(memory.randomAccessView.getBackingArray(),memory.randomAccessView.translateOffset(0),memory.randomAccessView.size());
    reset();
    return true;
  }
 else {
    return false;
  }
}
