{
  if (super.bind(memory)) {
    this.outputView=new HeapStackDataOutputView(memory.randomAccessView.getBackingArray(),memory.randomAccessView.translateOffset(0),memory.randomAccessView.size());
    this.randomAccessView=memory.randomAccessView;
    reset();
    return true;
  }
 else {
    return false;
  }
}
