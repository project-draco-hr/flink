{
  if (super.bind(memory)) {
    this.outputView=new HeapStackDataOutputView(memory.getBackingArray(),memory.translateOffset(0),memory.size());
    reset();
    return true;
  }
 else {
    return false;
  }
}
