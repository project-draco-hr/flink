{
  if (this.isMutable) {
    while (recordIterator.next(reuse) != null) {
      sinkFunction.invoke((IN)reuse.getTuple());
    }
  }
 else {
    while (recordIterator.next(reuse) != null) {
      sinkFunction.invoke((IN)reuse.getTuple());
      resetReuse();
    }
  }
}
