{
  int key=((readBuffer[pos] & 0xFF) << 24) | ((readBuffer[pos + 1] & 0xFF) << 16) | ((readBuffer[pos + 2] & 0xFF) << 8)| (readBuffer[pos + 3] & 0xFF);
  this.key.setValue(key);
  target.setField(0,this.key);
  target.setField(1,this.payLoad);
  return true;
}
