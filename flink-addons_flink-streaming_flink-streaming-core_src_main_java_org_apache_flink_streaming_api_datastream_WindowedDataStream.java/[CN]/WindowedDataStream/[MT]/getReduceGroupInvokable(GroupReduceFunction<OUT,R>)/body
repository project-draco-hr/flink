{
  StreamInvokable<OUT,R> invokable;
  if (isGrouped) {
    invokable=new GroupedWindowInvokable<OUT,R>(reducer,keySelector,getDistributedTriggers(),getDistributedEvicters(),getCentralTriggers());
  }
 else {
    invokable=new WindowGroupReduceInvokable<OUT,R>(reducer,getTriggers(),getEvicters());
  }
  return invokable;
}
