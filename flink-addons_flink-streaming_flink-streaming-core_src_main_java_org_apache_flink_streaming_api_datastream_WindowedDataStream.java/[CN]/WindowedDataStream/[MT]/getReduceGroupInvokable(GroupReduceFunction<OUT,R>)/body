{
  StreamInvokable<OUT,R> invokable;
  if (isGrouped) {
    invokable=new GroupedWindowingInvokable<OUT,R>(reducer,keySelector,getDistributedTriggers(),getDistributedEvicters(),getCentralTriggers());
  }
 else {
    invokable=new WindowingGroupInvokable<OUT,R>(reducer,getTriggers(),getEvicters());
  }
  return invokable;
}
