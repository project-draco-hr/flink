{
  StreamInvokable<OUT,OUT> invokable=getReduceInvokable(aggregator);
  SingleOutputStreamOperator<OUT,?> returnStream=dataStream.transform("windowReduce",getType(),invokable);
  return returnStream;
}
