{
  StringBuilder builder=new StringBuilder("ObjectSchema [");
  for (int index=0; index < mappings.size(); index++)   builder.append(mappings.get(index)).append(", ");
  builder.append("<other>]");
  return builder.toString();
}
