{
  final StringBuilder builder=new StringBuilder("ObjectSchema [");
  for (int index=0; index < this.mappings.size(); index++)   builder.append(this.mappings.get(index)).append(", ");
  builder.append("<other>]");
  return builder.toString();
}
