{
  ExecutionEnvironment env=ExecutionEnvironment.createLocalEnvironment();
  TableEnvironment tableEnv=new TableEnvironment();
  DataSet<WC> input1=env.fromElements(new WC(1,"d"),new WC(1,"d"),new WC(1,"d"));
  Table table1=tableEnv.fromDataSet(input1);
  DataSet<WC> input2=env.fromElements(new WC(1,"d"),new WC(1,"d"),new WC(1,"d"));
  Table table2=tableEnv.fromDataSet(input2);
  String result=table1.unionAll(table2).explain();
  String source=new Scanner(new File(testFilePath + "../../src/test/scala/resources/testUnion0.out")).useDelimiter("\\A").next();
  assertEquals(result,source);
}
