{
  if (args.length == 3) {
    host=args[0];
    port=Integer.parseInt(args[1]);
    topic=args[2];
    return true;
  }
 else {
    System.err.println("Usage: KafkaConsumerExample <host> <port> <topic>");
    return false;
  }
}
