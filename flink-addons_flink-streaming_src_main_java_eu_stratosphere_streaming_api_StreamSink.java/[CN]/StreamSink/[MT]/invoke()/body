{
  try {
    userFunction=UserFunction.newInstance();
  }
 catch (  Exception e) {
  }
  boolean hasInput=true;
  while (hasInput) {
    hasInput=false;
    if (input.hasNext()) {
      hasInput=true;
      userFunction.invoke(input.next(),input);
    }
  }
}
