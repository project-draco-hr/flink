{
  RangeCalculator[] rangeCalculators={new LastBitsToRange(8),new LastBitsToRange(3),new LastBitsToRange(3)};
  Iterator<Integer> importIterator=new StepRangeIterator(-300000,300000,17);
  HashFunction hashFunction=new RotateHashFunction();
  HashTester ht=new HashTester(hashFunction,importIterator,rangeCalculators);
  BucketBoundaries[] boundaries={new BucketBoundaries(90,150,5,0.05),new BucketBoundaries(0,30,5,0.1),new BucketBoundaries(0,10,5000,0.1)};
  ht.runTest(boundaries);
}
