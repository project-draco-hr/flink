{
  final PactModule pactModule=new PactModule(this.toString(),1,0);
  final DataSinkContract<PactJsonObject.Key,PactJsonObject> contract=new DataSinkContract<PactJsonObject.Key,PactJsonObject>(JsonOutputFormat.class,this.outputName,this.outputName);
  contract.setInput(pactModule.getInput(0));
  pactModule.addInternalOutput(contract);
  return pactModule;
}
