{
  if (Double.compare(value1.getValue(),value2.getValue()) == 0)   return 0;
  double diff=Math.abs(value1.getValue() - value2.getValue());
  if (diff <= this.delta)   return diff;
  return NO_MATCH;
}
