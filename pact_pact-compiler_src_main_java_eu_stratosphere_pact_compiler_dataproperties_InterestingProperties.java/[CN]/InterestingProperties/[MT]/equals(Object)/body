{
  if (obj != null && obj instanceof InterestingProperties) {
    InterestingProperties other=(InterestingProperties)obj;
    return this.globalProps.equals(other.globalProps) && this.localProps.equals(other.localProps) && this.maximalCostsGlobal.equals(other.maximalCostsGlobal)&& this.maximalCostsLocal.equals(other.maximalCostsLocal);
  }
 else {
    return false;
  }
}
