{
  if (input.closed) {
    throw new IllegalStateException("An iterative data stream can only be closed once. Use union to close with multiple stream.");
  }
  input.closed=true;
  input.streamGraph.addIterationTail((List)feedbackStream.unionedStreams,input.iterationID,true);
  return feedbackStream;
}
