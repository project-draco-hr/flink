{
  final SopremoModule sopremoModule=new SopremoModule(getName(),2,1);
  final Projection left=new Projection(inputKeyExtractor.asExpression(),EvaluationExpression.SAME_VALUE,sopremoModule.getInput(0));
  left.setName("InputKeyExtractor");
  final Projection right=new Projection(dictionaryKeyExtraction,EvaluationExpression.SAME_VALUE,sopremoModule.getInput(1));
  right.setName("DictionaryKeyExtraction");
  sopremoModule.getOutput(0).setInput(0,new ReplaceWithRightInput(inputKeyExtractor,left,right));
  return sopremoModule;
}
