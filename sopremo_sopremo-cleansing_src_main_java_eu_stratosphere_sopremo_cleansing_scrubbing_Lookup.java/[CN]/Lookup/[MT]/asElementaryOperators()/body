{
  final SopremoModule sopremoModule=new SopremoModule(this.getName(),2,1);
  final Projection left=new Projection(this.inputKeyExtractor.asExpression(),EvaluationExpression.SAME_VALUE,sopremoModule.getInput(0));
  final Projection right=new Projection(this.dictionaryKeyExtraction,this.dictionaryValueExtraction,sopremoModule.getInput(1));
  sopremoModule.getOutput(0).setInput(0,new ReplaceWithRightInput(this.inputKeyExtractor,left,right));
  return sopremoModule;
}
