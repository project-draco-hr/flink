{
  List<FrequentItemset> fis=this.findUnaryFrequentItemSets(values);
  this.emit(fis,out);
  int setSize=1;
  while (setSize < this.maxSetSize) {
    setSize++;
    final List<FrequentItemset> candidates=FrequentItemset.generateCandidates(fis);
    fis=this.findFrequentItemsets(candidates,values);
    if (fis.isEmpty())     break;
    this.emit(fis,out);
  }
}
