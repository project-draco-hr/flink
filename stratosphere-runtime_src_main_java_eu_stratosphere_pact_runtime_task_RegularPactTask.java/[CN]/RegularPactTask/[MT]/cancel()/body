{
  this.running=false;
  if (LOG.isInfoEnabled())   LOG.info(formatLogString("Cancelling PACT code"));
  try {
    if (this.driver != null) {
      this.driver.cancel();
    }
  }
  finally {
    closeLocalStrategiesAndCaches();
  }
}
