{
  double distance=0;
  for (int index=0; index < Math.max(value1.size(),value2.size()); index++) {
    final double elemDistance=this.getDistance(value1.get(index),value2.get(index));
    if (elemDistance < 0)     return elemDistance;
    distance+=elemDistance;
  }
  return distance;
}
