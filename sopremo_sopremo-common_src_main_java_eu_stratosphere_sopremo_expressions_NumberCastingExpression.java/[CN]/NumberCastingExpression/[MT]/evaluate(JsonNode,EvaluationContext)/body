{
  if (!(node instanceof NumericNode))   throw new EvaluationException(String.format("The given node %s is not a number and cannot be casted",node));
  return NumberCoercer.INSTANCE.coerce((NumericNode)node,this.targetType);
}
