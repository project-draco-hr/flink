{
  for (  Contract contract : contracts)   contract.accept(this);
  for (int index=0; index < contracts.length; index++)   if (!(contracts[index] instanceof GenericDataSink))   contracts[index]=this.replaceWithDefaultOutput(contracts[index]);
  return contracts;
}
