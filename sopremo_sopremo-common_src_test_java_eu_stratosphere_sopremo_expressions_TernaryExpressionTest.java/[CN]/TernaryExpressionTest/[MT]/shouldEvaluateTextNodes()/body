{
  final IJsonNode result=new TernaryExpression(new ConstantExpression(TextNode.valueOf("a")),new ConstantExpression(IntNode.valueOf(0))).evaluate(IntNode.valueOf(42),this.context);
  Assert.assertEquals(IntNode.valueOf(0),result);
}
