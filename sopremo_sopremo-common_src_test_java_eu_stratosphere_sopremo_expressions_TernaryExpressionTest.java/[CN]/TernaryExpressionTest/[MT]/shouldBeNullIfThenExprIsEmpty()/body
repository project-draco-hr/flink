{
  final IJsonNode result=new TernaryExpression(new InputSelection(1),new ConstantExpression(IntNode.valueOf(0))).evaluate(createArrayNode(BooleanNode.TRUE,BooleanNode.FALSE),null,this.context);
  Assert.assertEquals(NullNode.getInstance(),result);
}
