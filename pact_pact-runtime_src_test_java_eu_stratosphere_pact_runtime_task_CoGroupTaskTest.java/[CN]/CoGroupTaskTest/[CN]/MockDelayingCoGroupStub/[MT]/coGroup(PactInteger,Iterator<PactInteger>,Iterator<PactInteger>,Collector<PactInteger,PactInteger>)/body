{
  while (values1.hasNext()) {
    try {
      Thread.sleep(100);
    }
 catch (    InterruptedException e) {
    }
    values1.next();
  }
  while (values2.hasNext()) {
    try {
      Thread.sleep(100);
    }
 catch (    InterruptedException e) {
    }
    values2.next();
  }
}
