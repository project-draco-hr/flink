{
  this.outputs=outputs;
  this.componentID=sourceInstanceID;
  exactlyOnce=false;
  if (exactlyOnce) {
    this.buffer=new ExactlyOnceFaultToleranceBuffer(numberOfChannels,sourceInstanceID);
  }
 else {
    this.buffer=new AtLeastOnceFaultToleranceBuffer(numberOfChannels,sourceInstanceID);
  }
  tracker=new PerformanceTracker("pc",1000,1000,10000,"/home/strato/stratosphere-distrib/log/counter/Buffer" + componentName + sourceInstanceID);
  counter=new PerformanceCounter("pc",1000,1000,10000,"/home/strato/stratosphere-distrib/log/counter/Emitted" + componentName + sourceInstanceID);
}
