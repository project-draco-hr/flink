{
  for (int index=0; index < node.size(); index++)   if (aggregationTarget.get(index).isMissing() && !node.get(index).isMissing())   aggregationTarget.set(index,node.get(index));
  return aggregationTarget;
}
