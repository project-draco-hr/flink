{
  IArrayNode array=(IArrayNode)node, unifiedArray=(IArrayNode)aggregationTarget;
  for (int index=0; index < array.size(); index++)   if (unifiedArray.get(index).isMissing() && !array.get(index).isMissing())   unifiedArray.set(index,array.get(index));
  return unifiedArray;
}
