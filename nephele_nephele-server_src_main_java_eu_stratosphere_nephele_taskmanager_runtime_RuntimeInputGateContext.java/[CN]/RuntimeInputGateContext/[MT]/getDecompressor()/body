{
  if (this.decompressor == null) {
    this.decompressor=CompressionLoader.getDecompressorByCompressionLevel(this.inputGate.getCompressionLevel(),new CompressionBufferProvider(this));
  }
 else {
    this.decompressor.increaseChannelCounter();
  }
  return this.decompressor;
}
