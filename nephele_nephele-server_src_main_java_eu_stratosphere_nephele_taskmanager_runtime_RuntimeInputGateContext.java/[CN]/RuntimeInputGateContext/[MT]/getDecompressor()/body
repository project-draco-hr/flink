{
  if (this.decompressor == null) {
    this.decompressor=CompressionLoader.getDecompressorByCompressionLevel(this.inputGate.getCompressionLevel(),new CompressionBufferProvider(this,true));
  }
 else {
    this.decompressor.increaseChannelCounter();
  }
  return this.decompressor;
}
