{
  BatchReduceInvokable<OUT> invokable;
  if (isGrouped) {
    invokable=new GroupedBatchReduceInvokable<OUT>(reducer,batchSize,slideSize,keySelector);
  }
 else {
    invokable=new BatchReduceInvokable<OUT>(reducer,batchSize,slideSize);
  }
  return invokable;
}
