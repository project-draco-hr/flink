{
  StringList values=new StringList();
  while (values1.hasNext())   values.add(new PactString(values1.next().getValue()));
  while (values2.hasNext())   values.add(new PactString(values2.next().getValue()));
  out.collect(key,values);
}
