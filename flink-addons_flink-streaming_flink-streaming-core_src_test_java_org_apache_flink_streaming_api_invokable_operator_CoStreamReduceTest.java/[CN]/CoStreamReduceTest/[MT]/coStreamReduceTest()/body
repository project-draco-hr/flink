{
  CoReduceInvokable<Integer,String,Integer> coReduce=new CoReduceInvokable<Integer,String,Integer>(new MyCoReduceFunction());
  List<Integer> expected1=Arrays.asList(1,9,2,99,6,998,24);
  List<Integer> result=MockCoInvokable.createAndExecute(coReduce,Arrays.asList(1,2,3,4),Arrays.asList("9","9","8"));
  assertEquals(expected1,result);
}
