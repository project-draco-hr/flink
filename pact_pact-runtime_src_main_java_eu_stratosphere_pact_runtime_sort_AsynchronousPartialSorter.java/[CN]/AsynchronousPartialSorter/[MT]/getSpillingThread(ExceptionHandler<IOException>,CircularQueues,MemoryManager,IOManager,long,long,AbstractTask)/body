{
  this.bufferIterator=new BufferQueueIterator(queues);
  setResultIterator(this.bufferIterator);
  return new SpillingThread(exceptionHandler,queues,memoryManager,ioManager,this.keySerialization,this.valueSerialization,writeMemSize,readMemSize,parentTask);
}
