{
  super(memoryManager,null,input,parentTask,serializer,comparator,totalMemory,totalMemory < 2 * MIN_NUM_SORT_MEM_SEGMENTS * memoryManager.getPageSize() ? 1 : Math.max((int)Math.ceil(((double)totalMemory) / MAX_MEM_PER_PARTIAL_SORT),2),2,0.0f,true);
}
