{
  WebLogAnalysis relOLAP=new WebLogAnalysis();
  Job plan=relOLAP.createJob(config.getString("WebLogAnalysisTest#NoSubtasks","1"),getFilesystemProvider().getURIPrefix() + docsPath,getFilesystemProvider().getURIPrefix() + ranksPath,getFilesystemProvider().getURIPrefix() + visitsPath,getFilesystemProvider().getURIPrefix() + resultPath);
  PactCompiler pc=new PactCompiler(new DataStatistics());
  OptimizedPlan op=pc.compile(plan);
  NepheleJobGraphGenerator jgg=new NepheleJobGraphGenerator();
  return jgg.compileJobGraph(op);
}
