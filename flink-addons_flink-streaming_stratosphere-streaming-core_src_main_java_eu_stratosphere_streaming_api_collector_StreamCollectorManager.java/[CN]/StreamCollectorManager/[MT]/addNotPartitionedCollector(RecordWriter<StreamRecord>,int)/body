{
  StreamCollector<Tuple> collector=new StreamCollector<Tuple>(batchSize,batchTimeout,channelID,serializationDelegate,output,0);
  NotPartitionedCollector<Tuple> notPartitionedCollector=new NotPartitionedCollector<Tuple>(collector);
  collectors.add(notPartitionedCollector);
  return notPartitionedCollector;
}
