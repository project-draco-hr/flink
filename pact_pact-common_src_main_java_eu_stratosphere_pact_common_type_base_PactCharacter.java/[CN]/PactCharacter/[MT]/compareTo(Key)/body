{
  if (!(o instanceof PactCharacter))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to PactChar!");
  final int other=((PactCharacter)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
