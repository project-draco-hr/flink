{
  if (!(otherNode instanceof JsonNodeWrapper))   throw new IllegalArgumentException("Other node is not a JsonNodeWrapper");
  if (this.value.getType() != otherNode.getType())   this.value=ReflectUtil.newInstance(otherNode.getType().getClazz());
  this.value.copyValueFrom(((JsonNodeWrapper)otherNode).value);
}
