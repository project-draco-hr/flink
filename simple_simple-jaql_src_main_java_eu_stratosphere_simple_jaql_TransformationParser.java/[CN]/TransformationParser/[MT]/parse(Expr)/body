{
  Transformation transformation=(Transformation)this.transformationConverter.handleRecursively(ExprNavigator.INSTANCE,expr);
  if (transformation == null) {
    transformation=new Transformation();
    EvaluableExpression path=this.queryParser.parsePath(expr);
    transformation.addMapping(new ValueAssignment(path));
  }
  return transformation;
}
