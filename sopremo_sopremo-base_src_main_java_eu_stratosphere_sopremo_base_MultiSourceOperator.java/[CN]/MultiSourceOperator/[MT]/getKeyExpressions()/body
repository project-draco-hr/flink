{
  return CollectionUtil.mergeUnique(new ConversionIterable<JsonStream,List<? extends EvaluationExpression>>(getInputs()){
    @Override protected List<? extends EvaluationExpression> convert(    JsonStream stream){
      return getKeyExpressions(stream);
    }
  }
);
}
