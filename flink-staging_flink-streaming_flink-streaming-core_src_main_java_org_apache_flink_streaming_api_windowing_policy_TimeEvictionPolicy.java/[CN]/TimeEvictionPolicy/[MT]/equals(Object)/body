{
  if (other == null || !(other instanceof TimeEvictionPolicy)) {
    return false;
  }
 else {
    try {
      @SuppressWarnings("unchecked") TimeEvictionPolicy<DATA> otherPolicy=(TimeEvictionPolicy<DATA>)other;
      return granularity == otherPolicy.granularity && timestampWrapper.equals(otherPolicy.timestampWrapper);
    }
 catch (    ClassCastException e) {
      return false;
    }
  }
}
