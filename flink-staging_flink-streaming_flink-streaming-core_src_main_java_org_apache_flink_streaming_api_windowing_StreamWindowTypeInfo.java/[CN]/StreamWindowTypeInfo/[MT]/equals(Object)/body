{
  if (obj instanceof StreamWindowTypeInfo) {
    @SuppressWarnings("unchecked") StreamWindowTypeInfo<T> streamWindowTypeInfo=(StreamWindowTypeInfo<T>)obj;
    return streamWindowTypeInfo.canEqual(this) && innerType.equals(streamWindowTypeInfo.innerType);
  }
 else {
    return false;
  }
}
