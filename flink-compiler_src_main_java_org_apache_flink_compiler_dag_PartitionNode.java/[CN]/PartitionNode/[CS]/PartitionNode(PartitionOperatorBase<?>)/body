{
  super(operator);
  OperatorDescriptorSingle descr=new PartitionDescriptor(this.getPactContract().getPartitionMethod(),this.keys,operator.getCustomPartitioner());
  this.possibleProperties=Collections.singletonList(descr);
}
