{
  if (reachableNodes instanceof List)   return (List<Operator>)reachableNodes;
  ArrayList<Operator> operatorList=new ArrayList<Operator>();
  for (  Operator operator : reachableNodes)   operatorList.add(operator);
  return operatorList;
}
