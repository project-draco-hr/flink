{
  JsonPath[] elements=new JsonPath[constants.length];
  for (int index=0; index < elements.length; index++)   if (constants[index] instanceof JsonPath)   elements[index]=(JsonPath)constants[index];
 else   elements[index]=new JsonPath.Constant(constants[index]);
  return new JsonPath.ArrayCreation(elements);
}
