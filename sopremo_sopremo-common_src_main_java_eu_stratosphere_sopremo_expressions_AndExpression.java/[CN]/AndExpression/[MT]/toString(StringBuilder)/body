{
  builder.append("(").append(this.expressions[0]).append(")");
  for (int index=1; index < this.expressions.length; index++)   builder.append(" AND ").append("(").append(this.expressions[index]).append(")");
}
