{
  if (fileOutput) {
    return env.readCsvFile(pointsPath).fieldDelimiter('|').includeFields(true,true).types(Double.class,Double.class).map(new TuplePointConverter());
  }
 else {
    throw new UnsupportedOperationException("Use file output");
  }
}
