{
  if (newExecutionState == ExecutionState.FAILED) {
    LOG.error(optionalMessage);
  }
  this.taskManager.executionStateChanged(this.environment.getJobID(),this.id,this.environment,newExecutionState,optionalMessage);
}
