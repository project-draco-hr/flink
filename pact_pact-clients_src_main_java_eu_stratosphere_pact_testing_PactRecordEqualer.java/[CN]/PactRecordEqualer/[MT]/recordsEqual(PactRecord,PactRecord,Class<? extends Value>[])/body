{
  if (object1.getNumFields() != schema.length)   return false;
  for (int index=0; index < schema.length; index++)   if (!Equaler.SafeEquals.equal(object1.getField(index,schema[index]),object2.getField(index,schema[index])))   return false;
  return true;
}
