{
  super.configure(parameters);
  final String encoding=parameters.getString(PARAMETER_ENCODING,null);
  if (encoding != null)   this.encoding=Charset.forName(encoding);
 else   this.encoding=Charset.forName("UTF8");
}
