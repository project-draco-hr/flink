{
  final Object previousGate=this.channelGroupMap.put(sourceChannelID,inputGate);
  if (previousGate != null) {
    LOG.error("Source channel ID has been previously registered to input gate " + inputGate.getJobID() + ", "+ inputGate.getIndex());
  }
}
