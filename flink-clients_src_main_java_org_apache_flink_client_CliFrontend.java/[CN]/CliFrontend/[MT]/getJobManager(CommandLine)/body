{
  String jobManagerAddressStr=getJobManagerAddressString(line);
  if (jobManagerAddressStr == null) {
    return null;
  }
  return JobManager.getJobManager(RemoteExecutor.getInetFromHostport(jobManagerAddressStr),ActorSystem.create("CliFrontendActorSystem",AkkaUtils.getDefaultAkkaConfig()),getAkkaTimeout());
}
