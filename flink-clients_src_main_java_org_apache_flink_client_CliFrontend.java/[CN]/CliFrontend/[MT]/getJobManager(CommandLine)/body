{
  InetSocketAddress jobManagerAddress=getJobManagerAddress(line);
  if (jobManagerAddress == null) {
    return null;
  }
  return JobManager.getJobManager(jobManagerAddress,ActorSystem.create("CliFrontendActorSystem",AkkaUtils.getDefaultActorSystemConfig()));
}
