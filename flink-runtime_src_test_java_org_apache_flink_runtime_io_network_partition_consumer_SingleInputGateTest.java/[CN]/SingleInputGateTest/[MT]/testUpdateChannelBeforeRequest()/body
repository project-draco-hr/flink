{
  SingleInputGate inputGate=new SingleInputGate("t1",new JobID(),new ExecutionAttemptID(),new IntermediateDataSetID(),0,1,mock(PartitionStateChecker.class));
  ResultPartitionManager partitionManager=mock(ResultPartitionManager.class);
  InputChannel unknown=new UnknownInputChannel(inputGate,0,new ResultPartitionID(),partitionManager,new TaskEventDispatcher(),new LocalConnectionManager(),new Tuple2<>(0,0));
  inputGate.setInputChannel(unknown.partitionId.getPartitionId(),unknown);
  inputGate.updateInputChannel(new InputChannelDeploymentDescriptor(unknown.partitionId,ResultPartitionLocation.createLocal()));
  verify(partitionManager,never()).createSubpartitionView(any(ResultPartitionID.class),anyInt(),any(BufferProvider.class));
}
