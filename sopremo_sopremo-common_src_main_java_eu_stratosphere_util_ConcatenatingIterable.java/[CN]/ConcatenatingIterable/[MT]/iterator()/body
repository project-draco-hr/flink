{
  return new ConcatenatingIterator<T>(new ConversionIterator<Iterable<T>,Iterator<T>>(this.inputs.iterator()){
    @Override protected Iterator<T> convert(    Iterable<T> inputObject){
      return inputObject.iterator();
    }
  }
);
}
