{
  return new ConcatenatingIterator<T>(new ConversionIterator<Iterable<? extends T>,Iterator<? extends T>>(this.inputs.iterator()){
    @Override protected Iterator<? extends T> convert(    final Iterable<? extends T> inputObject){
      return inputObject.iterator();
    }
  }
);
}
