{
  return "The given HDFS file URI (" + path.toString() + ") did not describe the HDFS Namenode."+ " The attempt to use a default HDFS configuration, as specified in the '"+ HDFS_DEFAULT_CONFIG+ "' or '"+ HDFS_SITE_CONFIG+ "' config parameter failed due to the following problem: ";
}
