{
  for (int i=0; i < tasks.length; i++) {
    tasks[i].start();
  }
  try {
    for (int i=0; i < tasks.length; i++) {
      tasks[i].join();
    }
  }
 catch (  InterruptedException iex) {
    throw new RuntimeException("Interrupted!");
  }
}
