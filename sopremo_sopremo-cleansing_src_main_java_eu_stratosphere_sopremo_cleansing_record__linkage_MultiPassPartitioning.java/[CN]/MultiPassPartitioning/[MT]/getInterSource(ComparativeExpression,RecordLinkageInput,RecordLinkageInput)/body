{
  final List<Operator> passes=new ArrayList<Operator>();
  for (int index=0; index < this.passPartitionKeys.size(); index++)   passes.add(this.createSinglePassInterSource(this.passPartitionKeys.get(index),similarityCondition,input1,input2));
  return new Union().withInputs(passes);
}
