{
  final List<Operator> passes=new ArrayList<Operator>();
  for (int index=0; index < this.passPartitionKeys.size(); index++)   passes.add(this.createSinglePassIntraSource(this.passPartitionKeys.get(index)[0],similarityCondition,input));
  return new Union(passes);
}
