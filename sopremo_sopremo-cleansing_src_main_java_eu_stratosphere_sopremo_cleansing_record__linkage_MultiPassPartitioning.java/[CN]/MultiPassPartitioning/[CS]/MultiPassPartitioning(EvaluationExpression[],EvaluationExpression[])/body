{
  if (leftPartitionKeys.length != rightPartitionKeys.length)   throw new IllegalArgumentException();
  for (int index=0; index < leftPartitionKeys.length; index++)   this.passPartitionKeys.add(new EvaluationExpression[]{leftPartitionKeys[index],rightPartitionKeys[index]});
}
