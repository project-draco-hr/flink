{
  Result expectedResult=new Result(emptyGraphVertexCount,0);
  Result averageClusteringCoefficient=new AverageClusteringCoefficient<LongValue,NullValue,NullValue>().run(emptyGraph).execute();
  assertEquals(expectedResult,averageClusteringCoefficient);
}
