{
  Result expectedResult=new Result(6,1.0 / 2 + 2.0 / 6 + 2.0 / 6 + 1.0 / 12);
  Result averageClusteringCoefficient=new AverageClusteringCoefficient<IntValue,NullValue,NullValue>().run(directedSimpleGraph).execute();
  assertEquals(expectedResult,averageClusteringCoefficient);
}
