{
  for (  final IJsonNode value : values) {
    final Point point=new Point();
    point.read(AnnotatorNodes.getAnnotatee((ObjectNode)value));
    this.queue.add(new BaseCluster(point,String.valueOf(this.createNewId())));
  }
}
