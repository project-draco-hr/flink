{
  int patternPos=patternString.indexOf("%");
  if (patternPos == -1)   patternString+="%s %s";
 else   if (patternString.indexOf("%",patternPos + 1) == -1)   patternString=patternString.replaceAll("%","%s %");
  this.pattern=patternString;
}
