{
  Set<InetSocketAddress> set=new HashSet<>();
  FlinkJedisClusterConfig.Builder builder=new FlinkJedisClusterConfig.Builder();
  builder.setMinIdle(0).setMaxIdle(0).setMaxTotal(0).setTimeout(0).setNodes(set).build();
}
