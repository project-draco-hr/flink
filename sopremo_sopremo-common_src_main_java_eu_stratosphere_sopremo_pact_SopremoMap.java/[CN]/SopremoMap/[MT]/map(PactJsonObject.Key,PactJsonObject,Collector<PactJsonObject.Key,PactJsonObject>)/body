{
  this.context.increaseInputCounter();
  if (SopremoUtil.LOG.isTraceEnabled())   SopremoUtil.LOG.trace(String.format("%s %s/%s",this.getClass().getSimpleName(),key,value));
  this.map(key.getValue(),value.getValue(),new JsonCollector(out));
}
