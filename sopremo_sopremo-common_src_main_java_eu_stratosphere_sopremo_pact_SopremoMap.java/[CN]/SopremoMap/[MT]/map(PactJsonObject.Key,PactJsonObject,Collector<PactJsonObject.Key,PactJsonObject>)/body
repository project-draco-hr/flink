{
  this.context.increaseInputCounter();
  if (SopremoUtil.LOG.isDebugEnabled())   SopremoUtil.LOG.debug(String.format("%s %s/%s",this.getClass().getSimpleName(),key,value));
  this.map(key.getValue(),value.getValue(),new JsonCollector(out));
}
