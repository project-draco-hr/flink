{
  context.increaseInputCounter();
  if (SopremoUtil.LOG.isDebugEnabled())   SopremoUtil.LOG.debug(String.format("%s %s/%s",getClass().getSimpleName(),key,value));
  map(key.getValue(),value.getValue(),new JsonCollector(out));
}
