{
  ClassLoader classLoader=Thread.currentThread().getContextClassLoader();
  return new SavepointCoordinator(jobId,checkpointTimeout,checkpointTimeout,triggerVertices,ackVertices,commitVertices,classLoader,checkpointIdCounter,savepointStore,new DisabledCheckpointStatsTracker());
}
