{
  Logger rootLogger=Logger.getRootLogger();
  rootLogger.setLevel(rootLevel);
  Logger pkgLogger=rootLogger.getLoggerRepository().getLogger(UnilateralSortMerger.class.getPackage().getName());
  pkgLogger.setLevel(pkqLevel);
}
