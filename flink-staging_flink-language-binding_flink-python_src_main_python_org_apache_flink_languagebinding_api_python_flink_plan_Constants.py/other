import sys
PY2 = (sys.version_info[0] == 2)
PY3 = (sys.version_info[0] == 3)
if PY2:
    BOOL = True
    INT = 1
    LONG = long(1)
    FLOAT = 2.5
    STRING = 'type'
    BYTES = bytearray('byte')
elif PY3:
    BOOL = True
    INT = 1
    FLOAT = 2.5
    STRING = 'type'
    BYTES = bytearray('byte')
