{
  String val=configuration.getString(newConfigName,"");
  if (val.isEmpty()) {
    val=configuration.getString(oldConfigName,"");
  }
  if (val.isEmpty()) {
    return defaultValue;
  }
  return val;
}
