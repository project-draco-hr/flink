{
  int numOutputs=getMinOutputs();
  for (int index=numOutputs; index < getMaxOutputs() && index < this.outputs.size(); index++)   if (this.outputs.get(index) != null)   numOutputs++;
  return numOutputs;
}
