{
  int numInputs=getMinInputs();
  for (int index=numInputs; index < getMaxInputs() && index < this.inputs.size(); index++)   if (this.inputs.get(index) != null)   numInputs++;
  return numInputs;
}
