{
  long keySum=0;
  for (  PactConnection c : this.input) {
    OptimizerNode pred=c.getSourcePact();
    if (pred != null) {
      if (pred.estimatedKeyCardinality == -1)       return -1;
      keySum+=pred.estimatedKeyCardinality;
    }
  }
  return keySum;
}
