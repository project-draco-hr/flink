{
  if (this.keys != null) {
    for (    int keyField : this.keys) {
      if (!isFieldConstant(0,keyField)) {
        return null;
      }
    }
    return Collections.singletonList(this.keys);
  }
  return null;
}
