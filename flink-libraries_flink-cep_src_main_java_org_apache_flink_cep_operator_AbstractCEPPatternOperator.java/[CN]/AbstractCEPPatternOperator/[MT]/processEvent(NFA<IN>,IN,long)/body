{
  Collection<Map<String,IN>> patterns=nfa.process(event,timestamp);
  if (!patterns.isEmpty()) {
    StreamRecord<Map<String,IN>> streamRecord=new StreamRecord<Map<String,IN>>(null,timestamp);
    for (    Map<String,IN> pattern : patterns) {
      streamRecord.replace(pattern);
      output.collect(streamRecord);
    }
  }
}
