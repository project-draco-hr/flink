{
  final Boolean flinkTuple=this.r.nextBoolean();
  final StormTuple<Boolean> tuple=new StormTuple<Boolean>(flinkTuple,null,-1,null,null,null);
  Assert.assertEquals(flinkTuple,tuple.getBoolean(0));
}
