{
  this.stream=strm;
  readBuffer=new byte[buffersize];
  wrapBuffer=new byte[256];
  this.lengthLeft=length;
  this.readPos=0;
  this.overLimit=false;
  if (start != 0) {
    strm.seek(start);
    readLine();
  }
 else {
    fillBuffer();
  }
}
