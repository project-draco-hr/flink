{
  checkErroneous();
  byte[] serialized=schema.serialize(next);
  ProducerRecord<byte[],byte[]> record=new ProducerRecord<byte[],byte[]>(topicId,partitioner.partition(next,partitions.length),null,serialized);
  producer.send(record,callback);
}
