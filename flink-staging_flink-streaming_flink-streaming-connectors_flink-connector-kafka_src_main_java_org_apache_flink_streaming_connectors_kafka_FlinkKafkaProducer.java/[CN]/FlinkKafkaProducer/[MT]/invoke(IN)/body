{
  byte[] serialized=schema.serialize(next);
  producer.send(new ProducerRecord<byte[],byte[]>(topicId,partitioner.partition(next,partitions.length),null,serialized),new ErrorLoggingCallback(topicId,null,serialized,false));
}
