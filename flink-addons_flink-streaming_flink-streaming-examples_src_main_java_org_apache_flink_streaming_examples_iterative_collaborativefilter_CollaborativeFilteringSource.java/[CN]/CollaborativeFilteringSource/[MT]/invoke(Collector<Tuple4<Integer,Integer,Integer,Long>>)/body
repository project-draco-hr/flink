{
  @SuppressWarnings("resource") BufferedReader br=new BufferedReader(new FileReader("src/test/resources/testdata/MovieLens100k.data"));
  while (true) {
    line=br.readLine();
    if (line == null) {
      break;
    }
    if (line != "") {
      String[] items=line.split("\t");
      outRecord.f0=Integer.valueOf(items[0]);
      outRecord.f1=Integer.valueOf(items[1]);
      outRecord.f2=Integer.valueOf(items[2]);
      outRecord.f3=timestamp;
      collector.collect(outRecord);
      timestamp++;
    }
  }
}
