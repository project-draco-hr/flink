{
  if (file == null) {
    return;
  }
  try {
    file.delete();
  }
 catch (  SecurityException se) {
    if (LOG.isDebugEnabled()) {
      LOG.debug(StringUtils.stringifyException(se));
    }
  }
}
