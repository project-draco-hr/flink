{
  if (this.position < this.limit - 1) {
    short num=(short)(((this.source[this.position + 0] & 0xff) << 8) | ((this.source[this.position + 1] & 0xff)));
    this.position+=2;
    return num;
  }
 else {
    throw new EOFException();
  }
}
