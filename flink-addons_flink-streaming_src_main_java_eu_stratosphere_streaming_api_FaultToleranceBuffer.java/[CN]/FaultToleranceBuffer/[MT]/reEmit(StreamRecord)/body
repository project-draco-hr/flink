{
  for (  RecordWriter<StreamRecord> output : outputs) {
    try {
      output.emit(record);
      log.warn("Re-emitted");
    }
 catch (    Exception e) {
      log.warn("Re-emit failed");
    }
  }
}
