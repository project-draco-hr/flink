{
  try {
    out.writeInt(value.getType().ordinal());
    if (value.getType() == Type.CustomNode)     out.writeUTF(value.getClass().getName());
    value.write(out);
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
}
