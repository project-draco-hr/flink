{
  try {
    out.writeInt(iJsonNode.getType().ordinal());
    if (iJsonNode.getType() == IJsonNode.Type.CustomNode)     out.writeUTF(iJsonNode.getClass().getName());
    iJsonNode.write(out);
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
}
