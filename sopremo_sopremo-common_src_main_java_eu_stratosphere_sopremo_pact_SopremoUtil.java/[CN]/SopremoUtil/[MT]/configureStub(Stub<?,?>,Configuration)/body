{
  for (  Field stubField : stub.getClass().getDeclaredFields())   if ((stubField.getModifiers() & (Modifier.TRANSIENT | Modifier.FINAL | Modifier.STATIC)) == 0)   if (parameters.getString(stubField.getName(),null) != null)   try {
    stubField.setAccessible(true);
    stubField.set(stub,SopremoUtil.deserialize(parameters,stubField.getName(),Serializable.class));
  }
 catch (  Exception e) {
    LOG.error(String.format("Could not serialize field %s of class %s",stubField.getName(),stub.getClass(),e));
  }
}
