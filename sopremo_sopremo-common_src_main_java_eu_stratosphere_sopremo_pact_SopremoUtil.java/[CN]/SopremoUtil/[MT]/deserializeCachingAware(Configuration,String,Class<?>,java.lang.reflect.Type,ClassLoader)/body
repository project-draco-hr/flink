{
  final Object object=deserialize(config,key,Serializable.class,classLoader);
  if (CachingExpression.class.isAssignableFrom(targetRawType) && !(object instanceof CachingExpression)) {
    final Class<IJsonNode> cachingType;
    if (targetType instanceof ParameterizedType)     cachingType=(Class<IJsonNode>)BoundType.of((ParameterizedType)targetType).getParameters()[0].getType();
 else     cachingType=IJsonNode.class;
    return CachingExpression.of((EvaluationExpression)object,cachingType);
  }
  return object;
}
