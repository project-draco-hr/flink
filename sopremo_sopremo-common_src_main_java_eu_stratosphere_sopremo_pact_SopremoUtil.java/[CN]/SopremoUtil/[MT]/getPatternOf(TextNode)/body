{
  final Map<CharSequence,Pattern> localCache=CACHED_PATTERN.get();
  Pattern pattern=localCache.get(node);
  if (pattern == null)   localCache.put(node,pattern=Pattern.compile(node.getJavaValue().toString()));
  return pattern;
}
