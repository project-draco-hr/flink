{
  FieldSet fs;
  if ((fs=this.forwardedFields.get(sourceField)) != null)   fs.addAll(destinationFields);
 else {
    fs=new FieldSet(destinationFields);
    this.forwardedFields.put(sourceField,fs);
  }
}
