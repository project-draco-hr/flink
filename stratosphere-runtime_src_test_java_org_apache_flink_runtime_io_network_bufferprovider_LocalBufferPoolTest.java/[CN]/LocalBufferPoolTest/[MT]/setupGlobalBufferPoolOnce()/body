{
  Assert.assertEquals("GlobalBufferPool does not have required number of buffers.",NUM_BUFFERS,GLOBAL_BUFFER_POOL.numBuffers());
  Assert.assertEquals("GlobalBufferPool does not have required number of available buffers.",NUM_BUFFERS,GLOBAL_BUFFER_POOL.numAvailableBuffers());
}
