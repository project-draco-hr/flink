{
  JsonNode[] streamNodes=new JsonNode[objectIterators.length];
  for (int index=0; index < streamNodes.length; index++)   streamNodes[index]=wrapWithNode((Iterator<PactJsonObject>)objectIterators[index]);
  return new CompactArrayNode(streamNodes);
}
