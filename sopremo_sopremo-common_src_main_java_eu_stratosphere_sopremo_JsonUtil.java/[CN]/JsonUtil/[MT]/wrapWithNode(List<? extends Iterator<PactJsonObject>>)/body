{
  JsonNode[] streamNodes=new JsonNode[objectIterators.size()];
  for (int index=0; index < streamNodes.length; index++)   streamNodes[index]=wrapWithNode(objectIterators.get(index));
  return new CompactArrayNode(streamNodes);
}
