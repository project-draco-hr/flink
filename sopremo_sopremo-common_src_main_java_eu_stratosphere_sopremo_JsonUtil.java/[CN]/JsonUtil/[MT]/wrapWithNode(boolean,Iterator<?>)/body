{
  final JsonNode[] streamNodes=new JsonNode[objectIterators.length];
  for (int index=0; index < streamNodes.length; index++)   streamNodes[index]=wrapWithNode(resettable,(Iterator<PactJsonObject>)objectIterators[index]);
  return new ArrayNode(streamNodes);
}
