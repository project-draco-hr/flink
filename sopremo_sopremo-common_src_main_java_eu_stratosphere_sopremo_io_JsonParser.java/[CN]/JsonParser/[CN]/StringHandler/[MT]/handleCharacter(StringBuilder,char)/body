{
  if (sb.length() == 0)   JsonParser.this.insideString=true;
 else   if (!sb.toString().endsWith("\\")) {
    JsonParser.this.insideString=false;
    JsonParser.this.wasString=true;
  }
 else   sb.append(character);
}
