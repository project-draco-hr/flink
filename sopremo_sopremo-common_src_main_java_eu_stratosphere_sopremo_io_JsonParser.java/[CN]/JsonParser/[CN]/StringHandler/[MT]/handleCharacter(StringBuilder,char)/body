{
  if (sb.length() == 0) {
    insideString=true;
  }
 else {
    if (!sb.toString().endsWith("\\")) {
      insideString=false;
      wasString=true;
    }
 else {
      sb.append(character);
    }
  }
}
