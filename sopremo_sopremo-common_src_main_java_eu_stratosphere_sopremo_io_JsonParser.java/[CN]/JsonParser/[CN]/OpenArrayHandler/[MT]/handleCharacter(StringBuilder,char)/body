{
  if (state.top() == root) {
    ArrayNode node=new ArrayNode();
    root.add(node);
    state.push(node);
  }
 else {
    ArrayNode newArray=new ArrayNode();
    ((ArrayNode)state.top()).add(newArray);
    state.push(newArray);
  }
}
