{
  if (this == o) {
    return true;
  }
 else   if (o instanceof StateForTask) {
    StateForTask that=(StateForTask)o;
    return this.subtask == that.subtask && this.operatorId.equals(that.operatorId) && this.state.equals(that.state);
  }
 else {
    return false;
  }
}
