{
  if (bucketBoundaries == null)   throw new IllegalArgumentException("Bucket boundaries must not be null.");
  if (bucketBoundaries.length == 0)   throw new IllegalArgumentException("Bucket boundaries must not be empty.");
  dim=1;
  boundaries=packIntegers(bucketBoundaries);
}
