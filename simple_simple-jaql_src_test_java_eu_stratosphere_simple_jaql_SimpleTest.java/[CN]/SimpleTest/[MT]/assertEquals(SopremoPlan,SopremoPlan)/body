{
  Operator<?> unmatchingOperator=actualPlan.getUnmatchingOperator(expectedPlan);
  if (unmatchingOperator != null)   Assert.failNotEquals(String.format("plans are different at %s",unmatchingOperator),expectedPlan,actualPlan);
}
