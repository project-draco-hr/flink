{
  final AbstractJsonNode[] streamNodes=new AbstractJsonNode[objectIterators.length];
  for (int index=0; index < streamNodes.length; index++)   streamNodes[index]=wrapWithNode(resettable,(Iterator<IJsonNode>)objectIterators[index]);
  return new ArrayNode(streamNodes);
}
