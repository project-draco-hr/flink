{
  try {
    if (writer == null) {
      writer=new BufferedWriter(new FileWriter(filename));
    }
    writer.write(tuple + "\n");
    writer.flush();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
