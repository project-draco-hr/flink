{
  EventListener eventListener=new eu.stratosphere.streaming.faulttolerance.AckEventListener(sourceInstanceID,recordBuffer);
  for (  RecordWriter<StreamRecord> output : outputs) {
    output.subscribeToEvent(eventListener,AckEvent.class);
  }
}
