{
  EventListener eventListener=new FailEventListener(sourceInstanceID,recordBuffer);
  for (  RecordWriter<StreamRecord> output : outputs) {
    output.subscribeToEvent(eventListener,FailEvent.class);
  }
}
