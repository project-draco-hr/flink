{
  try {
    if (this.repeatPosition >= this.limit) {
      return false;
    }
    this.inView.setPosition(repeatPosition);
    object.read(this.inView);
    return true;
  }
 catch (  IOException e) {
    throw new RuntimeException("This should not happen: A repeated read failed when the initial read succeeded.");
  }
}
