{
  if (!isBound()) {
    throw new UnboundMemoryBackedException();
  }
  try {
    if (position >= limit) {
      return false;
    }
    memory.inputView.skip(4);
    object.read(memory.inputView);
    position=memory.inputView.getPosition();
    return position <= limit;
  }
 catch (  IOException e) {
    return false;
  }
}
