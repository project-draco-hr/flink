{
  words=record.getString(0).split(" ");
  for (  String word : words) {
    outputRecord.setString(0,word);
    pTimer.startTimer();
    emit(outputRecord);
    pTimer.stopTimer();
    pCounter.count();
  }
}
