{
  Projection[] keyExtractors=new Projection[2];
  if (getInput(0) == getInput(1))   keyExtractors[0]=keyExtractors[1]=new Projection(partitionKeys[0],EvaluationExpression.SAME_VALUE,getInput(0));
 else   for (int index=0; index < 2; index++)   keyExtractors[index]=new Projection(partitionKeys[index],EvaluationExpression.SAME_VALUE,getInput(index));
  throw new UnsupportedOperationException("SNM not yet supported");
}
