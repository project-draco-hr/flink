{
  return super.isEmpty() && (forwardedFields1 == null || forwardedFields1.isEmpty()) && (forwardedFields2 == null || forwardedFields2.isEmpty())&& (readFields1 == null || readFields1.size() == 0)&& (readFields2 == null || readFields2.size() == 0);
}
