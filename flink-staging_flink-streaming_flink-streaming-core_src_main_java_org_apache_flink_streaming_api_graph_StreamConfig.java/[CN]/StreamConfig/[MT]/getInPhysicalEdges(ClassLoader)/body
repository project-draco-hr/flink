{
  try {
    return (List<StreamEdge>)InstantiationUtil.readObjectFromConfig(this.config,IN_STREAM_EDGES,cl);
  }
 catch (  Exception e) {
    throw new RuntimeException("Could not instantiate inputs.");
  }
}
