{
  try {
    return (KeySelector<?,Serializable>)InstantiationUtil.readObjectFromConfig(this.config,STATE_PARTITIONER,cl);
  }
 catch (  Exception e) {
    throw new StreamTaskException("Could not instantiate state partitioner.",e);
  }
}
