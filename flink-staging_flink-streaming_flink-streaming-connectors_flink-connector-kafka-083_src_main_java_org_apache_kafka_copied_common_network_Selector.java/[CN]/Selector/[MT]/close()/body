{
  List<String> connections=new LinkedList<String>(keys.keySet());
  for (  String id : connections)   close(id);
  try {
    this.nioSelector.close();
  }
 catch (  IOException e) {
    log.error("Exception closing nioSelector:",e);
  }
}
