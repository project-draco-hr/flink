{
  final ArrayList<CachingExpression<IJsonNode>> list=new ArrayList<CachingExpression<IJsonNode>>();
  for (  final EvaluationExpression evaluationExpression : expressions)   list.add(ofAny(evaluationExpression));
  return list;
}
