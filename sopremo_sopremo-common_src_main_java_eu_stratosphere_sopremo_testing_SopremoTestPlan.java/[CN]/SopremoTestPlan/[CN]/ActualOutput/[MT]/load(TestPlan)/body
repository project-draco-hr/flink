{
  this.setEmpty();
  final TestRecords actualOutput=testPlan.getActualOutput(this.getIndex());
  for (  final PactRecord record : actualOutput)   this.add(this.schema.recordToJson(record,null));
  actualOutput.close();
}
