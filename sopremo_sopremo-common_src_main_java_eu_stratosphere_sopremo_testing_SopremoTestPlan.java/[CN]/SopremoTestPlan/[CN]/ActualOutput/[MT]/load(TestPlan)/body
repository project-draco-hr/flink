{
  this.actualRecords=testPlan.getActualOutput(this.getIndex());
  FileDataSink sink=testPlan.getSinks().get(this.getIndex());
  this.schema=SopremoUtil.deserialize(sink.getParameters(),SopremoUtil.CONTEXT,EvaluationContext.class).getInputSchema(0);
}
