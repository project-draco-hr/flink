{
  return new ConversionIterator<KeyValuePair<JsonNode,JsonNode>,JsonNode>(this.pairs.iterator()){
    @Override protected JsonNode convert(    final KeyValuePair<JsonNode,JsonNode> inputObject){
      return SopremoUtil.unwrap(inputObject.getValue());
    }
  }
;
}
