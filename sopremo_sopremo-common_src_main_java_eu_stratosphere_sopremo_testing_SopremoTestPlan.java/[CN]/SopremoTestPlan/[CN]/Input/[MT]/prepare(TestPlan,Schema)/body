{
  PactRecord record=null;
  if (operator.isAdhoc())   for (  final JsonNode node : (ArrayNode)operator.getAdhocValues())   testPlan.getInput(this.getIndex()).add(record=schema.jsonToRecord(node,record));
}
