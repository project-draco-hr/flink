{
  super.prepare(testPlan,schema);
  int sinkIndex=findSinkIndex(testPlan);
  if (this.doublePrecision > 0)   testPlan.addFuzzyValueSimilarity(testPlan.getSinks().get(sinkIndex),new DoubleNodeSimilarity(doublePrecision));
}
