{
  if (selected != null) {
    config.setBytes(OUTPUT_NAME + output,SerializationUtils.serialize((Serializable)selected));
  }
 else {
    config.setBytes(OUTPUT_NAME + output,SerializationUtils.serialize(new ArrayList<String>()));
  }
}
