{
  if (!(o instanceof PactDouble))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to PactDouble!");
  final double other=((PactDouble)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
