{
  if (isPartitionedState) {
    this.state=new PartitionedStreamOperatorState<Integer,Integer,Integer>(new LocalStateHandleProvider<Integer>(),new ModKey(2));
  }
 else {
    this.state=new StreamOperatorState<Integer,Integer>(new LocalStateHandleProvider<Integer>());
  }
}
