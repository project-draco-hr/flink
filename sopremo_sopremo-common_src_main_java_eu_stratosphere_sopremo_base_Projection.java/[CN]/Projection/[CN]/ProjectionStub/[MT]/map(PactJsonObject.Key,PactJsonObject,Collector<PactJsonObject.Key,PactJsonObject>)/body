{
  System.out.print(key + " / " + value);
  if (this.keyTransformation != EvaluableExpression.SAME_KEY)   key=PactJsonObject.keyOf(this.keyTransformation.evaluate(value.getValue(),this.getContext()));
  if (this.keyTransformation != EvaluableExpression.SAME_VALUE)   value=new PactJsonObject(this.valueTransformation.evaluate(value.getValue(),this.getContext()));
  System.out.println(" -> " + key + " / "+ value);
  out.collect(key,value);
}
