{
  JobExecutionResult result=ClusterUtil.runOnMiniCluster(getStreamGraph().getJobGraph(),getParallelism(),-1,getConfig().isSysoutLoggingEnabled(),false,this.conf);
  transformations.clear();
  return result;
}
