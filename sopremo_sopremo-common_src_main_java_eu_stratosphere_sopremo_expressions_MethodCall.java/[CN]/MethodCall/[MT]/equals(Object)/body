{
  if (obj == null || this.getClass() != obj.getClass())   return false;
  return this.name.equals(((MethodCall)obj).name) && this.target.equals(((MethodCall)obj).target) && Arrays.equals(this.paramExprs,((MethodCall)obj).paramExprs);
}
