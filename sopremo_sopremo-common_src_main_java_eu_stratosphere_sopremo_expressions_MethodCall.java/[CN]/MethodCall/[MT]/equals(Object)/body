{
  if (obj == null || this.getClass() != obj.getClass())   return false;
  return this.function.equals(((MethodCall)obj).function) && this.target.equals(((MethodCall)obj).target) && Arrays.equals(this.paramExprs,((MethodCall)obj).paramExprs);
}
