{
  List<Projection> projections=new ArrayList<Projection>();
  for (  EvaluationExpression expression : this.expressions)   projections.add(new Projection(expression,getInput(0)));
  return SopremoModule.valueOf(getName(),projections);
}
