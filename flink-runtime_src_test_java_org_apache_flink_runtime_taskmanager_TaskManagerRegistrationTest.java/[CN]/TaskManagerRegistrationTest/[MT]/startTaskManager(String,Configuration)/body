{
  config.setInteger(ConfigConstants.TASK_MANAGER_MEMORY_SIZE_KEY,1);
  return TaskManager.startTaskManagerComponentsAndActor(config,actorSystem,"localhost",NONE_STRING,new Some<String>(jobManagerUrl),true,TaskManager.class);
}
