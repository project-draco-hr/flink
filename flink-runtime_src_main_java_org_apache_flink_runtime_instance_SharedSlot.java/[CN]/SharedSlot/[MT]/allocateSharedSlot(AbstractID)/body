{
  if (isAlive()) {
    SharedSlot slot=new SharedSlot(getJobID(),getInstance(),subSlots.size(),assignmentGroup,this,groupId);
    subSlots.add(slot);
    return slot;
  }
 else {
    return null;
  }
}
