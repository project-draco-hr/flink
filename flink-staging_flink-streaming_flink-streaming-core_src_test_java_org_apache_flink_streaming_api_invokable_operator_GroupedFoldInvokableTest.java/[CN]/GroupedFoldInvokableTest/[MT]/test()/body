{
  TypeInformation<String> outType=TypeExtractor.getForObject("A string");
  GroupedFoldInvokable<Integer,String> invokable1=new GroupedFoldInvokable<Integer,String>(new MyFolder(),new KeySelector<Integer,String>(){
    private static final long serialVersionUID=1L;
    @Override public String getKey(    Integer value) throws Exception {
      return value.toString();
    }
  }
,"100",outType);
  List<String> expected=Arrays.asList("1001","10011","1002","10022","1003");
  List<String> actual=MockContext.createAndExecute(invokable1,Arrays.asList(1,1,2,2,3));
  assertEquals(expected,actual);
}
