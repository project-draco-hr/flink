{
  return set.map(new MapFunction<Object,Integer>(){
    @Override public Integer map(    Object o) throws Exception {
      return 1;
    }
  }
).reduce(new ReduceFunction<Integer>(){
    @Override public Integer reduce(    Integer one,    Integer two) throws Exception {
      return one + two;
    }
  }
).first(1);
}
