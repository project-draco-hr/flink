{
  @SuppressWarnings({"unchecked","rawtypes"}) KeyMap<Key,Aggregate>[] panes=previousPanes.toArray(new KeyMap[previousPanes.size() + 1]);
  panes[panes.length - 1]=latestPane;
  KeyMap.traverseMaps(panes,traversal,traversalPass);
}
