{
  this.partials=new ArrayList<Partial>(functions.size());
  for (  AggregationFunction function : functions)   partials.add(new Partial(function,EvaluationExpression.SAME_VALUE,partials.size()));
}
