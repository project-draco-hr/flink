{
  this.getFunction().toString(builder);
  builder.append('(');
  BatchAggregationExpression.this.toString(builder);
  if (this.getPreprocessing() != EvaluationExpression.VALUE)   this.getPreprocessing().toString(builder);
  builder.append(')');
}
