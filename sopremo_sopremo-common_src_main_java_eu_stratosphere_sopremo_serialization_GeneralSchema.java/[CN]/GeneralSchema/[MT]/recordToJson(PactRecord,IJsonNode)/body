{
  IJsonNode source=(IJsonNode)SopremoUtil.unwrap(record.getField(record.getNumFields() - 1,JsonNodeWrapper.class));
  if (target == null) {
    return source;
  }
  return this.recycleTargetNode(target,source);
}
