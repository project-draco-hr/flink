{
  if (target == null)   target=new PactRecord(this.mappings.size() + 1);
  target.setField(this.mappings.size(),SopremoUtil.wrap(value));
  for (int i=0; i < this.mappings.size(); i++)   target.setField(i,SopremoUtil.wrap(this.mappings.get(i).evaluate(value,null,context)));
  return target;
}
