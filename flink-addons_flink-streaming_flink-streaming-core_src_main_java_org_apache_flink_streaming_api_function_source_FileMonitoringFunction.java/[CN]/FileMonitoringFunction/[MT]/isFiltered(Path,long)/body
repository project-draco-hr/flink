{
  String filename=path.getName();
  return lastModificationTime > modificationTime || (watchType == WatchType.ONLY_NEW_FILES && offsetOfFiles.containsKey(path.toString())) || filename.startsWith(".") || filename.contains("_COPYING_");
}
