{
  final Contract contract=new OperatorWithTwoStubs().getContract();
  assertEquals(ReduceContract.class,contract.getClass());
  assertTrue(Arrays.asList(OperatorWithTwoStubs.Implementation1.class,OperatorWithTwoStubs.Implementation2.class).contains(contract.getUserCodeClass()));
}
