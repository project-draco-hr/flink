{
  KMeans kmi=new KMeans();
  return kmi.getScalaPlan(config.getInteger("IterativeKMeansITCase#NoSubtasks",1),dataPath,clusterPath,resultPath,Integer.parseInt(config.getString("IterativeKMeansITCase#NumIterations","1")));
}
