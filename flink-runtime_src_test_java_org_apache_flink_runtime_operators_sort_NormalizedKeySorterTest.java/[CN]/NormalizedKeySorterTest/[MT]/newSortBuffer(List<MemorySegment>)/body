{
  @SuppressWarnings({"unchecked","rawtypes"}) RecordComparator accessors=new RecordComparator(new int[]{0},new Class[]{Key.class});
  return new NormalizedKeySorter<Record>(RecordSerializer.get(),accessors,memory);
}
