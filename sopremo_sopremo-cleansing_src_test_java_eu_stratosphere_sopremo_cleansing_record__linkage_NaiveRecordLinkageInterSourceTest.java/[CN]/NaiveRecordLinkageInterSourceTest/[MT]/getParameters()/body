{
  final EvaluationExpression[][] projections={{null,null},{getAggregativeProjection1(),getAggregativeProjection2()}};
  final ArrayList<Object[]> parameters=new ArrayList<Object[]>();
  for (  final EvaluationExpression[] projection : projections)   parameters.add(new Object[]{projection[0],projection[1]});
  return parameters;
}
