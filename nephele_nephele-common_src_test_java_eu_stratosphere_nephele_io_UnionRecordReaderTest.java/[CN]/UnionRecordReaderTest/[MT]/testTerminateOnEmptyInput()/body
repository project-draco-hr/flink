{
  @SuppressWarnings("unchecked") RecordReader<Record>[] r=new RecordReader[]{new RecordReader<Record>(new DummyTask(),null,0),new RecordReader<Record>(new DummyTask(),null,0)};
  UnionRecordReader<Record> reader=new UnionRecordReader<Record>(r);
  assertThat(reader.hasNext(),is(false));
}
