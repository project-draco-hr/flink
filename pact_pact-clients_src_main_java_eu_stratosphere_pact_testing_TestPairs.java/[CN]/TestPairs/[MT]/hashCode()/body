{
  final int prime=31;
  int result=1;
  final Iterator<KeyValuePair<K,V>> iterator=this.iterator();
  while (iterator.hasNext())   result=prime * result + iterator.next().hashCode();
  return result;
}
