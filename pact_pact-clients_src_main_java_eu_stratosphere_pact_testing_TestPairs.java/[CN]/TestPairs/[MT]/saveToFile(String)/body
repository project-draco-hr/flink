{
  final SequentialOutputFormat outputFormat=FormatUtil.createOutputFormat(SequentialOutputFormat.class,path,null);
  final Iterator<KeyValuePair<K,V>> iterator=this.iterator();
  while (iterator.hasNext())   outputFormat.writePair((KeyValuePair<Key,Value>)iterator.next());
}
