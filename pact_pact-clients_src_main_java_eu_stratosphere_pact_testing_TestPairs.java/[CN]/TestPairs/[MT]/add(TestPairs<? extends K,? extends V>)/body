{
  if (pairs.isEmpty())   setEmpty();
 else {
    for (    final KeyValuePair<? extends K,? extends V> pair : pairs)     this.pairs.add((KeyValuePair<K,V>)pair);
    setEmpty(false);
  }
  return this;
}
