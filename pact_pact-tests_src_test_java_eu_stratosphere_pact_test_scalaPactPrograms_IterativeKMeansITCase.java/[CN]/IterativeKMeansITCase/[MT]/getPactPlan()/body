{
  KMeans kmi=new KMeans();
  Plan plan=kmi.getPlan(Integer.parseInt(config.getString("IterativeKMeansITCase#NumIterations","1")),dataPath,clusterPath,resultPath);
  plan.setDefaultParallelism(config.getInteger("IterativeKMeansITCase#NoSubtasks",1));
  return plan;
}
