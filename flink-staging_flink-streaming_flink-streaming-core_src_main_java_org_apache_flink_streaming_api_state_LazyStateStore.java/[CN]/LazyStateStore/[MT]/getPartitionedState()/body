{
  for (  Entry<Serializable,StateHandle<C>> handleEntry : unfetchedState.entrySet()) {
    fetchedState.put(handleEntry.getKey(),checkpointer.restoreState(handleEntry.getValue().getState()));
  }
  unfetchedState.clear();
  return fetchedState;
}
