{
  Calendar cal=Calendar.getInstance();
  cal.setTime(value.f0);
  for (  Character key : value.f1.keySet()) {
    out.collect(new Tuple2<Integer,Tuple2<Character,Integer>>(cal.get(Calendar.MONTH) + 1,new Tuple2<Character,Integer>(key,value.f1.get(key))));
  }
}
