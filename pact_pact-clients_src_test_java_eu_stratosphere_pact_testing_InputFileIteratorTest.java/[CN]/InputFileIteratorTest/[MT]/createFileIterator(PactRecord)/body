{
  String testPlanFile=TestPlan.getTestPlanFile("fileIteratorTest");
  SequentialOutputFormat output=FormatUtil.openOutput(SequentialOutputFormat.class,testPlanFile,null);
  for (  PactRecord keyValuePair : pairs)   output.writeRecord(keyValuePair);
  output.close();
  SequentialInputFormat inputFormat=FormatUtil.openInput(SequentialInputFormat.class,testPlanFile,null);
  InputFileIterator inputFileIterator=new InputFileIterator(inputFormat);
  return inputFileIterator;
}
