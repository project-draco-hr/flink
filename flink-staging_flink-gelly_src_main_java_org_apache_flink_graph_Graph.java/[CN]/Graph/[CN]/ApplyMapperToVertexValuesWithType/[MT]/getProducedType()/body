{
  @SuppressWarnings("unchecked") TypeInformation<VV> newVertexValueType=TypeExtractor.getMapReturnTypes(innerMapper,(TypeInformation<K>)keyType);
  return new TupleTypeInfo<Tuple2<K,VV>>(keyType,newVertexValueType);
}
