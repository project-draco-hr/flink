{
  return vertices.map(new MapFunction<Tuple2<K,VV>,Tuple2<K,VV>>(){
    @Override public Tuple2<K,VV> map(    Tuple2<K,VV> kvvTuple2) throws Exception {
      return new Tuple2<>(kvvTuple2.f0,mapper.map(kvvTuple2.f1));
    }
  }
);
}
