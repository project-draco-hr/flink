{
  return vertices.map(new MapFunction<Vertex<K,VV>,Vertex<K,V2>>(){
    @Override public Vertex<K,V2> map(    Vertex<K,VV> kvvVertex) throws Exception {
      return new Vertex<>(kvvVertex.getKey(),mapper.map(kvvVertex.getValue()));
    }
  }
);
}
