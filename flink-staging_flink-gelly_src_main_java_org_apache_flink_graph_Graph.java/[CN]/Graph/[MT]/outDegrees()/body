{
  TypeInformation<?>[] types={(BasicTypeInfo<?>)vertexKeyType,BasicTypeInfo.LONG_TYPE_INFO};
  return vertices.join(edges).where(0).equalTo(0).map(new VertexKeyWithOne<K,EV,VV>(new TupleTypeInfo<Tuple2<K,Long>>(types))).groupBy(0).sum(1);
}
