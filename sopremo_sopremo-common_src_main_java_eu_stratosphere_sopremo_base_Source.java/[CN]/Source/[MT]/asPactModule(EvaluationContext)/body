{
  if (this.type == PersistenceType.ADHOC)   throw new UnsupportedOperationException();
  PactModule pactModule=new PactModule(toString(),0,1);
  DataSourceContract<PactJsonObject.Key,PactJsonObject> contract=new DataSourceContract<PactJsonObject.Key,PactJsonObject>(JsonInputFormat.class,this.inputName,this.inputName);
  pactModule.getOutput(0).setInput(contract);
  return pactModule;
}
