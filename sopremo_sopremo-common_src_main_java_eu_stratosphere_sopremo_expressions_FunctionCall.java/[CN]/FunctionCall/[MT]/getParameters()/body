{
  final ArrayList<EvaluationExpression> parameters=new ArrayList<EvaluationExpression>();
  for (  CachingExpression<IJsonNode> param : this.paramExprs)   parameters.add(param.getInnerExpression());
  return parameters;
}
