{
  final SopremoFunction function=(SopremoFunction)scope.getFunctionRegistry().get(functionName);
  if (function == null)   throw new IllegalArgumentException(String.format("No method %s found",functionName));
  return function;
}
