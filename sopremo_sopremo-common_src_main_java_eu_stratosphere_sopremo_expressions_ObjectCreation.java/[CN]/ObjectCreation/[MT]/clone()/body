{
  final ObjectCreation clone=(ObjectCreation)super.clone();
  clone.mappings=new ArrayList<ObjectCreation.Mapping<?>>(this.mappings.size());
  for (  Mapping<?> mapping : this.mappings)   clone.mappings.add(mapping.clone());
  return clone;
}
