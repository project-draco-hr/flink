{
  return new ConversionIterator<Mapping,EvaluableExpression>(this.mappings.iterator()){
    @Override protected EvaluableExpression convert(    Mapping inputObject){
      return inputObject.getExpression();
    }
  }
;
}
