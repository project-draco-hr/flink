{
  return new ConversionIterator<Mapping,EvaluationExpression>(this.mappings.iterator()){
    @Override protected EvaluationExpression convert(    Mapping inputObject){
      return inputObject.getExpression();
    }
  }
;
}
