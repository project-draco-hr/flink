{
  final ObjectNode targetObject=SopremoUtil.reinitializeTarget(target,ObjectNode.class);
  for (  final Mapping<?> mapping : this.mappings)   mapping.evaluate(node,targetObject,context);
  return targetObject;
}
