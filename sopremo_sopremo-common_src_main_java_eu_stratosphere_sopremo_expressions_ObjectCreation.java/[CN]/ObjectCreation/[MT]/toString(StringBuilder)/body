{
  builder.append("{");
  Iterator<Mapping> mappingIterator=this.mappings.iterator();
  while (mappingIterator.hasNext()) {
    Mapping entry=mappingIterator.next();
    entry.toString(builder);
    if (mappingIterator.hasNext())     builder.append(", ");
  }
  builder.append("}");
}
