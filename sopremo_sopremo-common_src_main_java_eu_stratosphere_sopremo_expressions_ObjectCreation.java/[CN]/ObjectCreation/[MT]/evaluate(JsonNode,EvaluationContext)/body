{
  final ObjectNode transformedNode=JsonUtil.OBJECT_MAPPER.createObjectNode();
  for (  final Mapping mapping : this.mappings)   mapping.evaluate(transformedNode,node,context);
  return transformedNode;
}
