{
  if (this.mappings.size() != children.size())   throw new IllegalArgumentException();
  for (int index=0; index < children.size(); index++)   this.mappings.get(index).setExpression(children.get(index));
}
