{
  this.index--;
  if (!this.lastReturnedWasKey && (this.lastReturnedWasKey=this.lastMapping.target instanceof EvaluationExpression))   return (EvaluationExpression)this.lastMapping.target;
  return this.lastMapping.expression;
}
