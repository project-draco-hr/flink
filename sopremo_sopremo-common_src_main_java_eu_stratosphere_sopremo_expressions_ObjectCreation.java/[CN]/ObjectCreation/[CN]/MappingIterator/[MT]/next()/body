{
  this.index++;
  if (this.lastReturnedWasKey)   return this.lastMapping.expression;
  this.lastMapping=this.iterator.next();
  if (this.lastReturnedWasKey=(this.lastMapping.target instanceof EvaluationExpression))   return (EvaluationExpression)this.lastMapping.target;
  return this.lastMapping.expression;
}
