{
  Result expectedResult=new Result(13,6);
  Result globalClusteringCoefficient=new GlobalClusteringCoefficient<IntValue,NullValue,NullValue>().run(directedSimpleGraph).execute();
  assertEquals(expectedResult,globalClusteringCoefficient);
}
