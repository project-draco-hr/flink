{
  ArrayNode valueArray=new ArrayNode(((ArrayNode)value).get(0),((ArrayNode)value).get(1));
  ArrayNode keyArray=new ArrayNode(((ObjectNode)valueArray.get(0)).get("partition"),((ObjectNode)valueArray.get(1)).get("partition"));
  out.collect(keyArray,valueArray);
}
