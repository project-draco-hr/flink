{
  int len=cs.length();
  while (len >= HIGH_BIT) {
    out.write(len | HIGH_BIT);
    len>>>=7;
  }
  out.write(len);
  for (int i=0; i < cs.length(); i++) {
    int c=cs.charAt(i);
    while (c >= HIGH_BIT) {
      out.write(c | HIGH_BIT);
      c>>>=7;
    }
    out.write(c);
  }
}
