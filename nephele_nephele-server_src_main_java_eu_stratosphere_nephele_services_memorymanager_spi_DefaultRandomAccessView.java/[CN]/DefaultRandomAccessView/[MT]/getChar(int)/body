{
  if (index >= 0 && index + 1 < this.size) {
    return (char)(((this.memory[this.offset + index + 0] & 0xff) << 8) | ((this.memory[this.offset + index + 1] & 0xff) << 0));
  }
 else {
    throw new IndexOutOfBoundsException();
  }
}
