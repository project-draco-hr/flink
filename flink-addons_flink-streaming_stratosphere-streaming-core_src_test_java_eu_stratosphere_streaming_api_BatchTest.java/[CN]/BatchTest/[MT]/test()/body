{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  DataStream<Tuple1<String>> dataStream=env.addSource(new MySource(),SOURCE_PARALELISM).flatMap(new MyMap(),PARALLELISM).batch(4).flatMap(new MyMap(),PARALLELISM).batch(2).flatMap(new MyMap(),PARALLELISM).batch(5).flatMap(new MyMap(),PARALLELISM).batch(4).addSink(new MySink());
  env.execute();
  assertEquals(20,count);
}
