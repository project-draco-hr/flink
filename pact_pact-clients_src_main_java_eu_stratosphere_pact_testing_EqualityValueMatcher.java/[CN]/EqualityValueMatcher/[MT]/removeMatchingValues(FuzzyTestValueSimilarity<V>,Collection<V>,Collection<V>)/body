{
  Iterator<V> actualIterator=actualValues.iterator();
  while (!expectedValues.isEmpty() && actualIterator.hasNext()) {
    final V actual=actualIterator.next();
    if (expectedValues.remove(actual))     actualIterator.remove();
  }
}
