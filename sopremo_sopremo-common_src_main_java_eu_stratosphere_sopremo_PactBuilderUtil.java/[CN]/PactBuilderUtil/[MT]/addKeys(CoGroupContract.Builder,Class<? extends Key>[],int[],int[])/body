{
  Preconditions.checkArgument(keyClasses.length == keyIndices.length,"Lenght of keyClasses and keyIndices must match.");
  for (int i=0; i < keyClasses.length; ++i) {
    builder.keyField(keyClasses[i],keyIndices[i],keyIndices2[i]);
  }
}
