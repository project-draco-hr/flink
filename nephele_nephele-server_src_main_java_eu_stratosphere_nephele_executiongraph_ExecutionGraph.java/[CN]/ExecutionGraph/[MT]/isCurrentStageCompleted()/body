{
  if (this.indexToCurrentExecutionStage.get() >= this.stages.size()) {
    return true;
  }
  final Iterator<ExecutionGroupVertex> it=new ExecutionGroupVertexIterator(this,true,this.indexToCurrentExecutionStage.get());
  while (it.hasNext()) {
    final ExecutionGroupVertex groupVertex=it.next();
    if (groupVertex.getGroupExecutionState() != ExecutionState.FINISHED) {
      return false;
    }
  }
  return true;
}
