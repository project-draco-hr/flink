{
  int f=0;
  int s=0;
  while (f < a.length && s < b.length) {
    if (a[f] < b[s]) {
      f++;
    }
 else     if (a[f] > b[s]) {
      s++;
    }
 else {
      return false;
    }
  }
  return true;
}
