{
  if (!(o instanceof PactFloat))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to PactFloat!");
  final double other=((PactFloat)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
