{
  if (e1.getClass() != e2.getClass()) {
    if (e1 instanceof AbstractNumericNode && e2 instanceof AbstractNumericNode)     return this.isTrue(e1.compareTo(e2));
    return false;
  }
  return this.isTrue(e1.compareToSameType(e2));
}
