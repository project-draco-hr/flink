{
  if (output == null) {
    if (newLine)     System.out.println(message);
 else     System.out.print(message);
  }
 else {
    if (newLine)     output.append(message + "\n");
 else     output.append(message);
  }
}
