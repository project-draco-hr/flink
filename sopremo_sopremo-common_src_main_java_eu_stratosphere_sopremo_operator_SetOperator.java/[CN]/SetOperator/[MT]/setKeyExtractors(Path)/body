{
  if (setKeyExtractors == null)   throw new NullPointerException("setKeyExtractors must not be null");
  for (  Path keyExtractor : keyExtractors) {
    int inputIndex=PactUtil.getInputIndex(keyExtractor);
    if (inputIndex == -1)     throw new IllegalArgumentException("extractor does not contain input selector: " + keyExtractor);
    this.setKeyExtractors[inputIndex]=keyExtractor;
  }
}
