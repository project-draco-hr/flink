{
  StreamReduce<Integer> operator1=new StreamReduce<Integer>(new MyReducer());
  List<Integer> expected=Arrays.asList(1,2,4,7,10);
  List<Integer> actual=MockContext.createAndExecute(operator1,Arrays.asList(1,1,2,3,3));
  assertEquals(expected,actual);
}
