{
  if (!(o instanceof PactByte))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to PactByte!");
  final byte other=((PactByte)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
