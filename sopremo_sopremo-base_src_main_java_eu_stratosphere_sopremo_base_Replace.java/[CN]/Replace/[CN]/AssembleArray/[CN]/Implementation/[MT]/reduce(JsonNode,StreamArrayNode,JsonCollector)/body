{
  JsonNode[] array=new JsonNode[key.size()];
  int replacedCount=0;
  for (  JsonNode value : values) {
    int index=value.get(0).getIntValue();
    JsonNode element=value.get(1);
    array[index]=element;
    replacedCount++;
  }
  if (replacedCount == array.length)   out.collect(key,JsonUtil.asArray(array));
}
