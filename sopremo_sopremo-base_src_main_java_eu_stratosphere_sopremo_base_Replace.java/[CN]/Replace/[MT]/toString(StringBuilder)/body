{
  builder.append(getName());
  if (isArrayElementsReplacement())   builder.append(" all ");
  getReplaceExpression().toString(builder);
  builder.append(" with ");
  getDictionary().toString(builder);
  builder.append(" default ");
  getDefaultExpression().toString(builder);
}
