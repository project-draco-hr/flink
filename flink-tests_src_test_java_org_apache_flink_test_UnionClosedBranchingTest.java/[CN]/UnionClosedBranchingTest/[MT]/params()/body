{
  Collection<Object[]> params=Arrays.asList(new Object[][]{{ExecutionMode.PIPELINED,PIPELINED,BATCH},{ExecutionMode.PIPELINED_FORCED,PIPELINED,PIPELINED},{ExecutionMode.BATCH,BATCH,BATCH},{ExecutionMode.BATCH_FORCED,BATCH,BATCH}});
  assertEquals(ExecutionMode.values().length,params.size());
  return params;
}
