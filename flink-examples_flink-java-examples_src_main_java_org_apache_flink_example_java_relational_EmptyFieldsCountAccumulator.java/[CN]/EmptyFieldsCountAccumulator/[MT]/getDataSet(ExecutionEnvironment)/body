{
  DataSet<? extends Tuple> source;
  if (filePath == null) {
    source=env.fromCollection(getExampleInputTuples());
  }
 else {
    source=env.readCsvFile(filePath).fieldDelimiter(';').types(String.class,String.class,String.class);
  }
  return (DataSet<Tuple>)source;
}
