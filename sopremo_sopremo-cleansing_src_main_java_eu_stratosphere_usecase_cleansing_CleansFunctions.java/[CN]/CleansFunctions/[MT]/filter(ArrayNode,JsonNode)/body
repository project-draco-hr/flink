{
  ArrayNode output=new ArrayNode(null);
  HashSet<JsonNode> filterSet=new HashSet<JsonNode>(Arrays.asList(elementsToFilter));
  for (int index=0; index < input.size(); index++)   if (!filterSet.contains(input.get(index)))   output.add(input.get(index));
  return output;
}
