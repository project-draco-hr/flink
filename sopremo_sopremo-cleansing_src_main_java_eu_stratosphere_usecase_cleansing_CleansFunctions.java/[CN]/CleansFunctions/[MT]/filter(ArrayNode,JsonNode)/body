{
  ArrayNode output=new ArrayNode(null);
  HashSet<JsonNode> filterSet=new HashSet<JsonNode>(Arrays.asList(elementsToFilter));
  for (int index=0; index < output.size(); )   if (filterSet.contains(output.get(index)))   output.remove(index);
 else   index++;
  return output;
}
