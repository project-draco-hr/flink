{
  String[] split=input.getTextValue().split(splitString.getTextValue());
  ArrayNode splitNode=new ArrayNode(null);
  for (  String string : split)   splitNode.add(TextNode.valueOf(string));
  return splitNode;
}
