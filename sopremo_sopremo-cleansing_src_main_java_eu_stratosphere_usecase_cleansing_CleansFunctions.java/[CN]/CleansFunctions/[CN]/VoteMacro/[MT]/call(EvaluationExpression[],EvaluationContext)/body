{
  for (int index=0; index < params.length; index++)   if (params[index] instanceof MethodPointerExpression)   params[index]=new MethodCall(((MethodPointerExpression)params[index]).getFunctionName(),EvaluationExpression.VALUE);
  return new BeliefResolution(params);
}
