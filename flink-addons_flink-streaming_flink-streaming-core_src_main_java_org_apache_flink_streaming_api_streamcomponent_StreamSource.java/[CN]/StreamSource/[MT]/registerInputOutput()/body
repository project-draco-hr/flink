{
  initialize();
  try {
    setSerializers();
    setCollector();
    setConfigOutputs(outputs);
  }
 catch (  StreamComponentException e) {
    throw new StreamComponentException("Cannot register outputs for " + getClass().getSimpleName(),e);
  }
  setInvokable();
}
