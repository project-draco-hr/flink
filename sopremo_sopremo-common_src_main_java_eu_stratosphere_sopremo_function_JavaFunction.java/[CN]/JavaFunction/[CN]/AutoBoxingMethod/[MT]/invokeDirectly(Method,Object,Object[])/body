{
  for (int index=0; index < params.length; index++)   params[index]=JavaToJsonMapper.INSTANCE.valueToTree(params[index]);
  return JavaToJsonMapper.INSTANCE.valueToTree(super.invokeDirectly(method,context,params));
}
