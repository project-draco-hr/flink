{
  JsonNode[] params;
  if (node instanceof ArrayNode) {
    params=new JsonNode[((ArrayNode)node).size()];
    for (int index=0; index < params.length; index++)     params[index]=((ArrayNode)node).get(index);
  }
 else   params=new JsonNode[]{node};
  return params;
}
