{
  for (  final FileDataSourceContract<?,?> FileDataSourceContract : this.sources) {
    final TestPairs<?,?> input=this.getInput(FileDataSourceContract);
    if (input.isAdhoc())     input.saveToFile(FileDataSourceContract.getFilePath());
  }
}
