{
  Int2ObjectMap<List<ValueSimilarity<?>>> indexMap=this.fuzzySimilarity.get(sink);
  if (indexMap == null)   this.fuzzySimilarity.put(sink,indexMap=new Int2ObjectArrayMap<List<ValueSimilarity<?>>>());
  return indexMap;
}
