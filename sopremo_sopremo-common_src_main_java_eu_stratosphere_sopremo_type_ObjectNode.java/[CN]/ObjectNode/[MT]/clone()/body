{
  final ObjectNode clone=(ObjectNode)super.clone();
  clone.children=new LinkedHashMap<String,IJsonNode>(this.children);
  for (  final Entry<String,IJsonNode> entry : clone.children.entrySet())   entry.setValue(entry.getValue().clone());
  return clone;
}
