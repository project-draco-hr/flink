{
  final ObjectNode clone=(ObjectNode)super.clone();
  clone.children=new LinkedHashMap<String,JsonNode>(this.children);
  for (  final Entry<String,JsonNode> entry : clone.children.entrySet())   entry.setValue(entry.getValue().clone());
  return clone;
}
