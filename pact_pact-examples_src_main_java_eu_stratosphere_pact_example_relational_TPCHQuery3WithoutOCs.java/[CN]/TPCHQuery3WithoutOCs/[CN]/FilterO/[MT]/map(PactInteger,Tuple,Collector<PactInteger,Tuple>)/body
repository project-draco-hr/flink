{
  try {
    if ((Integer.parseInt(value.getStringValueAt(4).substring(0,4)) > YEAR_FILTER) && (value.getStringValueAt(2).equals("F")) && (value.getStringValueAt(5).startsWith(PRIO_FILTER))) {
      value.project(129);
      out.collect(oKey,value);
    }
  }
 catch (  StringIndexOutOfBoundsException e) {
    LOGGER.error(e);
  }
catch (  Exception ex) {
    LOGGER.error(ex);
  }
}
