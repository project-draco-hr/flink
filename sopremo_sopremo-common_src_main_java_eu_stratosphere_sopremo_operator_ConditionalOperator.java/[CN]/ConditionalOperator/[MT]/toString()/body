{
  StringBuilder builder=new StringBuilder(getName());
  builder.append(", ").append(condition);
  if (getTransformation() != Transformation.IDENTITY)   builder.append(", ").append(getTransformation());
  return builder.toString();
}
