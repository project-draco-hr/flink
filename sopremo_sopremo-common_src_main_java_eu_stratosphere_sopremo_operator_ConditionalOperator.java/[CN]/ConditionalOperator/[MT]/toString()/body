{
  StringBuilder builder=new StringBuilder(getName());
  builder.append(" on ").append(condition);
  if (getTransformation() != Transformation.IDENTITY)   builder.append(" to ").append(getTransformation());
  return builder.toString();
}
