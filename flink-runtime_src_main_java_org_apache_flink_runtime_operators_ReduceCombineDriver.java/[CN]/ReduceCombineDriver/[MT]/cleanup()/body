{
  try {
    if (sorter != null) {
      sorter.dispose();
    }
    if (table != null) {
      table.close();
    }
  }
 catch (  Exception e) {
  }
  taskContext.getMemoryManager().release(memory);
}
