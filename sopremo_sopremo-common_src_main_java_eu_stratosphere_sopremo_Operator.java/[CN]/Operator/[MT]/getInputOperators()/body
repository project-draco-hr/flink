{
  return new AbstractList<Operator>(){
    @Override public Operator get(    int index){
      return inputs.get(index) == null ? null : inputs.get(index).getOperator();
    }
    @Override public int size(){
      return inputs.size();
    }
  }
;
}
