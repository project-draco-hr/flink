{
  int length=valueMode == ValueMode.FIX_LENGTH ? valueLength : valueLength - random.nextInt(valueLength / 3);
  target.setLength(0);
  for (int i=0; i < length; i++) {
    target.append(alpha[random.nextInt(alpha.length)]);
  }
}
