{
  if (this.compressor == null) {
    this.compressor=CompressionLoader.getCompressorByCompressionLevel(this.outputGate.getCompressionLevel(),this.taskContext.getCompressionBufferProvider());
  }
 else {
    this.compressor.increaseChannelCounter();
  }
  return this.compressor;
}
