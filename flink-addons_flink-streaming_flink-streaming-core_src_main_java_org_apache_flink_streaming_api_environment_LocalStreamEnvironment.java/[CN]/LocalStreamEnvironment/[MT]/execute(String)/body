{
  if (localExecutionIsAllowed()) {
    ClusterUtil.runOnMiniCluster(this.jobGraphBuilder.getJobGraph(jobName),getExecutionParallelism());
  }
 else {
    ClusterUtil.runOnLocalCluster(this.jobGraphBuilder.getJobGraph(jobName),getExecutionParallelism());
  }
}
