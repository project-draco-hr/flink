{
  boolean curHasNext;
  while ((curHasNext=this.currentIterator.hasNext()) || this.inputs.hasNext()) {
    if (curHasNext)     return this.currentIterator.next();
    this.currentIterator=this.inputs.next();
  }
  return this.noMoreElements();
}
