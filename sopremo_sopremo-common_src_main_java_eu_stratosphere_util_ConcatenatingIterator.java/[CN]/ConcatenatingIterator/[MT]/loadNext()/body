{
  boolean curHasNext;
  while ((curHasNext=currentIterator.hasNext()) || this.inputs.hasNext()) {
    if (curHasNext)     return currentIterator.next();
    currentIterator=this.inputs.next();
  }
  return this.noMoreElements();
}
