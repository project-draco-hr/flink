{
  final TailArraySchema schema=new TailArraySchema(5);
  final PactRecord record=schema.jsonToRecord(new ArrayNode(IntNode.valueOf(0),IntNode.valueOf(1),IntNode.valueOf(3)),null,null);
  return new LazyTailArrayNode(record,schema);
}
