{
  final Class<?>[] actualParamTypes=actualSignature.parameterTypes;
  if (this.parameterTypes.length != actualParamTypes.length)   return INCOMPATIBLE;
  int distance=0;
  for (int index=0; index < this.parameterTypes.length; index++) {
    final Class<?> paramType=actualParamTypes[index];
    if (paramType != null) {
      final int actualDistance=ReflectUtil.getDistance(this.parameterTypes[index],paramType);
      if (actualDistance < 0)       return INCOMPATIBLE;
      distance+=actualDistance;
    }
  }
  return distance;
}
