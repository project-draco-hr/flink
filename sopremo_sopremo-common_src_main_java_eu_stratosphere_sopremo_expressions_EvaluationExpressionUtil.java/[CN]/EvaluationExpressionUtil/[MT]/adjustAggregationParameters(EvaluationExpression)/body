{
  return evaluationExpression.clone().remove(InputSelection.class).replace(new IsInstancePredicate(ArrayProjection.class),new TransformFunction(){
    @Override public EvaluationExpression call(    EvaluationExpression argument){
      return ((ArrayProjection)argument).getExpression();
    }
  }
).simplify();
}
