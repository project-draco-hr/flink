{
  validateStateId(stateId);
  PreparedStatement smt=con.prepareStatement("SELECT v " + "FROM kvstate_" + stateId + " WHERE k = ? "+ " AND id <= ? "+ "ORDER BY id DESC");
  smt.setMaxRows(1);
  return smt;
}
