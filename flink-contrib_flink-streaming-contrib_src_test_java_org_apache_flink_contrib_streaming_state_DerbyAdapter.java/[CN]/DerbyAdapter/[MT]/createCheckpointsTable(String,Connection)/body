{
  try (Statement smt=con.createStatement()){
    smt.executeUpdate("CREATE TABLE checkpoints_" + appId + " ("+ "checkpointId bigint, "+ "timestamp bigint, "+ "handleId bigint,"+ "checkpoint blob,"+ "PRIMARY KEY (handleId)"+ ")");
  }
 catch (  SQLException se) {
    if (se.getSQLState().equals("X0Y32")) {
    }
 else {
      throw se;
    }
  }
}
