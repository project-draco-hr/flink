{
  final ArrayList<List<? extends EvaluationExpression>> allKeys=new ArrayList<List<? extends EvaluationExpression>>();
  final List<JsonStream> inputs=getInputs();
  for (int index=0; index < inputs.size(); index++)   allKeys.add(getKeyExpressions(index));
  return allKeys;
}
