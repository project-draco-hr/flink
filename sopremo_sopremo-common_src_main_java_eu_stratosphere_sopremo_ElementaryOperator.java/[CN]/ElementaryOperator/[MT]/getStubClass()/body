{
  for (  Class<?> contractClass : getClass().getDeclaredClasses()) {
    if ((contractClass.getModifiers() & Modifier.STATIC) != 0 && Stub.class.isAssignableFrom(contractClass))     return (Class<? extends Stub<?,?>>)contractClass;
  }
  return null;
}
