{
  super(template,globalProps,localProps);
  this.input=new PactConnection(conn,pred,this);
  if (pred.branchPlan != null && pred.branchPlan.size() > 0)   this.branchPlan=new HashMap<OptimizerNode,OptimizerNode>(pred.branchPlan);
 else   this.branchPlan=null;
}
