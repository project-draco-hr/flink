{
  SopremoMethod javaMethod=get(method.getName());
  if (javaMethod == null || !(javaMethod instanceof JavaMethod))   this.put(method.getName(),javaMethod=new JavaMethod(method.getName()));
  ((JavaMethod)javaMethod).addSignature(method);
}
