{
  circularList.add(nextValue);
  counter++;
  if (miniBatchEnd()) {
    circularList.newSlide();
    minibatchCounter++;
    if (batchEnd()) {
      reduceBatch();
      circularList.shiftWindow(batchPerSlide);
    }
  }
}
