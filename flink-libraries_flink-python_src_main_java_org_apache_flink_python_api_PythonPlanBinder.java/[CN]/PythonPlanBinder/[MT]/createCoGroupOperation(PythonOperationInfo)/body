{
  DataSet op1=(DataSet)sets.get(info.parentID);
  DataSet op2=(DataSet)sets.get(info.otherID);
  sets.put(info.setID,new CoGroupRawOperator(op1,op2,new Keys.ExpressionKeys(info.keys1,op1.getType()),new Keys.ExpressionKeys(info.keys2,op2.getType()),new PythonCoGroup(info.setID,info.types),info.types,info.name));
}
