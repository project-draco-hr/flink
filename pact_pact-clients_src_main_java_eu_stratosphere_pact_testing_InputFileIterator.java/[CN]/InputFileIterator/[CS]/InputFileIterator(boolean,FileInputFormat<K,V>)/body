{
  this.inputFormats=Arrays.asList(inputFormats);
  this.formatIterator=this.inputFormats.iterator();
  this.currentFormat=inputFormats[0];
  this.reusePair=reusePair;
  if (reusePair)   this.pair=this.currentFormat.createPair();
}
