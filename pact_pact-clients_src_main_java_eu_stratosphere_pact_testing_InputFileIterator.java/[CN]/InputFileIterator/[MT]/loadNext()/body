{
  final InputFormat<K,V> currentFormat=this.currentFormat();
  if (currentFormat == null) {
    this.nextPair=null;
    return;
  }
  if (!this.reusePair)   this.nextPair=currentFormat.createPair();
  try {
    if (!currentFormat.nextPair(this.nextPair))     this.nextPair=null;
  }
 catch (  final IOException e) {
    Assert.fail("reading expected values " + StringUtils.stringifyException(e));
  }
}
