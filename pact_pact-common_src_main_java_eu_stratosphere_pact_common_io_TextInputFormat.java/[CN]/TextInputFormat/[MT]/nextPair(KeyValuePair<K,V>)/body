{
  try {
    byte[] line=readLine();
    if (line == null) {
      end=true;
      return false;
    }
 else {
      return readLine(pair,line);
    }
  }
 catch (  IOException e) {
    LOG.error(e);
    return false;
  }
}
