{
  readBuffer=new byte[bufferSize];
  wrapBuffer=new byte[256];
  this.readPos=0;
  this.overLimit=false;
  this.end=false;
  try {
    if (start != 0) {
      stream.seek(start);
      readLine();
    }
 else {
      fillBuffer();
    }
  }
 catch (  IOException ex) {
  }
}
