{
  RichEdge<VV,EV> userRichEdge=new RichEdge<VV,EV>(richEdge.f0.getValue(),richEdge.f1.getValue());
  K key=richEdge.f1.getTarget();
  M result=this.gatherFunction.gather(userRichEdge);
  return new Tuple2<K,M>(key,result);
}
