{
  if (this == obj)   return true;
  if (obj == null)   return false;
  if (getClass() != obj.getClass())   return false;
  SopremoTestPlan other=(SopremoTestPlan)obj;
  return Arrays.equals(inputs,other.inputs) && Arrays.equals(expectedOutputs,other.expectedOutputs) && Arrays.equals(actualOutputs,other.actualOutputs);
}
