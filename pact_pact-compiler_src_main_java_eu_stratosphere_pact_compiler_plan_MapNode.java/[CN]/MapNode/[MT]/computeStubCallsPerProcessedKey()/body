{
  long numRecords=computeNumberOfStubCalls();
  if (numRecords == -1)   return -1;
  long keyCardinality=computeNumberOfProcessedKeys();
  if (keyCardinality == -1)   return -1;
  return numRecords / (double)keyCardinality;
}
