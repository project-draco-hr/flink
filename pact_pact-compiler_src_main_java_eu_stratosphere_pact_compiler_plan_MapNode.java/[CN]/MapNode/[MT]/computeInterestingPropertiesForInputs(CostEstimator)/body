{
  List<InterestingProperties> thisNodesIntProps=getInterestingProperties();
  List<InterestingProperties> props=InterestingProperties.filterByConstantSet(thisNodesIntProps,getInputConstantSet(0));
  for (  PactConnection c : this.input) {
    if (!props.isEmpty()) {
      c.addAllInterestingProperties(props);
    }
 else {
      c.setNoInterestingProperties();
    }
  }
}
