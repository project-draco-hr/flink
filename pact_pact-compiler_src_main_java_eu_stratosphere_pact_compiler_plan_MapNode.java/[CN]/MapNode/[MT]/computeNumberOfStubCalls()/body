{
  long sumStubCalls=0;
  for (  PactConnection c : this.input) {
    OptimizerNode pred=c.getSourcePact();
    if (pred != null) {
      if (pred.estimatedNumRecords == -1)       return -1;
      sumStubCalls+=pred.estimatedNumRecords;
    }
  }
  return sumStubCalls;
}
