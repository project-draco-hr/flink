{
  this.children.clear();
  final int len=in.readInt();
  for (int i=0; i < len; i++) {
    JsonNode node=SopremoUtil.deserializeNode(in);
    node.read(in);
    this.add(node.canonicalize());
  }
}
