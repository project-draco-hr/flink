{
  when(mockJobClient.submitJobDetached(any(JobGraph.class),any(boolean.class),any(ActorRef.class))).thenReturn(mockSubmissionFailure);
  Client out=new Client(configMock,getClass().getClassLoader());
  out.run(program.getPlanWithJars(),-1,false);
  program.deleteExtractedLibraries();
}
