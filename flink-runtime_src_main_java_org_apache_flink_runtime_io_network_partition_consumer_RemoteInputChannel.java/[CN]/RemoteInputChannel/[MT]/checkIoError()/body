{
  IOException error=ioError.get();
  if (error != null) {
    throw new IOException(String.format("%s at remote input channel of task '%s': %s].",error.getClass().getName(),reader.getTaskNameWithSubtasks(),error.getMessage()));
  }
}
