{
  assertEquals(1,fieldsPartitioner.selectChannels(streamRecord1,1).length);
  assertEquals(1,fieldsPartitioner.selectChannels(streamRecord1,2).length);
  assertEquals(1,fieldsPartitioner.selectChannels(streamRecord1,1024).length);
}
