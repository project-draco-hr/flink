{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  @SuppressWarnings("unused") DataStream<Tuple2<Integer,Long>> dataStream=env.addSource(new WindowSumSource()).map(new WindowSumMultiple()).flatMap(new WindowSumAggregate()).addSink(new WindowSumSink());
  env.execute();
}
