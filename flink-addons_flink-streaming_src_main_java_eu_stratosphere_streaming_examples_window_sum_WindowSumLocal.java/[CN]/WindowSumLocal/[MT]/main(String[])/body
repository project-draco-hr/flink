{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  @SuppressWarnings("unused") DataStream<Tuple2<Integer,Long>> dataStream=context.addSource(new WindowSumSource()).map(new WindowSumMultiple()).flatMap(new WindowSumAggregate()).addSink(new WindowSumSink());
  context.execute();
}
