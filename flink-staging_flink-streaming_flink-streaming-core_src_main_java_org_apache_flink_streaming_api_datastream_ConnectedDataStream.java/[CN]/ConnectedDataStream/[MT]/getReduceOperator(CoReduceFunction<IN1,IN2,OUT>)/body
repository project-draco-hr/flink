{
  if (isGrouped) {
    return new CoStreamGroupedReduce<IN1,IN2,OUT>(clean(coReducer),keySelector1,keySelector2);
  }
 else {
    throw new UnsupportedOperationException("Reduce can only be applied on grouped streams.");
  }
}
