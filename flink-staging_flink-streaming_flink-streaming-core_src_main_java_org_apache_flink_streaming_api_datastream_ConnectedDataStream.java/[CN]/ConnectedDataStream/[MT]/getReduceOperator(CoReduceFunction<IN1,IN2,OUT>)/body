{
  CoStreamReduce<IN1,IN2,OUT> operator;
  if (isGrouped) {
    operator=new CoStreamGroupedReduce<IN1,IN2,OUT>(clean(coReducer),keySelector1,keySelector2);
  }
 else {
    operator=new CoStreamReduce<IN1,IN2,OUT>(clean(coReducer));
  }
  return operator;
}
