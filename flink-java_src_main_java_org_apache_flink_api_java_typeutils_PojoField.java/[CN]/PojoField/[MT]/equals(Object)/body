{
  if (obj instanceof PojoField) {
    PojoField other=(PojoField)obj;
    return other.canEqual(this) && type.equals(other.type) && Objects.equals(field,other.field);
  }
 else {
    return false;
  }
}
