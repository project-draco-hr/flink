{
  T result=null;
  for (  T v : values) {
    if (result == null) {
      result=v;
    }
 else {
      result=reduceFunction.reduce(result,v);
    }
  }
  if (result != null) {
    out.collect(Tuple2.of(window,result));
  }
}
