{
  String query="$daily = read from 'NYSE_dividends.json';\n" + "$grpd  = group $daily by $daily.symbol into { " + "  mean: mean($daily) "+ "  count: count($daily) "+ "};\n"+ "write $grpd to 'hdfs://localhost:9000/grouped';\n";
  assertParserError(query,"json object");
}
