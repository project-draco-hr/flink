{
  if (obj instanceof AbstractTypeSchema) {
    AbstractTypeSchema<?> other=(AbstractTypeSchema<?>)obj;
    return this.schema.equals(other.schema) && this.numConnectionsThatContributed == other.numConnectionsThatContributed;
  }
 else {
    return false;
  }
}
