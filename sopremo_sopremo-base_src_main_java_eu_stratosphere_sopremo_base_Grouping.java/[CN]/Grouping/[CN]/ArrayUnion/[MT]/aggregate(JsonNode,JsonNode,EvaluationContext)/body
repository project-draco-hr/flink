{
  for (int index=0; index < array.size(); index++) {
    if (mergedArray.size() <= index)     ((ArrayNode)mergedArray).add(JsonUtil.NODE_FACTORY.arrayNode());
    if (!array.get(index).isNull())     ((ArrayNode)mergedArray.get(index)).add(array.get(index));
  }
  return mergedArray;
}
