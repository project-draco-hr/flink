{
  if (inputs.size() <= 1)   return new GroupProjection(this.projection,inputs.get(0));
  final UnionAll union=new UnionAll(inputs);
  return new GroupProjection(new PathExpression(new AggregationExpression(new ArrayUnion()),this.projection),union);
}
