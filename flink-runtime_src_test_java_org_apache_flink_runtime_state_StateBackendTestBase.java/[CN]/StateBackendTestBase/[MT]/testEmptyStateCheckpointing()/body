{
  try {
    DummyEnvironment env=new DummyEnvironment("test",1,0);
    backend.initializeForJob(env,"test_op",IntSerializer.INSTANCE);
    HashMap<String,KvStateSnapshot<?,?,?,?,?>> snapshot=backend.snapshotPartitionedState(682375462379L,1);
    assertNull(snapshot);
    backend.dispose();
    backend.initializeForJob(env,"test_op",IntSerializer.INSTANCE);
    backend.injectKeyValueStateSnapshots((HashMap)snapshot);
    backend.dispose();
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
}
