{
  ArrayNode arrayNode=(ArrayNode)node;
  for (int index=0, size=node.size(); index < size; index++)   arrayNode.set(index,((WritableEvaluable)this.expression).set(node.get(index),value,context));
  return arrayNode;
}
