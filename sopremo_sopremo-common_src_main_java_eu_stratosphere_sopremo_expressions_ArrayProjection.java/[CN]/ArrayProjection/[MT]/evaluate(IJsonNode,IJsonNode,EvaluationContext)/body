{
  final IArrayNode array=(IArrayNode)node;
  final ArrayNode arrayNode=new ArrayNode();
  for (int index=0, size=array.size(); index < size; index++)   arrayNode.add(this.expression.evaluate(array.get(index),null,context));
  return arrayNode;
}
