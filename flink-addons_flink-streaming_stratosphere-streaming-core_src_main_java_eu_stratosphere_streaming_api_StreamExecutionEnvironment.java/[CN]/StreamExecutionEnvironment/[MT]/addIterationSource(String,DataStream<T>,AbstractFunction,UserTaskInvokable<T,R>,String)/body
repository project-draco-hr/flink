{
  DataStream<R> returnStream=new DataStream<R>(this,functionName);
  jobGraphBuilder.setIterationSource(returnStream.getId(),functionInvokable,functionName,serializeToByteArray(function),degreeOfParallelism);
  jobGraphBuilder.shuffleConnect(returnStream.getId(),cID);
}
