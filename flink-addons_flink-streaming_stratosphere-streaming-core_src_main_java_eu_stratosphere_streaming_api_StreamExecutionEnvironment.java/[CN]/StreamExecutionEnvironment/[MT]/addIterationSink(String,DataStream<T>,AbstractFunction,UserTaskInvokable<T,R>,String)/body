{
  DataStream<R> returnStream=new DataStream<R>(this,functionName);
  jobGraphBuilder.setIterationSink(returnStream.getId(),functionInvokable,functionName,serializeToByteArray(function),degreeOfParallelism);
  jobGraphBuilder.shuffleConnect(cID,returnStream.getId());
}
