{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  @SuppressWarnings("unused") DataStream<Tuple2<String,Integer>> dataStream=context.addSource(new KMeansSource(2,2,1,5)).addFixPoint(new KMeansMap(),new KMeansReduce(),20).addSink(new KMeansSink());
  context.execute();
}
