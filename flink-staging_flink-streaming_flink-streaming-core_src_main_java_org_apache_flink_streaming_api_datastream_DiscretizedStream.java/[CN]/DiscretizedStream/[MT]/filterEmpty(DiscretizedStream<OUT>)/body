{
  return wrap(input.discretizedStream.transform("Filter",input.discretizedStream.getType(),new FilterInvokable<StreamWindow<OUT>>(new EmptyWindowFilter<OUT>()).withoutInputCopy()),input.isPartitioned);
}
