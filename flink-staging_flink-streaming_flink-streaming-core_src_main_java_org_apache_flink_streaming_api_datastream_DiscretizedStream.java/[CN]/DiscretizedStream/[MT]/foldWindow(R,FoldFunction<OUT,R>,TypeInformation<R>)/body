{
  DiscretizedStream<R> out=partition(transformation).transform(WindowTransformation.FOLDWINDOW,"Fold Window",outType,new WindowFolder<OUT,R>(discretizedStream.clean(foldFunction),initialValue)).setParallelism(discretizedStream.getParallelism()).merge();
  return out;
}
