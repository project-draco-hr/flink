{
  final IJsonNode target=new ArrayNode();
  final IJsonNode result=new ArrayCreation(new ConstantExpression(IntNode.valueOf(42))).evaluate(IntNode.valueOf(42),target,this.context);
  Assert.assertEquals(new ArrayNode(IntNode.valueOf(42)),result);
  Assert.assertSame(target,result);
}
