{
  Result expectedResult=new Result(13,6);
  Result globalClusteringCoefficient=new GlobalClusteringCoefficient<IntValue,NullValue,NullValue>().run(undirectedSimpleGraph).execute();
  assertEquals(expectedResult,globalClusteringCoefficient);
}
