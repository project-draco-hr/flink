{
  super(memoryManager,null,input,parentTask,serializerFactory,comparator,memoryFraction,memoryManager.computeNumberOfPages(memoryFraction) < 2 * MIN_NUM_SORT_MEM_SEGMENTS ? 1 : Math.max((int)Math.ceil(((double)memoryManager.computeMemorySize(memoryFraction)) / MAX_MEM_PER_PARTIAL_SORT),2),2,0.0f,true);
}
