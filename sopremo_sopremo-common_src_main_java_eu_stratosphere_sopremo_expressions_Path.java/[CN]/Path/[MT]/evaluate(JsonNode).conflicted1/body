{
  if (this.fragments.size() == 0)   return nodes[0];
  JsonNode fragmentNode=this.fragments.get(0).evaluate(nodes);
  for (  EvaluableExpression fragment : this.fragments.subList(1,fragments.size()))   fragmentNode=fragment.evaluate(fragmentNode);
  return fragmentNode;
}
