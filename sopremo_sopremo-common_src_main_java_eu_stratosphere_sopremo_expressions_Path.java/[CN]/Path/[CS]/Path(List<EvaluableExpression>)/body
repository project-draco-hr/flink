{
  for (  EvaluableExpression evaluableExpression : fragments) {
    if (evaluableExpression instanceof Path)     this.fragments.addAll(((Path)evaluableExpression).fragments);
 else     this.fragments.add(evaluableExpression);
  }
}
