{
  this.fragments=Arrays.asList(fragments);
  for (  EvaluableExpression evaluableExpression : fragments) {
    if (evaluableExpression instanceof Path)     throw new IllegalArgumentException();
  }
}
