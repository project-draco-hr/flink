{
  StackTraceElement[] st=Thread.currentThread().getStackTrace();
  if (st.length < depth) {
    return "<unknown>";
  }
  String callLoc=st[depth].toString();
  int idx=StringUtils.lastOrdinalIndexOf(callLoc,".",2);
  return callLoc.substring(idx + 1,callLoc.length());
}
