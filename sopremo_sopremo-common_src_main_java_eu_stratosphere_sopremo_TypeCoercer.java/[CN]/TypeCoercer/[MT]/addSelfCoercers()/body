{
  Coercer selfCoercer=new Coercer(){
    @Override public JsonNode coerce(    JsonNode node){
      return node;
    }
  }
;
  for (  Entry<Class<? extends JsonNode>,Map<Class<? extends JsonNode>,Coercer>> toCoercers : this.coercers.entrySet())   toCoercers.getValue().put(toCoercers.getKey(),selfCoercer);
}
