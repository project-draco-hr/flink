{
  this.defaultType=(Class<To>)BoundTypeUtil.getBindingOfSuperclass(this.getClass(),Coercer.class).getParameters()[1].getType();
  try {
    this.defaultType.newInstance();
  }
 catch (  final InstantiationException e) {
    e.printStackTrace();
  }
catch (  final IllegalAccessException e) {
    e.printStackTrace();
  }
}
