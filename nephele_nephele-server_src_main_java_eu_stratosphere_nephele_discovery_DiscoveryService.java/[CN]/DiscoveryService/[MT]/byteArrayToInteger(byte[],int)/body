{
  int integer=0;
  for (int i=0; i < 4; ++i) {
    System.out.print(byteArray[i]);
  }
  for (int i=0; i < 4; ++i) {
    integer|=(byteArray[(offset + 3) - i] & 0xff) << (i << 3);
  }
  return integer;
}
