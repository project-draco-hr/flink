{
  acquire();
  try {
    log.debug("Unsubscribed from partitions(s): {}",Utils.join(partitions,", "));
    for (    TopicPartition partition : partitions)     this.subscriptions.unsubscribe(partition);
  }
  finally {
    release();
  }
}
