{
  try {
    TypeInformation<IN> inputTypeInfo=(TypeInformation<IN>)typeWrapper.getOutputTypeInfo();
    inputSerializer=new StreamRecordSerializer<IN>(inputTypeInfo);
  }
 catch (  RuntimeException e) {
  }
}
