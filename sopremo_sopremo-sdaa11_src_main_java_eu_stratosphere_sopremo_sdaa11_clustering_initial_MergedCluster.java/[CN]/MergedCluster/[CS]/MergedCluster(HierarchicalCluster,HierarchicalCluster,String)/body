{
  super(id);
  this.child1=child1;
  this.child2=child2;
  this.size=child1.size() + child2.size();
  if (this.canBeFinal()) {
    this.createPoints();
    this.updateRowsums();
    this.chooseClustroid();
    this.calculateRadius();
  }
 else   this.clustroid=child1.getClustroid();
}
