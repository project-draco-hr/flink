{
  this.function.initialize();
  for (  final JsonNode node : (ArrayNode)nodes)   this.function.aggregate(this.preprocessing.evaluate(node,context),context);
  return this.function.getFinalAggregate();
}
