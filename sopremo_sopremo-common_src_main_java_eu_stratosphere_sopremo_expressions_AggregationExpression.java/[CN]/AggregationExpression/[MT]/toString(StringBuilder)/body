{
  super.toString(builder);
  builder.append('.');
  function.toString(builder);
  builder.append('(');
  if (preprocessing != EvaluationExpression.SAME_VALUE)   builder.append(preprocessing);
  builder.append(')');
}
