{
  this.function.initialize();
  for (  final IJsonNode node : (IArrayNode)nodes)   this.function.aggregate(this.preprocessing.evaluate(node,null,context),context);
  return this.function.getFinalAggregate();
}
