{
  this.function.initialize();
  for (  final IJsonNode node : (IArrayNode)nodes)   this.function.aggregate(this.preprocessing.evaluate(node,context),context);
  return this.function.getFinalAggregate();
}
