{
  if (!(o instanceof PactShort))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to PactShort!");
  final int other=((PactShort)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
