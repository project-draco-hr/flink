{
  this.context.increaseInputCounter();
  if (SopremoUtil.LOG.isDebugEnabled())   SopremoUtil.LOG.debug(String.format("%s %s/%s/%s",this.getClass().getSimpleName(),key,value1,value2));
  this.match(key.getValue(),value1.getValue(),value2.getValue(),new JsonCollector(out));
}
