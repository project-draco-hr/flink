{
  StreamRecordGeneric<T> newRecord=new StreamRecordGeneric<T>(numOfFields,numOfTuples);
  newRecord.uid=new UID(Arrays.copyOf(uid.getId(),20));
  for (  T tuple : tupleBatch) {
    newRecord.tupleBatch.add(copyTuple(tuple));
  }
  return newRecord;
}
