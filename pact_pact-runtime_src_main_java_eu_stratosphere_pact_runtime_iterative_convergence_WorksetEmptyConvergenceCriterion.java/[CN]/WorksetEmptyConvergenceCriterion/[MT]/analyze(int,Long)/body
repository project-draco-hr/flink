{
  if (log.isInfoEnabled()) {
    log.info("Received payload [" + elementsInWorkset + "] from worker ["+ workerIndex+ "] in iteration ["+ iteration+ "]");
  }
  if (elementsInWorkset.longValue() != 0) {
    worksetEmpty=false;
  }
}
