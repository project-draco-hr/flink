{
  if (log.isInfoEnabled()) {
    log.info("solutionset of worker [" + workerIndex + "] had ["+ updates+ "] updates in iteration "+ "["+ iteration+ "]");
  }
  this.updatesInSolutionset+=updates.longValue();
}
