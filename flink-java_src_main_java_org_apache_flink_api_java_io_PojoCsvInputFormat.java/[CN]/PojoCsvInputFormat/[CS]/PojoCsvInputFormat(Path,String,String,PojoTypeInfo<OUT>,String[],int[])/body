{
  super(filePath);
  boolean[] mask=(includedFieldsMask == null) ? createDefaultMask(fieldNames.length) : toBooleanMask(includedFieldsMask);
  configure(lineDelimiter,fieldDelimiter,pojoTypeInfo,fieldNames,mask);
}
