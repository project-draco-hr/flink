{
  if (!((ArrayNode)key).get(0).equals(new IntNode(this.iterationStep)) && !((ArrayNode)key).get(1).equals(new IntNode(this.iterationStep))) {
    out.collect(((ArrayNode)key).get(1),new ArrayNode(key,value));
  }
}
