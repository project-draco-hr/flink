{
  for (  File file : dir.listFiles())   if (file.isDirectory())   importFromProject(classPath,file);
 else   if (file.getName().endsWith(".class") && !file.getName().contains("$"))   importFromFile(classPath,file);
}
