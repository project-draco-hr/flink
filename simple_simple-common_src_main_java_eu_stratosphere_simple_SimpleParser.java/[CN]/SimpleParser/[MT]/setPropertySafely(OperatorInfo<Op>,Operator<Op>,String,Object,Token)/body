{
  OperatorPropertyInfo property=info.getOperatorProperty(propertyName);
  if (property == null)   throw new SimpleException("Unknown property",reference);
  try {
    property.setValue(op,value);
  }
 catch (  Exception e) {
    throw new SimpleException(String.format("Cannot set value of property %s to %s",propertyName,value),reference);
  }
}
