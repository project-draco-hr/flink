{
  ObjectNode objectNode;
  if (node == null || !(node instanceof ObjectNode))   objectNode=new ObjectNode();
 else   objectNode=(ObjectNode)node;
  final ArrayNode valuesNode=new ArrayNode();
  for (  final String value : this.values)   valuesNode.add(new TextNode(value));
  PointNodes.write(objectNode,new TextNode(this.key),valuesNode,new IntNode(this.rowsum));
  return objectNode;
}
