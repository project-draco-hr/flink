{
  long ctime=System.currentTimeMillis() - startTime;
  values.add(value);
  labels.add(label);
  timeStamps.add(ctime);
  if (dumpInterval > 0) {
    if (ctime - lastDump > dumpInterval) {
      writeCSV();
      lastDump=ctime;
    }
  }
}
