{
  counter=counter + i;
  intervalCounter++;
  if (intervalCounter % countInterval == 0) {
    intervalCounter=0;
    timeStamps.add(System.currentTimeMillis());
    values.add(counter);
    labels.add(label);
  }
}
