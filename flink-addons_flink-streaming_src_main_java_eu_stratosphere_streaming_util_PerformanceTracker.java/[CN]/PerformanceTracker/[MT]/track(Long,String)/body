{
  buffer=buffer + value;
  intervalCounter++;
  if (intervalCounter % countInterval == 0) {
    timeStamps.add(System.currentTimeMillis());
    values.add(buffer);
    labels.add(label);
    buffer=0;
    intervalCounter=0;
  }
}
