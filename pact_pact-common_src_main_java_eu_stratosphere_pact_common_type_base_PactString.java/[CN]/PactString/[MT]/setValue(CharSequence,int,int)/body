{
  if (value == null)   throw new NullPointerException("Value must not be null");
  ensureSize(len);
  this.len=len;
  for (int i=0; i < len; i++)   this.value[i]=value.charAt(i);
  this.len=len;
  this.hashCode=0;
}
