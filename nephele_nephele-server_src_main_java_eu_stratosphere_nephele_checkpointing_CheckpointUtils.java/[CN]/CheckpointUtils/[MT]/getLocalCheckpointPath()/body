{
  if (LOCAL_CHECKPOINT_PATH == null) {
    LOCAL_CHECKPOINT_PATH=new Path(GlobalConfiguration.getString(LOCAL_CHECKPOINT_PATH_KEY,DEFAULT_LOCAL_CHECKPOINT_PATH));
  }
  return LOCAL_CHECKPOINT_PATH;
}
