{
  StreamExecutionEnvironment env=StreamExecutionEnvironment.createLocalEnvironment();
  DataStream<Tuple2<Integer,Long>> dataStream=env.addSource(new WindowSumSource(),SOURCE_PARALELISM).map(new WindowSumMultiple(),PARALELISM).flatMap(new WindowSumAggregate(100,20,10,1),PARALELISM).addSink(new WindowSumSink());
  env.execute();
}
