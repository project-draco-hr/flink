{
  DataStream<T> returnStream=new DataStream<T>(this,"source");
  jobGraphBuilder.addSource(returnStream.getId(),sourceFunction,"source",SerializationUtils.serialize(sourceFunction),parallelism);
  return returnStream;
}
