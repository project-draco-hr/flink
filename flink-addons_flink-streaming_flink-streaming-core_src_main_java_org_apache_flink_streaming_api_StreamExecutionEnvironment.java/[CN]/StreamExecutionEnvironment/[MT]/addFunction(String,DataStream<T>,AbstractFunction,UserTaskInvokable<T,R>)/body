{
  DataStream<R> returnStream=new DataStream<R>(this,functionName);
  jobGraphBuilder.addTask(returnStream.getId(),functionInvokable,functionName,serializeToByteArray(function),degreeOfParallelism);
  connectGraph(inputStream,returnStream.getId());
  return returnStream;
}
