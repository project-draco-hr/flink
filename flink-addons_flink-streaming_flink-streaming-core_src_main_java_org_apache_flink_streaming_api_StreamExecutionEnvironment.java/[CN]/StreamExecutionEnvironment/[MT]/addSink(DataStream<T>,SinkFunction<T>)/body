{
  DataStream<T> returnStream=new DataStream<T>(this,"sink");
  jobGraphBuilder.addSink(returnStream.getId(),new SinkInvokable<T>(sinkFunction),"sink",serializeToByteArray(sinkFunction),degreeOfParallelism);
  connectGraph(inputStream,returnStream.getId());
  return returnStream;
}
