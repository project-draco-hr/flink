{
  this.running=false;
  if (LOG.isWarnEnabled())   LOG.warn(formatLogString("Cancelling PACT code"));
  closeLocalStrategiesAndCaches();
  if (this.driver != null) {
    this.driver.cancel();
  }
}
