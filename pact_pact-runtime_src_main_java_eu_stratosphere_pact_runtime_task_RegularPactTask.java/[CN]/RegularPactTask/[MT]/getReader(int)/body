{
  if (index < 0 || index > this.driver.getNumberOfInputs()) {
    throw new IndexOutOfBoundsException();
  }
  @SuppressWarnings("unchecked") final MutableReader<X> in=(MutableReader<X>)this.inputReaders[index];
  return in;
}
