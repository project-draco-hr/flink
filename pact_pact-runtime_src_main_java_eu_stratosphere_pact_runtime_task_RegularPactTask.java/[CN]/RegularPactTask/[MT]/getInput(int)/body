{
  if (index < 0 || index > this.driver.getNumberOfInputs()) {
    throw new IndexOutOfBoundsException();
  }
  @SuppressWarnings("unchecked") final MutableObjectIterator<X> in=(MutableObjectIterator<X>)this.inputs[index];
  return in;
}
