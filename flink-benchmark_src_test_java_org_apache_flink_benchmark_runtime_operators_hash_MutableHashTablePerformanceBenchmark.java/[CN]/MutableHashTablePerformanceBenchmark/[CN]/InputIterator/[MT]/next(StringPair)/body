{
  if (this.numLeft > 0) {
    numLeft--;
    int currentKey=(numLeft * distance) % scope;
    reuse.setKey(Integer.toString(currentKey));
    reuse.setValue(COMMENT);
    return reuse;
  }
 else {
    return null;
  }
}
