{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  @SuppressWarnings("unused") DataStream<Tuple2<String,Integer>> dataStream=context.addSource(new SSSPSource()).flatMap(new SSSPCounter()).addSink(new SSSPSink());
  context.execute();
}
