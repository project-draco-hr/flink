{
  super(id);
  this.child1=child1;
  this.child2=child2;
  this.size=child1.size() + child2.size();
  if (canBeFinal()) {
    createPoints();
    updateRowsums();
    chooseClustroid();
    calculateRadius();
  }
 else {
    clustroid=child1.getClustroid();
  }
}
