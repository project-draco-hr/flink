{
  if (numPartitions != 3) {
    throw new IllegalArgumentException("Expected three partitions");
  }
  @SuppressWarnings("unchecked") Tuple2<Integer,Integer> element=(Tuple2<Integer,Integer>)key;
  return element.f0;
}
