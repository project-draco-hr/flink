{
  TypeInformation<R> outTypeInfo=TypeExtractor.getUnaryOperatorReturnType(patternSelectFunction,PatternSelectFunction.class,1,-1,inputType,null,false);
  return patternStream.map(new PatternSelectMapper<T,R>(patternStream.getExecutionEnvironment().clean(patternSelectFunction))).returns(outTypeInfo);
}
