{
  ExecutionEnvironment env=ExecutionEnvironment.getExecutionEnvironment();
  Graph<String> graph=Graph.fromEdges(env,String.class,new EdgesCsvBuilder<String>("/the/edges/file").sourceColumn(2).targetColumn(5));
}
