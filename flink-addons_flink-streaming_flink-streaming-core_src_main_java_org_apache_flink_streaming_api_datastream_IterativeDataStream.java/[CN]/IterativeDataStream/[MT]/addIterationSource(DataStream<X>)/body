{
  DataStream<X> iterationSource=new DataStreamSource<X>(environment,"Iteration Source",null,null,true);
  streamGraph.addIterationHead(iterationSource.getId(),dataStream.getId(),iterationID,dataStream.getParallelism(),waitTime);
}
