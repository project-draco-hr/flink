{
synchronized (this.buffers) {
    if (!this.buffers.isEmpty()) {
      return false;
    }
    if (this.isDestroyed) {
      return false;
    }
    this.listeners.add(listener);
  }
  return true;
}
