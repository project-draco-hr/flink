{
  StreamExecutionEnvironment context=new StreamExecutionEnvironment();
  @SuppressWarnings("unused") DataStream<Tuple2<String,Integer>> dataStream=context.addSource(new PageRankSource()).map(new Counter()).addSink(new PageRankSink());
  context.execute();
}
