{
  buffer=buffer + i;
  intervalCounter++;
  if (intervalCounter % interval == 0) {
    intervalCounter=0;
    timeStamps.add(System.currentTimeMillis());
    values.add(buffer);
    labels.add(label);
  }
}
