{
  addComponent(componentName,StreamTask.class,typeWrapper,taskInvokableObject,operatorName,serializedFunction,parallelism);
  if (LOG.isDebugEnabled()) {
    LOG.debug("TASK: " + componentName);
  }
}
