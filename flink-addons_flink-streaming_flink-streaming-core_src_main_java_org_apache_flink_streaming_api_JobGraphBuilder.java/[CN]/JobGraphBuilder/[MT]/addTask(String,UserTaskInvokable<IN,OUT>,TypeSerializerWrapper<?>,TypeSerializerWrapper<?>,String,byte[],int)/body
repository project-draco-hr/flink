{
  addComponent(componentName,StreamTask.class,taskInvokableObject,operatorName,serializedFunction,parallelism);
  addTypeWrappers(componentName,inTypeWrapper,null,outTypeWrapper,null);
  if (LOG.isDebugEnabled()) {
    LOG.debug("TASK: " + componentName);
  }
}
