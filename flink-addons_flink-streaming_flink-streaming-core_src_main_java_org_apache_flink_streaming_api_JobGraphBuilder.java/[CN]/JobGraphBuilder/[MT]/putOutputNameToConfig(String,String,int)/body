{
  Configuration config=new TaskConfig(components.get(upStreamComponentName).getConfiguration()).getConfiguration();
  String outputName=userDefinedNames.get(downStreamComponentName);
  if (outputName == null) {
    outputName="";
  }
  config.setString("outputName_" + (index),outputName);
}
