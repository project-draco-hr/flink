{
  acquire();
  try {
    log.debug("Unsubscribed from topic(s): {}",Utils.join(topics,", "));
    for (    String topic : topics)     this.subscriptions.unsubscribe(topic);
  }
  finally {
    release();
  }
}
