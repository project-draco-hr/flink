{
  ois.defaultReadObject();
  blacklistedValues=new ArrayList<JsonNode>();
  ArrayNode array=SopremoUtil.deserializeNode(ois,ArrayNode.class);
  for (  JsonNode jsonNode : array)   this.blacklistedValues.add(jsonNode);
}
