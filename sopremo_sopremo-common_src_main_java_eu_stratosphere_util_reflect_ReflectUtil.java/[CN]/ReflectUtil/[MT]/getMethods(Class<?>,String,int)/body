{
  ArrayList<Method> methods=new ArrayList<Method>();
  for (  Method method : clazz.getMethods())   if ((name == null || method.getName().equals(name)) && (method.getModifiers() & modifiers) != 0)   methods.add(method);
  return methods;
}
