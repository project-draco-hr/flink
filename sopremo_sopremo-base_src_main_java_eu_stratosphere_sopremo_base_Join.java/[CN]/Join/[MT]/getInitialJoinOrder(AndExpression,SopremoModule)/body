{
  final List<TwoSourceJoin> joins=new ArrayList<TwoSourceJoin>();
  for (  final EvaluationExpression expression : condition.getExpressions())   joins.add(this.getTwoSourceJoinForExpression(expression,module));
  return joins;
}
