{
  final List<TwoSourceJoin> joins=new ArrayList<TwoSourceJoin>();
  for (  final BinaryBooleanExpression expression : binaryConditions)   joins.add(this.getTwoSourceJoinForExpression(expression,module));
  return joins;
}
