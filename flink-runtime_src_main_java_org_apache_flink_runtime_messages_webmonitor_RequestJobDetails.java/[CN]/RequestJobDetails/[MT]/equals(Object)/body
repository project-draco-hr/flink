{
  if (this == o) {
    return true;
  }
 else   if (o instanceof RequestJobDetails) {
    RequestJobDetails that=(RequestJobDetails)o;
    return this.includeFinished == that.includeFinished && this.includeRunning == that.includeRunning;
  }
 else {
    return false;
  }
}
