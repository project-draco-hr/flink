{
  if (inputList.size() == 1) {
    return inputList.get(0);
  }
 else   if (inputList.size() > 1) {
    return new MutableUnionRecordReader<IOReadableWritable>((MutableRecordReader<IOReadableWritable>[])inputList.toArray());
  }
  return null;
}
