{
  Class<? extends CoInvokable> userFunctionClass=configuration.getClass("userfunction",CoMapInvokable.class,CoInvokable.class);
  userFunction=(CoInvokable<IN1,IN2,OUT>)getInvokable(userFunctionClass);
  userFunction.initialize(collector,inputIter1,inTupleSerializer1,inputIter2,inTupleSerializer2);
}
