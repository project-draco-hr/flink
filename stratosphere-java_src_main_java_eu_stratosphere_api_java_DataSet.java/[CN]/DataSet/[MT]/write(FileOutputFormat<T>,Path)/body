{
  if (filePath == null)   throw new IllegalArgumentException("File path must not be null.");
  if (outputFormat == null)   throw new IllegalArgumentException("The output format must not be null.");
  outputFormat.setOutputFilePath(filePath);
  output(outputFormat);
}
