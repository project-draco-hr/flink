{
  super(String.format("%s Task to schedule: < %s > in sharing group < %s >. Resources available to scheduler: Number of instances=%d, total number of slots=%d",BASE_MESSAGE,task.getTaskToExecute(),task.getSlotSharingGroup(),numInstances,numSlotsTotal));
}
