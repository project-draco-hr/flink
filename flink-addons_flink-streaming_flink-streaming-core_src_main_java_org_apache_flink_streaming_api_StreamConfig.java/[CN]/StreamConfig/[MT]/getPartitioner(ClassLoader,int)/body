{
  @SuppressWarnings("unchecked") StreamPartitioner<T> partitioner=(StreamPartitioner<T>)InstantiationUtil.readObjectFromConfig(this.config,PARTITIONER_OBJECT + outputIndex,cl);
  if (partitioner != null) {
    return partitioner;
  }
 else {
    return new ShufflePartitioner<T>();
  }
}
