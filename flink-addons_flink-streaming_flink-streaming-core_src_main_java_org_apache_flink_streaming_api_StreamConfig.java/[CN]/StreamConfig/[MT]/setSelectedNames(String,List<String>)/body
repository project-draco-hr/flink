{
  if (selected != null) {
    config.setBytes(OUTPUT_NAME + output,SerializationUtils.serialize((Serializable)selected));
  }
 else {
    config.setBytes(OUTPUT_NAME + output,SerializationUtils.serialize((Serializable)new ArrayList<String>()));
  }
}
