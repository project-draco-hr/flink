{
  try {
    return (ChainableInvokable<?,?>)InstantiationUtil.readObjectFromConfig(this.config,CHAINED_INVOKABLE + chainedTaskIndex,cl);
  }
 catch (  Exception e) {
    throw new RuntimeException("Could not instantiate invokable.");
  }
}
