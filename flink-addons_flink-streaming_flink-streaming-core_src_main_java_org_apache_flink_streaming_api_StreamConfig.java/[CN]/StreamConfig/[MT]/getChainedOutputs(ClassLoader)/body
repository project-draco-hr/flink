{
  try {
    return (List<String>)InstantiationUtil.readObjectFromConfig(this.config,CHAINED_OUTPUTS,cl);
  }
 catch (  Exception e) {
    throw new RuntimeException("Could not instantiate chained outputs.");
  }
}
