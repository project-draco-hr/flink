{
  final int size=this.size();
  if (index < 0 || index >= size)   return MissingNode.getInstance();
  final int recordPosition=this.schema.getTailSize() - size + index;
  if (recordPosition >= 0)   return SopremoUtil.unwrap(this.record.getField(recordPosition + 1,JsonNodeWrapper.class));
  return this.getOtherField().get(index);
}
