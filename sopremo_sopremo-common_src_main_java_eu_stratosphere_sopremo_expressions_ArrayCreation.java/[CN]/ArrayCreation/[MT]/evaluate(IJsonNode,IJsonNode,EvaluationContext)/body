{
  target=SopremoUtil.reuseTarget(target,this.expectedTarget);
  int index=0;
  for (  final EvaluationExpression expression : this.elements)   ((IArrayNode)target).add(expression.evaluate(node,((IArrayNode)target).get(index++),context));
  return target;
}
