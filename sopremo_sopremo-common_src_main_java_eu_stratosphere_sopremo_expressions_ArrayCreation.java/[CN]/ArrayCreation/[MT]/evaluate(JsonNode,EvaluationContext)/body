{
  final ArrayNode arrayNode=JsonUtil.NODE_FACTORY.arrayNode();
  for (  final EvaluationExpression expression : this.elements)   arrayNode.add(expression.evaluate(node,context));
  return arrayNode;
}
