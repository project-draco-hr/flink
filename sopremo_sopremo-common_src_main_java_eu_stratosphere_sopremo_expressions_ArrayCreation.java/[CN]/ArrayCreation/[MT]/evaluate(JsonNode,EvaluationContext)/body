{
  final ArrayNode arrayNode=new ArrayNode();
  for (  final SopremoExpression<EvaluationContext> expression : this.elements)   arrayNode.add(expression.evaluate(node,context));
  return arrayNode;
}
