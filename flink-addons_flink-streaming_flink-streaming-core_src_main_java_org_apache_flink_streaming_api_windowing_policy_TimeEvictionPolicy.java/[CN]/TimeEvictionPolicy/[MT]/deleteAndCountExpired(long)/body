{
  int counter=0;
  while (!buffer.isEmpty()) {
    if (timestamp.getTimestamp(buffer.getFirst()) < threshold) {
      buffer.removeFirst();
      counter++;
    }
 else {
      break;
    }
  }
  return counter;
}
