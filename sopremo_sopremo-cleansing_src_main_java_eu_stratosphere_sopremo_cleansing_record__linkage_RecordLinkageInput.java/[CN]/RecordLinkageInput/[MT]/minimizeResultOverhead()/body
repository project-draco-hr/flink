{
  if (getIdProjection() != EvaluationExpression.VALUE && !getResultProjection().equals(getIdProjection())) {
    RecordLinkageInput clone=clone();
    clone.setResultProjection(getIdProjection());
    return clone;
  }
  return this;
}
