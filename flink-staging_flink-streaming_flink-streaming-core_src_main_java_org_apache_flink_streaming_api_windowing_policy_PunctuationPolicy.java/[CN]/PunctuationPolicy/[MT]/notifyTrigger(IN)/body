{
  DATA tmp;
  if (extractor == null) {
    tmp=(DATA)datapoint;
  }
 else {
    tmp=extractor.extract(datapoint);
  }
  if (punctuation.equals(tmp)) {
    return true;
  }
 else {
    return false;
  }
}
