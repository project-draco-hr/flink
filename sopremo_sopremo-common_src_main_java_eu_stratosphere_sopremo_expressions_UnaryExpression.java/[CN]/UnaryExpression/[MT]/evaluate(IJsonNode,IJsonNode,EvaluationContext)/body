{
  final BooleanNode result=TypeCoercer.INSTANCE.coerce(this.expr.evaluate(node,target,context),null,BooleanNode.class);
  if (this.negate)   return result == BooleanNode.TRUE ? BooleanNode.FALSE : BooleanNode.TRUE;
  return result;
}
