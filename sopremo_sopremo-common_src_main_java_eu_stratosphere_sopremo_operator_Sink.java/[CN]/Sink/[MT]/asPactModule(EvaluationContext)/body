{
  PactModule pactModule=new PactModule(1,0);
  DataSinkContract<PactNull,PactJsonObject> contract=new DataSinkContract<PactNull,PactJsonObject>(JsonOutputFormat.class,this.outputName);
  contract.setInput(pactModule.getInput(0));
  pactModule.addInternalOutput(contract);
  return pactModule;
}
