{
  PactModule pactModule=new PactModule(1,1);
  DataSinkContract<PactNull,PactJsonObject> contract=new DataSinkContract<PactNull,PactJsonObject>(JsonOutputFormat.class,outputName);
  contract.setInput(pactModule.getInput(0));
  pactModule.setOutput(0,contract);
  return pactModule;
}
