{
  super(dataStream.getExecutionEnvironment(),new PartitionTransformation<>(dataStream.getTransformation(),new KeyGroupStreamPartitioner<>(keySelector,new HashKeyGroupAssigner<KEY>())));
  this.keySelector=keySelector;
  this.keyType=keyType;
}
