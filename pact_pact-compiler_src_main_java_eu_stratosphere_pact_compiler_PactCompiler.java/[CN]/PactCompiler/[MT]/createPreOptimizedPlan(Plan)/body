{
  GraphCreatingVisitor graphCreator=new GraphCreatingVisitor(null,-1,-1,false);
  pactPlan.accept(graphCreator);
  return new OptimizedPlan(graphCreator.sources,graphCreator.sinks,graphCreator.con2node.values(),pactPlan.getJobName());
}
