{
  OptimizerNode n=this.con2node.get(c);
  if (n.getId() > 0) {
    return;
  }
  n.SetId(this.id++);
  if (!(c instanceof GenericDataSource<?,?>))   n.setInputs(this.con2node);
  if (this.computeEstimates) {
    n.computeOutputEstimates(this.statistics);
  }
}
