{
  if (node == null)   throw new NullPointerException();
  for (int i=0; i < this.schema.getHeadSize(); i++)   if (this.record.isNull(i)) {
    this.record.setField(i,SopremoUtil.wrap(node));
    return this;
  }
  this.getOtherField().add(node);
  return this;
}
