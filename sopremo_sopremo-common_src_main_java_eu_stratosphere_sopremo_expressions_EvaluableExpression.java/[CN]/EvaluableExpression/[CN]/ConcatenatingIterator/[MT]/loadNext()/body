{
  while (!this.inputs.isEmpty()) {
    Iterator<JsonNode> iterator=this.inputs.getFirst();
    if (!iterator.hasNext())     this.inputs.pop();
 else     return EvaluableExpression.this.evaluate(iterator.next());
  }
  return this.noMoreElements();
}
