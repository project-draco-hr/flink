{
  bufferSpiller.close();
  File spillfile1=bufferSpiller.getSpillFile();
  if (spillfile1 != null) {
    spillfile1.delete();
  }
  spillReader.close();
  File spillfile2=spillReader.getSpillFile();
  if (spillfile2 != null) {
    spillfile2.delete();
  }
}
