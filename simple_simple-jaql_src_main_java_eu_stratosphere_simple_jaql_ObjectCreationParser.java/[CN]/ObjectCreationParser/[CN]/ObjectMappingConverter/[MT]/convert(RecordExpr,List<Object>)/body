{
  ObjectCreation creation=new ObjectCreation();
  for (  Object mapping : childEvaluableExpressions)   if (mapping instanceof ObjectCreation.Mapping)   creation.addMapping((Mapping)mapping);
  return creation;
}
