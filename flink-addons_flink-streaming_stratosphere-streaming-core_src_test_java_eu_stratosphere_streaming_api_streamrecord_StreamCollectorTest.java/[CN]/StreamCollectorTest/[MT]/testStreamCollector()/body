{
  MockRecordWriter recWriter=MockRecordWriterFactory.create();
  StreamCollector<Tuple1<Integer>> collector=new StreamCollector<Tuple1<Integer>>(10,1000,0,null,recWriter);
  assertEquals(10,collector.batchSize);
}
