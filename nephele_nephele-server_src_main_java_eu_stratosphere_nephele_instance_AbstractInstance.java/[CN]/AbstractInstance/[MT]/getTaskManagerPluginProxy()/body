{
  if (this.taskManagerPluginComponent == null) {
    this.taskManagerPluginComponent=this.rpcService.getProxy(new InetSocketAddress(getInstanceConnectionInfo().getAddress(),getInstanceConnectionInfo().getIPCPort()),PluginCommunicationProtocol.class);
  }
  return this.taskManagerPluginComponent;
}
