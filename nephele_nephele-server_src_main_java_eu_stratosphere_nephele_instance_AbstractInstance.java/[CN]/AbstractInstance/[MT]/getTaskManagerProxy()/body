{
  if (this.taskManager == null) {
    this.taskManager=this.rpcService.getProxy(new InetSocketAddress(getInstanceConnectionInfo().getAddress(),getInstanceConnectionInfo().getIPCPort()),TaskOperationProtocol.class);
  }
  return this.taskManager;
}
