{
  if (TypeCoercer.INSTANCE.coerce(ifClause.evaluate(node,context),BooleanNode.class) == BooleanNode.TRUE)   return ifExpression.evaluate(node,context);
  return thenExpression.evaluate(node,context);
}
