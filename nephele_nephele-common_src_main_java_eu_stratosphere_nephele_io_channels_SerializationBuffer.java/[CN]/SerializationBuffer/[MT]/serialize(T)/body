{
  if (dataLeftFromPreviousSerialization())   throw new IOException("Cannot write new data, " + leftInSerializationBuffer() + " bytes still left from previous call");
  record.write(this.serializationBuffer);
  integerToByteBuffer(serializationBuffer.getLength(),this.lengthBuf);
}
