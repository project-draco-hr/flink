{
  for (int i=0; i < SIZEOFINT; ++i) {
    int shift=i << (SIZEOFINT - 1);
    byteBuffer.put(SIZEOFINT - 1 - i,(byte)((val & (0xff << shift)) >>> shift));
  }
  byteBuffer.position(0);
  byteBuffer.limit(SIZEOFINT);
}
