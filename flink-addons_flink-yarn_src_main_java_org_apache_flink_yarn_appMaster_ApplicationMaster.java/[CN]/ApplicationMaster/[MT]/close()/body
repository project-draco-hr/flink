{
  if (!isClosed) {
    jobManager.shutdown();
    nmClient.close();
    rmClient.close();
    if (!isFailed) {
    }
 else {
      LOG.warn("Can not close AM RPC connection since the AM is in failed state");
    }
  }
  this.isClosed=true;
}
