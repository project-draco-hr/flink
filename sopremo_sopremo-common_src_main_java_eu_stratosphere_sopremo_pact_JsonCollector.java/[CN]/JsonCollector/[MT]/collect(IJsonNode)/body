{
  if (SopremoUtil.LOG.isTraceEnabled())   SopremoUtil.LOG.trace(String.format(" to %s",value));
  this.collector.collect(this.record=this.schema.jsonToRecord(this.resultProjection.evaluate(value,this.context),this.record,this.context));
}
