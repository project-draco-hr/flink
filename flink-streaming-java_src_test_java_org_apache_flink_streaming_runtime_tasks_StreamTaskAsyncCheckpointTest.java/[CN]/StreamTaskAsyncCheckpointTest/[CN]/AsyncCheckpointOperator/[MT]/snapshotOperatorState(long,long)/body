{
  StreamTaskState taskState=super.snapshotOperatorState(checkpointId,timestamp);
  AsynchronousStateHandle<String> asyncState=new DataInputViewAsynchronousStateHandle(checkpointId,timestamp);
  taskState.setOperatorState(asyncState);
  return taskState;
}
