{
  for (int i=0; i < 16; i++) {
    target.writeByte(source.readUnsignedByte());
  }
  int num=source.readInt();
  target.writeInt(num);
  for (int i=8 * num; i > 0; --i) {
    target.writeByte(source.readUnsignedByte());
  }
}
