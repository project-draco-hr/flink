{
  Type genericType=field.getGenericType();
  if (genericType == null || !(genericType instanceof ParameterizedType))   return new BoundType[0];
  return getStaticBoundTypes((ParameterizedType)genericType);
}
