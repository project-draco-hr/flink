{
  final IJsonNode result=new AggregationExpression(CoreFunctions.MEAN).evaluate(createArrayNode(2,4),null,this.context);
  Assert.assertTrue(result instanceof INumericNode);
  Assert.assertEquals(3.0,((INumericNode)result).getDoubleValue());
}
