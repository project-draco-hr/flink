{
  int x=first.f1;
  int y=first.f2;
  int a=second.f1;
  int b=second.f2;
  int c=second.f3;
  int p_x=p(x,a,b,c);
  int p_y=p(y,a,b,c);
  int min=Math.min(p_x,p_y);
  return new Tuple2<>(first.f0,min);
}
