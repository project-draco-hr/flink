{
  Fields schema=new Fields();
  Assert.assertSame(schema,new StormTuple<Object>(Tuple.getTupleClass(1).newInstance(),schema).getFields());
  Assert.assertSame(null,new StormTuple<Object>(null,schema).getFields());
}
