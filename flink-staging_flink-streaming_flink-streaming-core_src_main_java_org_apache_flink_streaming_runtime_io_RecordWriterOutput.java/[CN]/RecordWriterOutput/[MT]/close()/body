{
  if (recordWriter instanceof StreamRecordWriter) {
    ((StreamRecordWriter<?>)recordWriter).close();
  }
 else {
    try {
      recordWriter.flush();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
