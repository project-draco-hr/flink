{
  Object[] paramsAsStrings=new String[params.length];
  for (int index=0; index < paramsAsStrings.length; index++)   paramsAsStrings[index]=params[index].isTextual() ? params[index].getTextValue() : params[index].toString();
  return TextNode.valueOf(String.format(format.getTextValue(),paramsAsStrings));
}
