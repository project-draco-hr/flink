{
  for (int i=0; i < 1000000; i++) {
    if (i % 2 == 0) {
      record.setString(0,"Gyula Bela");
    }
 else {
      record.setString(0,"Gabor Frank");
    }
    pTimer.startTimer();
    emit(record);
    pTimer.stopTimer();
    pCounter.count();
  }
}
