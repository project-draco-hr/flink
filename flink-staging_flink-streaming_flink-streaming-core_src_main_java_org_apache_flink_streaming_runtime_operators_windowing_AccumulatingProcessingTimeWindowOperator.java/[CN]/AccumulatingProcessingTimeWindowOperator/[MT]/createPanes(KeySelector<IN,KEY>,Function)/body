{
  @SuppressWarnings("unchecked") KeyedWindowFunction<IN,OUT,KEY,Window> windowFunction=(KeyedWindowFunction<IN,OUT,KEY,Window>)function;
  return new AccumulatingKeyedTimePanes<>(keySelector,windowFunction);
}
