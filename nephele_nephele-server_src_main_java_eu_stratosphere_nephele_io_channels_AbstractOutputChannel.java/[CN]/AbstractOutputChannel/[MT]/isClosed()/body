{
  if (this.closeRequested && this.dataBuffer == null && !this.recordSerializer.dataLeftFromPreviousSerialization()) {
    if (!this.outputChannelBroker.hasDataLeftToTransmit()) {
      return true;
    }
  }
  return false;
}
