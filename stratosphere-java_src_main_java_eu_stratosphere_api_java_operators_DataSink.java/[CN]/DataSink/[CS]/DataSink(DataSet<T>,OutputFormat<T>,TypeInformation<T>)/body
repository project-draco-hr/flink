{
  if (format == null)   throw new IllegalArgumentException("The output format must not be null.");
  if (type == null)   throw new IllegalArgumentException("The input type information must not be null.");
  if (data == null)   throw new IllegalArgumentException("The data set must not be null.");
  this.format=format;
  this.data=data;
  this.type=type;
}
