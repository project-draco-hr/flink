{
  if (pos == 0) {
    return 0;
  }
  int ret=0;
  for (int i=0; i < pos; i++) {
    TypeInformation<?> fieldType=tupleType.getTypeAt(i);
    ret+=fieldType.getTotalFields() - 1;
  }
  return ret;
}
