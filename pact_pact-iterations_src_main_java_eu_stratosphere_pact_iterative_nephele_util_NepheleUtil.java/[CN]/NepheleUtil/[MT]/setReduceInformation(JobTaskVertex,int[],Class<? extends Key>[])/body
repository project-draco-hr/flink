{
  TaskConfig config=new TaskConfig(reduce.getConfiguration());
  PactRecordComparatorFactory.writeComparatorSetupToConfig(config.getConfiguration(),config.getPrefixForInputParameters(0),keyPos,keyClasses);
}
