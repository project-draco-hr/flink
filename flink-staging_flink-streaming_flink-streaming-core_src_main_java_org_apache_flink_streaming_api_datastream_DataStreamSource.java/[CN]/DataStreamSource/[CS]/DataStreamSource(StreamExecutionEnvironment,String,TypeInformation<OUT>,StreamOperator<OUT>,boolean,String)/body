{
  super(environment,outTypeInfo,operator);
  environment.getStreamGraph().addSource(getId(),operator,null,outTypeInfo,sourceName);
  this.isParallel=isParallel;
  if (!isParallel) {
    setParallelism(1);
  }
}
