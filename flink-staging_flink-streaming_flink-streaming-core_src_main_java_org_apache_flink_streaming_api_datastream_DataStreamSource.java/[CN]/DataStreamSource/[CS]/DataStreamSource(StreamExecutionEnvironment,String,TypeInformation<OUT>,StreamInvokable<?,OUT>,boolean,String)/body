{
  super(environment,operatorType,outTypeInfo,invokable);
  environment.getStreamGraph().addSource(getId(),invokable,null,outTypeInfo,sourceName);
  this.isParallel=isParallel;
  if (!isParallel) {
    setParallelism(1);
  }
}
