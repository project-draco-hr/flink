{
  if (this.isMutable) {
    while (recordIterator.next(reuse) != null) {
      flatMapper.flatMap(reuse.getTuple(),collector);
    }
  }
 else {
    while (recordIterator.next(reuse) != null) {
      flatMapper.flatMap(reuse.getTuple(),collector);
      resetReuse();
    }
  }
}
