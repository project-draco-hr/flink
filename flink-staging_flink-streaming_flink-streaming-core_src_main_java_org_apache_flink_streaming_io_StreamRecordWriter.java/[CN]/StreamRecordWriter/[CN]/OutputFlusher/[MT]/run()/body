{
  while (running) {
    try {
      flush();
      Thread.sleep(timeout);
    }
 catch (    InterruptedException e) {
    }
catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}
