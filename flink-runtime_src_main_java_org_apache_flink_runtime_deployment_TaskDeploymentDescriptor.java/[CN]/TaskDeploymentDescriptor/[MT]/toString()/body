{
  return String.format("TaskDeploymentDescriptor [job id: %s, job vertex id: %s, " + "execution id: %s, task name: %s (%d/%d), invokable: %s, " + "produced partitions: %s, input gates: %s]",jobID,vertexID,executionId,taskName,indexInSubtaskGroup,numberOfSubtasks,invokableClassName,collectionToString(producedPartitions),collectionToString(inputGates));
}
