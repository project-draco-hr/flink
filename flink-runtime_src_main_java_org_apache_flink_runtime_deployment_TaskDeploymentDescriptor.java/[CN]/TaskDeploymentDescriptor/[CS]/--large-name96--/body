{
  this(jobID,jobName,vertexID,executionId,serializedExecutionConfig,taskName,numberOfKeyGroups,indexInSubtaskGroup,numberOfSubtasks,attemptNumber,jobConfiguration,taskConfiguration,invokableClassName,producedPartitions,inputGates,requiredJarFiles,requiredClasspaths,targetSlotNumber,null,null);
}
