{
  this(jobID,vertexID,executionId,taskName,indexInSubtaskGroup,numberOfSubtasks,jobConfiguration,taskConfiguration,invokableClassName,producedPartitions,inputGates,requiredJarFiles,targetSlotNumber,null);
}
