{
  ExecutionEnvironment env=ExecutionEnvironment.getExecutionEnvironment();
  DataSource<Tuple2<Integer,Boolean>> input=env.fromElements(new Tuple2<Integer,Boolean>(5,true));
  ExpressionOperation<JavaBatchTranslator> expressionOperation=ExpressionUtil.from(input,"a, b");
  ExpressionOperation<JavaBatchTranslator> result=expressionOperation.select("b && true, b && false, b || false, !b");
  DataSet<Row> ds=ExpressionUtil.toSet(result,Row.class);
  ds.writeAsText(resultPath,FileSystem.WriteMode.OVERWRITE);
  env.execute();
  expected="true,false,true,false";
}
