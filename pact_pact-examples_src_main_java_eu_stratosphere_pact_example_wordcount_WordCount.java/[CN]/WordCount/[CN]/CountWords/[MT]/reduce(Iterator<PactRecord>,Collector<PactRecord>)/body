{
  PactRecord element=null;
  int sum=0;
  while (records.hasNext()) {
    element=records.next();
    sum+=element.getNumFields() <= 1 ? 1 : element.getField(1,PactInteger.class).getValue();
  }
  this.cnt.setValue(sum);
  element.setField(1,this.cnt);
  out.collect(element);
}
