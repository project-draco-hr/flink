{
  this.flinkCollector.setTimestamp(element.getTimestamp());
  IN value=element.getValue();
  this.bolt.execute(new StormTuple<>(value,this.inputSchema,this.topologyContext.getThisTaskId(),this.inputStreamId,this.inputComponentId));
}
