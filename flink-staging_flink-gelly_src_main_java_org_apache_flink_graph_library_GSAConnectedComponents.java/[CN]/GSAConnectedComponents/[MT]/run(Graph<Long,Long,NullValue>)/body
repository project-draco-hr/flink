{
  Graph<Long,Long,NullValue> undirectedGraph=graph.getUndirected();
  return undirectedGraph.runGatherSumApplyIteration(new GatherNeighborIds(),new SelectMinId(),new UpdateComponentId(),maxIterations);
}
