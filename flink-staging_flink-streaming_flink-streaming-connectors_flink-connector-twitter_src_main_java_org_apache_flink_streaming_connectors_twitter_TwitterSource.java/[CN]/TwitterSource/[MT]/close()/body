{
  if (LOG.isInfoEnabled()) {
    LOG.info("Initiating connection close");
  }
  if (client != null) {
    client.stop();
  }
  if (LOG.isInfoEnabled()) {
    LOG.info("Connection closed successfully");
  }
}
