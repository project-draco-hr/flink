{
  if (returnStatus == ReturnStatus.RETURN_SECOND) {
    return new Iterator<V1>(){
      @Override public boolean hasNext(){
        return false;
      }
      @Override public V1 next(){
        return null;
      }
      @Override public void remove(){
      }
    }
;
  }
 else {
    return iterator1.getValues();
  }
}
