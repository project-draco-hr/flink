{
  if (returnStatus == ReturnStatus.RETURN_FIRST) {
    return new Iterator<V2>(){
      @Override public boolean hasNext(){
        return false;
      }
      @Override public V2 next(){
        return null;
      }
      @Override public void remove(){
      }
    }
;
  }
 else {
    return iterator2.getValues();
  }
}
