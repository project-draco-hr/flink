{
  if (next == null) {
    if (iterator.hasNext()) {
      next=iterator.next();
      return true;
    }
 else {
      return false;
    }
  }
  if (nextKey) {
    nextKey=false;
    return true;
  }
  while (true) {
    KeyValuePair<K,V> prev=next;
    if (iterator.hasNext()) {
      next=iterator.next();
      if (next.getKey().compareTo(prev.getKey()) != 0) {
        return true;
      }
    }
 else {
      return false;
    }
  }
}
