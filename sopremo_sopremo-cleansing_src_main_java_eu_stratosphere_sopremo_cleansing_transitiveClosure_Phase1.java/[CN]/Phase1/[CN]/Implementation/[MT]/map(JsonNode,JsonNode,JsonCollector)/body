{
  IntNode itStep=new IntNode(this.iterationStep);
  if (((ArrayNode)key).get(0).equals(itStep) && ((ArrayNode)key).get(1).equals(itStep)) {
    TransitiveClosure.warshall((BinarySparseMatrix)matrix);
  }
  out.collect(key,matrix);
}
