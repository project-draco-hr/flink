{
  int c1=0, c2=0;
  while (values.hasNext()) {
    IntPair pair=values.next();
    if (c1 == 0 && pair.getFirst() != 0)     c1+=pair.getFirst();
    if (c2 == 0 && pair.getSecond() != 0)     c2+=pair.getSecond();
  }
  out.collect(key,new IntPair(c1,c2));
}
