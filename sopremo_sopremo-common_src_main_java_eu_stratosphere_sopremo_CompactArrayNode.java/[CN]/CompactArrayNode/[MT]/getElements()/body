{
  return new AbstractIterator<JsonNode>(){
    int index=0;
    @Override protected JsonNode loadNext(){
      if (this.index < CompactArrayNode.this.children.length)       return CompactArrayNode.this.children[this.index++];
      return this.noMoreElements();
    }
  }
;
}
