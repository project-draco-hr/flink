{
  try {
    this.listenerContext.sendDataAsynchronously(new OutputBufferLatency(this.listenerContext.getJobID(),this.listenerContext.getVertexID(),sourceChannelID,bufferLatency));
  }
 catch (  InterruptedException e) {
    LOG.error(StringUtils.stringifyException(e));
  }
}
