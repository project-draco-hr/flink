{
  try {
    this.listenerContext.sendDataAsynchronously(new ChannelThroughput(this.listenerContext.getJobID(),this.listenerContext.getVertexID(),sourceChannelID,throughput));
  }
 catch (  InterruptedException e) {
    LOG.error(StringUtils.stringifyException(e));
  }
}
