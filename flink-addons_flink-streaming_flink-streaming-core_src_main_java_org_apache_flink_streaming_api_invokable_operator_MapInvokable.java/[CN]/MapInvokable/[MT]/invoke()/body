{
  if (this.isMutable) {
    while (recordIterator.next(reuse) != null) {
      collector.collect(mapper.map(reuse.getTuple()));
    }
  }
 else {
    while (recordIterator.next(reuse) != null) {
      collector.collect(mapper.map(reuse.getTuple()));
      resetReuse();
    }
  }
}
