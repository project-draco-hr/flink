{
  try {
    @SuppressWarnings("unchecked") StreamRecord<T> t=StreamRecord.class.newInstance().setId(0);
    t.setTuple(tupleSerializer.createInstance());
    return t;
  }
 catch (  Exception e) {
    throw new RuntimeException("Cannot instantiate StreamRecord.",e);
  }
}
