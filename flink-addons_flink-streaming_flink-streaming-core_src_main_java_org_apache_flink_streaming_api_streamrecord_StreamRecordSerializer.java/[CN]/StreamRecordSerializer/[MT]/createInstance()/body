{
  try {
    @SuppressWarnings("unchecked") StreamRecord<T> t=StreamRecord.class.newInstance();
    t.isTuple=isTuple;
    t.setObject(typeSerializer.createInstance());
    return t;
  }
 catch (  Exception e) {
    throw new RuntimeException("Cannot instantiate StreamRecord.",e);
  }
}
