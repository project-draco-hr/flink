{
  line=br.readLine().replaceAll("[\\-\\+\\.\\^:,]","");
  while (line != null) {
    if (line != "") {
      lineTuple.setField(line,0);
      emit(new StreamRecord(lineTuple));
    }
    line=br.readLine();
  }
}
