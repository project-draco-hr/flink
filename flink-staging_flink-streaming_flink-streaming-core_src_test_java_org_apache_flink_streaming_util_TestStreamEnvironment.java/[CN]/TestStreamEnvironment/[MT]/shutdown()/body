{
  if (!sync) {
    cluster.stop();
    cluster=null;
    jobRunner.join();
    jobRunner=null;
    return latestResult;
  }
  throw new IllegalStateException("Cluster was not started via .start(...)");
}
