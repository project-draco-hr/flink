{
  try {
    final JsonParser parser=new JsonParser(new URL(getResourcePath("PathExpressionTest/test.json")));
    return parser.readValueAsTree();
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
  return null;
}
