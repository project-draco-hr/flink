{
  StreamExecutionEnvironment env=StreamExecutionEnvironment.createLocalEnvironment(PARALLELISM);
  DataStream<Tuple1<String>> stream=env.addSource(new BasicSource(),SOURCE_PARALLELISM).map(new BasicMap());
  stream.print();
  env.execute();
}
