{
  StreamExecutionEnvironment env=StreamExecutionEnvironment.createLocalEnvironment(PARALLELISM);
  DataStream<String> stream=env.addSource(new BasicSource(),SOURCE_PARALLELISM).map(new IdentityMap());
  stream.print();
  env.execute();
}
