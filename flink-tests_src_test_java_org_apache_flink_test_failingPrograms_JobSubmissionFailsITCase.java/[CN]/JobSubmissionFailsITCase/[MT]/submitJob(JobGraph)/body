{
  if (detached) {
    cluser.submitJobDetached(jobGraph);
    return null;
  }
 else {
    SerializedJobExecutionResult result=cluser.submitJobAndWait(jobGraph,false,TestingUtils.TESTING_DURATION());
    return result.toJobExecutionResult(getClass().getClassLoader());
  }
}
