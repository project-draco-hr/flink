{
  HeadArraySchema schema=new HeadArraySchema();
  schema.setHeadSize(5);
  PactRecord record=schema.jsonToRecord(new ArrayNode(IntNode.valueOf(0),IntNode.valueOf(1),IntNode.valueOf(3)),null,null);
  return new LazyHeadArrayNode(record,schema);
}
