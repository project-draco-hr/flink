{
  if (this.serializers != null) {
    for (    RecordSerializer<?> s : this.serializers) {
      Buffer b=s.getCurrentBuffer();
      if (b != null) {
        b.recycleBuffer();
      }
    }
  }
}
