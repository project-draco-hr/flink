{
  if (fieldSet.size() > fields.size()) {
    return false;
  }
  for (int i=0; i < fieldSet.size(); i++) {
    if (!fieldSet.contains(fields.get(i))) {
      return false;
    }
  }
  return true;
}
