{
  if (fieldSet.size() > indexes.size()) {
    return false;
  }
  for (int i=0; i < fieldSet.size(); i++) {
    if (!fieldSet.contains(indexes.get(i))) {
      return false;
    }
  }
  return true;
}
