{
  if (other.getClass() == HBaseKey.class) {
    return this.writable.compareTo(((HBaseKey)other).writable);
  }
 else {
    throw new IllegalArgumentException("Compare between HBase key and " + other.getClass().getName() + " is not defined.");
  }
}
