{
  if (!equals(prefix))   return false;
  if (getSelector() == null)   return prefix.getSelector() == null;
  if (prefix.getSelector() == null)   return true;
  return getSelector().isPrefix(prefix.getSelector());
}
