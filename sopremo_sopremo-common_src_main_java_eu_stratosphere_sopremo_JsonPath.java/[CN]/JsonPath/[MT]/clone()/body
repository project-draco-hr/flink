{
  try {
    JsonPath clone=(JsonPath)super.clone();
    if (getSelector() != null)     clone.setSelector(clone.getSelector().clone());
    return clone;
  }
 catch (  CloneNotSupportedException e) {
    throw new IllegalStateException("should never happen",e);
  }
}
