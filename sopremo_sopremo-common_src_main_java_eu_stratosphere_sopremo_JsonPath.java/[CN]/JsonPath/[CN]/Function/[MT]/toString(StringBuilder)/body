{
  builder.append(name);
  builder.append('(');
  for (int index=0; index < params.length; index++) {
    builder.append(params[index]);
    if (index < params.length - 1)     builder.append(", ");
  }
  builder.append(')');
}
