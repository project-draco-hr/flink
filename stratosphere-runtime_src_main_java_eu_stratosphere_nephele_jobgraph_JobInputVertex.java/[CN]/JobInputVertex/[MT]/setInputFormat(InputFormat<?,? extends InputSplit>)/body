{
  this.inputFormat=inputFormat;
  UserCodeWrapper<? extends InputFormat<?,? extends InputSplit>> wrapper=new UserCodeObjectWrapper<InputFormat<?,? extends InputSplit>>(inputFormat);
  TaskConfig config=new TaskConfig(this.getConfiguration());
  config.setStubWrapper(wrapper);
}
