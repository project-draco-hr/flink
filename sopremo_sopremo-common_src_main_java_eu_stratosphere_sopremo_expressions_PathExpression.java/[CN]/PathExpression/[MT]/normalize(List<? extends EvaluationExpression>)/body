{
  LinkedList<EvaluationExpression> linkedList=new LinkedList<EvaluationExpression>();
  for (  EvaluationExpression fragment : fragments)   if (fragment instanceof PathExpression)   linkedList.addAll(((PathExpression)fragment).fragments);
 else   if (!canIgnore(fragment))   linkedList.add(fragment);
  return linkedList;
}
