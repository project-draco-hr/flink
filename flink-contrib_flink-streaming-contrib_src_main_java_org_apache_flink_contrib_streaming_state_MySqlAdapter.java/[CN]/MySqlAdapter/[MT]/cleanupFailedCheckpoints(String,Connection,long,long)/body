{
  validateStateId(stateId);
  try (Statement smt=con.createStatement()){
    smt.executeUpdate("DELETE FROM kvstate_" + stateId + " WHERE timestamp > "+ checkpointTs+ " AND timestamp < "+ recoveryTs);
  }
 }
