{
  validateStateId(stateId);
  try (Statement smt=con.createStatement()){
    smt.executeUpdate("CREATE TABLE IF NOT EXISTS kvstate_" + stateId + " ("+ "id bigint, "+ "k varbinary(256), "+ "v blob, "+ "PRIMARY KEY (k, id) "+ ")");
  }
 }
