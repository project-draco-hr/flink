{
  CollectionEnvironment env=new CollectionEnvironment();
  env.fromElements("A","B","C","D").map(new MapFunction<String,String>(){
    public String map(    String value){
      return value + " " + 1;
    }
  }
).print();
  env.execute();
}
