{
  Map<String,OperatorPropertyInfo> properties=input ? this.inputProperties : this.operatorProperties;
  OperatorPropertyInfo property=properties.get(name);
  if (property == null)   throw new IllegalArgumentException(String.format("Unknown property %s for operator %s (available %s)",name,operator.getName(),properties));
  return property;
}
