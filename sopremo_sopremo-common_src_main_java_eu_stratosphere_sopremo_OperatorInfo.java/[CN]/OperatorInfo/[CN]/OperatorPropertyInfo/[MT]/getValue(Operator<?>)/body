{
  try {
    return getDescriptor().getReadMethod().invoke(operator,new Object[0]);
  }
 catch (  Exception e) {
    throw new RuntimeException(String.format("Could not get property %s of %s",getName(),operator),e);
  }
}
