{
  try {
    getDescriptor().getWriteMethod().invoke(operator,value);
  }
 catch (  Exception e) {
    throw new RuntimeException(String.format("Could not set property %s of %s to %s",getName(),operator,value),e);
  }
}
