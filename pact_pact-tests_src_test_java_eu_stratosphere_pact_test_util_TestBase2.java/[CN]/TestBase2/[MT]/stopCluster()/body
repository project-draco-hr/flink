{
  try {
    if (this.executer != null) {
      System.err.println("######################### STOPPING LOCAL EXECUTION CONTEXT #########################");
      this.executer.stop();
      this.executer=null;
      FileSystem.closeAll();
      System.gc();
    }
  }
  finally {
    deleteAllTempFiles();
  }
}
