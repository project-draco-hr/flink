{
  JsonNode result=this.getTransformation().evaluate(JsonUtils.asArray(new StreamArrayNode(new UnwrappingIterator(values1)),new StreamArrayNode(new UnwrappingIterator(values2))),this.getContext());
  out.collect(PactNull.getInstance(),new PactJsonObject(result));
}
