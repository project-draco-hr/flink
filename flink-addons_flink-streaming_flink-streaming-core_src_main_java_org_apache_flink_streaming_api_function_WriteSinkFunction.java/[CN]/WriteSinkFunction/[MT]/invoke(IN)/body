{
  if (!tuple.equals(endTuple)) {
    tupleList.add(tuple);
    if (updateCondition()) {
      format.write(path,tupleList);
      resetParameters();
    }
  }
 else {
    format.write(path,tupleList);
    resetParameters();
  }
}
