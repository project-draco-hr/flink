{
  if (obj instanceof StateTransition) {
    @SuppressWarnings("unchecked") StateTransition<T> other=(StateTransition<T>)obj;
    return action == other.action && targetState.getName().equals(other.targetState.getName());
  }
 else {
    return false;
  }
}
