{
  long nextStart=0;
  for (  FreeSegmentEntry entry : this.freeSegments) {
    if (entry.start != nextStart) {
      return false;
    }
    nextStart=entry.end;
  }
  return nextStart == this.totalSize;
}
