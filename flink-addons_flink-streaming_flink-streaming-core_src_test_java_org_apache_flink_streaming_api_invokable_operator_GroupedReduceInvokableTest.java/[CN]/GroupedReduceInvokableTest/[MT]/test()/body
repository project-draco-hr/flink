{
  GroupedReduceInvokable<Integer> invokable1=new GroupedReduceInvokable<Integer>(new MyReducer(),0);
  List<Integer> expected=Arrays.asList(1,2,2,4,3);
  List<Integer> actual=MockInvokable.createAndExecute(invokable1,Arrays.asList(1,1,2,2,3));
  assertEquals(expected,actual);
}
