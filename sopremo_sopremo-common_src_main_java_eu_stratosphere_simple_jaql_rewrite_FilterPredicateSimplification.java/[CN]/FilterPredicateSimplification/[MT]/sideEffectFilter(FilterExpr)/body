{
  for (int i=0; i < fe.conjunctivePred_count(); i++) {
    Expr pred=fe.conjunctivePred(i);
    boolean noExternalEffects=pred.getProperty(ExprProperty.HAS_SIDE_EFFECTS,true).never();
    if (!noExternalEffects)     return true;
  }
  return false;
}
