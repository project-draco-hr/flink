{
  final long numberOfVertices=network.numberOfVertices();
  return network.runGatherSumApplyIteration(new GatherRanks(numberOfVertices),new SumRanks(),new UpdateRanks<K>(beta,numberOfVertices),maxIterations);
}
