{
  final List<TaskSubmissionResult> resultList=new ArrayList<TaskSubmissionResult>();
  for (  final TaskSubmissionWrapper tsw : tasks)   resultList.add(this.submitTask(tsw.getVertexID(),tsw.getConfiguration(),tsw.getEnvironment()));
  return resultList;
}
