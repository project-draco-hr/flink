{
  final StringBuilder builder=new StringBuilder();
  for (  final JsonNode jsonNode : params)   builder.append(jsonNode.isTextual() ? ((TextNode)jsonNode).getTextValue() : jsonNode);
  return JsonUtil.OBJECT_MAPPER.valueToTree(builder);
}
