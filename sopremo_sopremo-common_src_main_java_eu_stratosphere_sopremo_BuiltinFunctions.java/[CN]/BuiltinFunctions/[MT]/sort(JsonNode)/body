{
  final List<JsonNode> nodes=new ArrayList<JsonNode>();
  for (  final JsonNode jsonNode : (ArrayNode)node)   nodes.add(jsonNode);
  Collections.sort(nodes);
  final ArrayNode arrayNode=new ArrayNode();
  arrayNode.addAll(nodes);
  return arrayNode;
}
