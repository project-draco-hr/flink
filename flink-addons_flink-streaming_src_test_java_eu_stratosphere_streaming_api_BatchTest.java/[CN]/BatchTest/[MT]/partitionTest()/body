{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  DataStream<Tuple1<String>> dataStream=env.addSource(new MySource(),SOURCE_PARALELISM).flatMap(new MyMap(),PARALELISM).batch(4).partitionBy(0).addSink(new MyPartitionSink(),93);
  env.execute();
  assertTrue(partitionCorrect);
}
