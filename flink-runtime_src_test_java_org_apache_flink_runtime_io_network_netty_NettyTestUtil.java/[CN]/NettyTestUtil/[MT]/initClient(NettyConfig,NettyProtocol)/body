{
  final NettyClient client=new NettyClient(config);
  try {
    client.init(protocol);
  }
 catch (  Exception e) {
    if (client != null) {
      client.shutdown();
    }
    throw e;
  }
  return client;
}
