{
  final NettyServer server=new NettyServer(config);
  try {
    server.init(protocol);
  }
 catch (  Exception e) {
    if (server != null) {
      server.shutdown();
    }
    throw e;
  }
  return server;
}
