{
  if (!stateFetched()) {
    ObjectInputStream stream=new ObjectInputStream(getInputStream());
    try {
      state=(Serializable)stream.readObject();
    }
  finally {
      stream.close();
    }
  }
  return state;
}
