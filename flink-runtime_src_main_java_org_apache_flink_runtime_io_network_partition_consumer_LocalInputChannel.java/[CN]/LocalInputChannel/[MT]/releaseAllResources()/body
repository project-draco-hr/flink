{
  if (!isReleased) {
    if (lookAhead != null) {
      lookAhead.recycle();
      lookAhead=null;
    }
    if (queueIterator != null) {
      queueIterator.discard();
      queueIterator=null;
    }
    isReleased=true;
  }
}
