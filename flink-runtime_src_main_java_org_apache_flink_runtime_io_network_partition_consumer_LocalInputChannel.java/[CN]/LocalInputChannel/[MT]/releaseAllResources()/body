{
  if (!isReleased) {
    if (lookAhead != null) {
      lookAhead.recycle();
      lookAhead=null;
    }
    if (queueIterator != null) {
      queueIterator.releaseAllResources();
      queueIterator=null;
    }
    isReleased=true;
  }
}
