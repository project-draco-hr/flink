{
  long line1=record.getField(0,LongValue.class).getValue();
  String line2=record.getField(1,StringValue.class).getValue();
  collector.collect(new Record(new LongValue(line1),new StringValue(line2)));
}
