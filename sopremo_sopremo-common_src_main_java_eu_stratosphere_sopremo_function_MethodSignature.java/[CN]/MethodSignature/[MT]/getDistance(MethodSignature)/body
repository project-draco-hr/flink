{
  Class<?>[] actualParamTypes=actualSignature.parameterTypes;
  if (this.parameterTypes.length != actualParamTypes.length)   return INCOMPATIBLE;
  int distance=0;
  for (int index=0; index < this.parameterTypes.length; index++) {
    if (!this.parameterTypes[index].isAssignableFrom(actualParamTypes[index]))     return INCOMPATIBLE;
    distance+=ReflectUtil.getDistance(this.parameterTypes[index],actualParamTypes[index]);
  }
  return distance;
}
