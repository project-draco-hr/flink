{
  final HashSet<IJsonNode> filterSet=new HashSet<IJsonNode>(Arrays.asList(elementsToFilter));
  result.clear();
  for (int index=0; index < input.size(); index++)   if (!filterSet.contains(input.get(index)))   result.add(input.get(index));
  return result;
}
