{
  try {
    Reflections reflections=new Reflections("org.apache.flink");
    @SuppressWarnings("unchecked") Set<Class<?>> stateHandleImplementations=(Set<Class<?>>)(Set<?>)reflections.getSubTypesOf(StateObject.class);
    for (    Class<?> clazz : stateHandleImplementations) {
      validataSerialVersionUID(clazz);
    }
    @SuppressWarnings("unchecked") Set<Class<?>> kvStateSnapshotImplementations=(Set<Class<?>>)(Set<?>)reflections.getSubTypesOf(KvStateSnapshot.class);
    System.out.println(kvStateSnapshotImplementations);
    for (    Class<?> clazz : kvStateSnapshotImplementations) {
      validataSerialVersionUID(clazz);
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
}
