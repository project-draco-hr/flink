{
  try {
    Assert.assertEquals("{\"taskmanagers\": " + cluster.getTaskManagers().size() + ", \"slots\": 4}",TestBaseUtils.getFromHTTP("http://localhost:" + port + "/jobsInfo?get=taskmanagers"));
  }
 catch (  Throwable e) {
    e.printStackTrace();
    Assert.fail(e.getMessage());
  }
}
