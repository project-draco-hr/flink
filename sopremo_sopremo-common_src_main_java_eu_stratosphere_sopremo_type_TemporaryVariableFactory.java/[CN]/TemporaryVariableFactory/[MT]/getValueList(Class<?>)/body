{
  final Map<Class<?>,Queue<Object>> classToInstances=this.cachedObjects.get();
  Queue<Object> objectList=classToInstances.get(type);
  if (objectList == null)   classToInstances.put(type,objectList=new LinkedList<Object>());
  return objectList;
}
