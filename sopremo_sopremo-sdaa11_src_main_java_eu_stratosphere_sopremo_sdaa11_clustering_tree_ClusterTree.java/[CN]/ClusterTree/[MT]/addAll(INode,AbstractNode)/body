{
  if (node instanceof Leaf)   mergedNode.add(node);
 else {
    final InnerNode innerNode=(InnerNode)node;
    for (    final INode subnode : innerNode.getSubnodes())     mergedNode.add(subnode);
  }
}
