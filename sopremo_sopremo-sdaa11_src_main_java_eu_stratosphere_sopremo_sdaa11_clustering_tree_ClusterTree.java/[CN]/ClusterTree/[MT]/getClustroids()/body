{
  final Collection<Leaf> leafs=this.getLeafs();
  final List<Point> clustroids=new ArrayList<Point>(leafs.size());
  for (  final Leaf leaf : leafs)   clustroids.add(leaf.getClustroid());
  return clustroids;
}
