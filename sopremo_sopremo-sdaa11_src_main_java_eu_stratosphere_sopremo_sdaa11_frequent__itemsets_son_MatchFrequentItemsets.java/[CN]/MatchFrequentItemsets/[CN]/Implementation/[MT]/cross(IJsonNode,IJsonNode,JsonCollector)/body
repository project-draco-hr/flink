{
  final IArrayNode fisItems=FrequentItemsetNodes.getItems((ObjectNode)fisNode);
  final IArrayNode basketItems=BasketNodes.getItems((ObjectNode)basketNode);
  if (this.isIncluded(fisItems,basketItems))   out.collect(fisNode);
}
