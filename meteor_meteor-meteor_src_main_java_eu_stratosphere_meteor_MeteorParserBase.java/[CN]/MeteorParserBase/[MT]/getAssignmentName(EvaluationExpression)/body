{
  if (expression instanceof PathExpression)   return getAssignmentName(((PathExpression)expression).getLastFragment());
  if (expression instanceof ObjectAccess)   return ((ObjectAccess)expression).getField();
  return expression.toString();
}
