{
  final JsonNode id=this.idGeneration.evaluate(this.params,this.getContext());
  this.params.getChildren()[1]=LongNode.valueOf(this.counter++);
  if (this.enumerationExpression == EvaluationExpression.SAME_KEY)   out.collect(id,value);
 else   out.collect(key,this.enumerationExpression.evaluate(JsonUtil.asArray(value,id),this.getContext()));
}
