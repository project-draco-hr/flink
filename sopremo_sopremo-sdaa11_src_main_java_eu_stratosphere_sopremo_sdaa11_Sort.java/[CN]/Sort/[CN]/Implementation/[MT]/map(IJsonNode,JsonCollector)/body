{
  final IArrayNode inputNodes=(IArrayNode)value;
  this.nodes.clear();
  for (  final IJsonNode inputNode : inputNodes)   this.nodes.add(inputNode);
  Collections.sort(this.nodes);
  inputNodes.clear();
  inputNodes.addAll(this.nodes);
  out.collect(inputNodes);
}
