{
  if (this.heap.size() > 0) {
    HeadStream top=heap.peek();
    PactRecord head=top.getHead();
    if (!top.nextHead(target)) {
      heap.poll();
    }
    heap.adjustTop();
    return head;
  }
 else {
    return null;
  }
}
