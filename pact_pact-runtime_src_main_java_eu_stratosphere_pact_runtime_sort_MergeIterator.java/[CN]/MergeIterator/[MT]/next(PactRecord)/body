{
  if (this.heap.size() > 0) {
    HeadStream top=heap.peek();
    top.getHead().copyTo(target);
    if (!top.nextHead()) {
      heap.poll();
    }
    heap.adjustTop();
    return true;
  }
 else {
    return false;
  }
}
