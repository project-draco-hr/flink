{
  if (this.heap.size() > 0) {
    HeadStream top=heap.peek();
    PactRecord head=top.getHead();
    if (!top.nextHead()) {
      heap.poll();
    }
    heap.adjustTop();
    head.copyTo(target);
    return true;
  }
 else {
    return false;
  }
}
