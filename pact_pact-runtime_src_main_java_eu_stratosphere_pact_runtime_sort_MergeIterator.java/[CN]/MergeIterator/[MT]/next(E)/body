{
  if (this.heap.size() > 0) {
    final HeadStream<E> top=this.heap.peek();
    top.accessors.copyTo(top.getHead(),target);
    if (!top.nextHead()) {
      this.heap.poll();
    }
 else {
      this.heap.adjustTop();
    }
    return true;
  }
 else {
    return false;
  }
}
