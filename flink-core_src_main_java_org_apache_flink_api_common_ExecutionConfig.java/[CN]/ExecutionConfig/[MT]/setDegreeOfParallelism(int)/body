{
  if (parallelism < 1 && parallelism != -1) {
    throw new IllegalArgumentException("Degree of parallelism must be at least one, or -1 (use system default).");
  }
  this.parallelism=parallelism;
  return this;
}
