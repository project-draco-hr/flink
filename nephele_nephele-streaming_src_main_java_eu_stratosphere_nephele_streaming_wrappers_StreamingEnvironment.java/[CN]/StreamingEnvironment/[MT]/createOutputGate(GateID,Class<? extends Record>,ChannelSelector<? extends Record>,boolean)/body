{
  final OutputGate<? extends Record> outputGate=getWrappedEnvironment().createOutputGate(gateID,outputClass,selector,isBroadcast);
  final StreamingOutputGate sog=new StreamingOutputGate(outputGate,this.streamListener);
  this.streamingOutputGates.add(sog);
  return sog;
}
