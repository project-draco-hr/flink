{
  List<Integer> expectedList=Arrays.asList(1,2,3);
  List<Integer> actualList=SourceFunctionUtil.runSourceFunction(new FromElementsFunction<Integer>(1,2,3));
  assertEquals(expectedList,actualList);
}
