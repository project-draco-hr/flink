{
  if (this.shallSplit(representation))   this.emitSplitRepresentations(representation,out);
 else   if (this.shallRecluster(representation,oldClustroid))   this.emitUnstableRepresentation(representation,out);
 else   this.emitStableRepresentation(representation,out);
}
