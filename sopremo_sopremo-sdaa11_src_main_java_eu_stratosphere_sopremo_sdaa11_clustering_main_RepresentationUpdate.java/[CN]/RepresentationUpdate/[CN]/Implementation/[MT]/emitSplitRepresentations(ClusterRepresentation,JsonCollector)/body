{
  final String oldID=representation.getId();
  final Point[] newClustroids=representation.findSplittingClustroids();
  this.emit(oldID + "a",newClustroids[0],ClusterRepresentation.SPLIT_FLAG,oldID,out);
  this.emit(oldID + "b",newClustroids[1],ClusterRepresentation.SPLIT_FLAG,oldID,out);
}
