{
  return String.format("TaskState jobId=%s, executionId=%s, state=%s, error=%s",jobID,executionId,executionState,error == null ? "(null)" : error.getClass().getName() + ": " + error.getMessage());
}
