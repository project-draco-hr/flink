{
  if (fileOutput) {
    return env.readCsvFile(dataPath).fieldDelimiter(' ').includeFields(true,true).types(Double.class,Double.class).map(new TupleDataConverter());
  }
 else {
    return LinearRegressionData.getDefaultDataDataSet(env);
  }
}
