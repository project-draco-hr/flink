{
  final TextNode idNode=JsonUtil2.getField(clusterNode,SequentialClustering.SCHEMA_ID,TextNode.class);
  final IArrayNode pointsNode=JsonUtil2.getField(clusterNode,SequentialClustering.SCHEMA_POINTS,IArrayNode.class);
  this.outputNode.put(PointMapper.SCHEMA_CLUSTER_ID,idNode);
  for (  final IJsonNode pointNode : pointsNode) {
    this.outputNode.put(PointMapper.SCHEMA_POINT,pointNode);
    out.collect(this.outputNode);
  }
}
