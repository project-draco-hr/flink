{
  MockCoInvokable<IN1,IN2,OUT> mock=new MockCoInvokable<IN1,IN2,OUT>(input1,input2);
  invokable.initialize(mock.getCollector(),mock.getIterator(),mock.getInDeserializer1(),mock.getInDeserializer2(),false);
  try {
    invokable.open(null);
    invokable.invoke();
    invokable.close();
  }
 catch (  Exception e) {
    throw new RuntimeException("Cannot invoke invokable.",e);
  }
  return mock.getOutputs();
}
