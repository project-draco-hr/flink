{
  PairwiseSP a2aSP=new PairwiseSP();
  Plan plan=a2aSP.getPlan(config.getString("All2AllSPTest#NoSubtasks","4"),getFilesystemProvider().getURIPrefix() + rdfDataPath,getFilesystemProvider().getURIPrefix() + resultPath,"true");
  PactCompiler pc=new PactCompiler();
  OptimizedPlan op=pc.compile(plan);
  JSONGenerator jg=new JSONGenerator();
  System.out.println(jg.compilePlanToJSON(op));
  JobGraphGenerator jgg=new JobGraphGenerator();
  return jgg.compileJobGraph(op);
}
