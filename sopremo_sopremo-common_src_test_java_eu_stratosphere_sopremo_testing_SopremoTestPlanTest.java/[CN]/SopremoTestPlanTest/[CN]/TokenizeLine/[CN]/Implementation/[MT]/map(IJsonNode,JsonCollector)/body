{
  Matcher matcher=WORD_PATTERN.matcher(((TextNode)((IObjectNode)value).get("line")).getJavaValue());
  while (matcher.find())   out.collect(JsonUtil.createObjectNode("word",TextNode.valueOf(matcher.group())));
}
