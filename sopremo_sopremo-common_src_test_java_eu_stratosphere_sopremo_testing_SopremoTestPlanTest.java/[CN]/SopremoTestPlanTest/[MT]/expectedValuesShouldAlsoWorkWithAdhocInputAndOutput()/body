{
  final SopremoTestPlan testPlan=new SopremoTestPlan(new Identity(null));
  testPlan.getInput(0).add(createValueNode("test1")).add(createValueNode("test2"));
  testPlan.getExpectedOutput(0).add(createValueNode("test1")).add(createValueNode("test2"));
  testPlan.run();
}
