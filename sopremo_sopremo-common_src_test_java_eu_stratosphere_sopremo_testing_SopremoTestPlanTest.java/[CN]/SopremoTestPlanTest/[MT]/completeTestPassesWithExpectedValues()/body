{
  final SopremoTestPlan testPlan=new SopremoTestPlan(new Identity().withInputs(new Source(PersistenceType.HDFS,this.getResourcePath("SopremoTestPlan/test.json"))));
  testPlan.getExpectedOutput(0).setOperator(new Source(PersistenceType.HDFS,this.getResourcePath("SopremoTestPlan/test.json")));
  testPlan.run();
}
