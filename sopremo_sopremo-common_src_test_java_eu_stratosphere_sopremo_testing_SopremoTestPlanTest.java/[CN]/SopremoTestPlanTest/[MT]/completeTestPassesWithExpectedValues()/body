{
  final SopremoTestPlan testPlan=new SopremoTestPlan(new Identity().withInputs(new Source(getResourcePath("SopremoTestPlan/test.json"))));
  testPlan.getExpectedOutput(0).setOperator(new Source(getResourcePath("SopremoTestPlan/test.json")));
  testPlan.run();
}
