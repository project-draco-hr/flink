{
  if (this == o) {
    return true;
  }
 else   if (o instanceof SubtaskState) {
    SubtaskState that=(SubtaskState)o;
    return this.chainedStateHandle.equals(that.chainedStateHandle) && stateSize == that.stateSize && duration == that.duration;
  }
 else {
    return false;
  }
}
