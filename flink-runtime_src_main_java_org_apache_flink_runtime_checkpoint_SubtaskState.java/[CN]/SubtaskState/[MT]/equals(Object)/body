{
  if (this == o) {
    return true;
  }
 else   if (o instanceof SubtaskState) {
    SubtaskState that=(SubtaskState)o;
    return this.state.equals(that.state) && stateSize == that.stateSize && duration == that.duration;
  }
 else {
    return false;
  }
}
