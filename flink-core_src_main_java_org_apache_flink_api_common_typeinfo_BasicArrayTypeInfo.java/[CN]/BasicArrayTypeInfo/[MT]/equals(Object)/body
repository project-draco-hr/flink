{
  if (obj instanceof BasicArrayTypeInfo) {
    BasicArrayTypeInfo<?,?> other=(BasicArrayTypeInfo<?,?>)obj;
    return other.canEqual(this) && arrayClass == other.arrayClass && componentInfo.equals(other.componentInfo);
  }
 else {
    return false;
  }
}
