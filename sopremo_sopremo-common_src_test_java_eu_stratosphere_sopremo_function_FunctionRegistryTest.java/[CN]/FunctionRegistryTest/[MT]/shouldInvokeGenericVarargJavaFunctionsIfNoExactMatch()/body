{
  this.registry.register(JavaFunctions.class);
  Assert.assertSame(GENERIC_VARARG_NODE,this.registry.evaluate("count",null,JsonUtil.asArray(new ObjectNode(),new ObjectNode(),new ObjectNode()),this.context));
}
