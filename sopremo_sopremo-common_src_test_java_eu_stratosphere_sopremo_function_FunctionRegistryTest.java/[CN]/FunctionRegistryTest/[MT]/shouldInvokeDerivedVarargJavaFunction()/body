{
  this.registry.register(JavaFunctions.class);
  Assert.assertSame(SUM_NODE,this.registry.evaluate("sum",JsonUtil.asArray(new IntNode(1),new IntNode(2),new IntNode(3)),this.context));
  Assert.assertSame(SUM_NODE,this.registry.evaluate("sum",JsonUtil.asArray(new IntNode(1)),this.context));
}
