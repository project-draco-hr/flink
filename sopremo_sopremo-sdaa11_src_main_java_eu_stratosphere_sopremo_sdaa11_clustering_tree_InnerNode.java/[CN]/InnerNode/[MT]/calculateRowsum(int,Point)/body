{
  int rowsum=0;
  for (int i=0; i < this.entries.length; i++) {
    if (withoutIndex == i || this.entries[i] == null)     continue;
    final int distance=point.getDistance(this.entries[i].getPoint());
    rowsum+=distance * distance;
  }
  return rowsum;
}
