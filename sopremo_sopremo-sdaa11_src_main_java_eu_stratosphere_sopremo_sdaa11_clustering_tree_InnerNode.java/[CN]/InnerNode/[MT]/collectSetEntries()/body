{
  final List<Integer> setIndexes=this.collectSetIndexes();
  final List<Entry> result=new ArrayList<InnerNode.Entry>(setIndexes.size());
  for (  final int i : setIndexes)   result.add(this.entries[i]);
  return result;
}
