{
  final int[] rowsums=new int[this.entries.length];
  for (int i=0; i < this.entries.length; i++)   if (this.entries[i] == null)   rowsums[i]=-1;
 else   rowsums[i]=this.calculateRowsum(i,point);
  return rowsums;
}
