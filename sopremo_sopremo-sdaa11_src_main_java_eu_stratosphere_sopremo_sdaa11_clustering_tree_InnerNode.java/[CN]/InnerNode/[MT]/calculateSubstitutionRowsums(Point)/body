{
  int[] rowsums=new int[entries.length];
  for (int i=0; i < entries.length; i++) {
    if (entries[i] == null) {
      rowsums[i]=-1;
    }
 else {
      rowsums[i]=calculateRowsum(i,point);
    }
  }
  return rowsums;
}
