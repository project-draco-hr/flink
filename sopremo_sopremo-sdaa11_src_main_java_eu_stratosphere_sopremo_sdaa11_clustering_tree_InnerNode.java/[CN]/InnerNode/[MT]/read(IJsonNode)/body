{
  this.representative=new Point();
  this.representative.read(JsonUtil2.getField(node,JSON_KEY_REPRESENTATIVE,ObjectNode.class));
  int i=0;
  for (  final IJsonNode member : JsonUtil2.getField(node,JSON_KEY_CHILDREN,IArrayNode.class))   this.entries[i++]=this.readEntry(member);
}
