{
  for (int i=0; i < this.entries.length; i++) {
    final Entry entry=this.entries[i];
    if (entry != null && entry.subnode == node)     return i;
  }
  throw new IllegalArgumentException("No such subnode");
}
