{
  for (int i=0; i < parallelSessions && streamFactory.getProducedGeneratorsCount() < sessionCountLimit; ++i) {
    subGeneratorLists.add(streamFactory.newSessionStreamForKey(randomGenerator.choseRandomElement(sessionKeys),0L));
  }
}
