{
  builder.append(this.name);
  builder.append('(');
  for (int index=0; index < this.params.length; index++) {
    builder.append(this.params[index]);
    if (index < this.params.length - 1)     builder.append(", ");
  }
  builder.append(')');
}
