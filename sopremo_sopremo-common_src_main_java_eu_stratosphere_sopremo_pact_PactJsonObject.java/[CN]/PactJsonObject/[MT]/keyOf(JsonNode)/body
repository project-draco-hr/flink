{
  if (node instanceof ValueNode)   return new Key(node);
  if (node.isArray()) {
    if (!isValidArray(node))     throw new IllegalArgumentException(node + " is not a valid key array");
    return new Key(node);
  }
  throw new IllegalArgumentException(node.getClass().getSimpleName());
}
