{
  if (node instanceof ValueNode)   return new Key((ValueNode)node);
  if (node instanceof ArrayNode)   return new Key((ArrayNode)node);
  throw new IllegalArgumentException(node.getClass().getSimpleName());
}
