{
  if (this.channelToReadFrom == -1) {
    if (this.isClosed()) {
      return true;
    }
synchronized (this.availableChannels) {
      return !(this.availableChannels.isEmpty());
    }
  }
  return true;
}
