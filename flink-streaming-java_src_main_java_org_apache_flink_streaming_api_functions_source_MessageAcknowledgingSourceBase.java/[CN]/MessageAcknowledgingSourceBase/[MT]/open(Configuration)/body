{
  idsForCurrentCheckpoint=new ArrayList<>(64);
  if (pendingCheckpoints == null)   pendingCheckpoints=new ArrayDeque<>(numCheckpointsToKeep);
  if (idsProcessedButNotAcknowledged == null)   idsProcessedButNotAcknowledged=new HashSet<>();
}
