{
  PactRecord rec=null;
  HashSet<PactString> hash=new HashSet<PactString>();
  while (records.hasNext()) {
    rec=records.next();
    hash.add(rec.getField(1,PactString.class));
  }
  out.collect(new PactRecord(rec.getField(0,PactString.class),new PactInteger(hash.size())));
}
