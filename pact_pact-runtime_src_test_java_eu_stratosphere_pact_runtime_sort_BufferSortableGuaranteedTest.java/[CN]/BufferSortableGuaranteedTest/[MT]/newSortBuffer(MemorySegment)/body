{
  Comparator<TestData.Key> keyComparator=new TestData.KeyComparator();
  @SuppressWarnings("unchecked") RawComparator comparator=new DeserializerComparator(new int[]{0},new Class[]{TestData.Key.class},new Comparator[]{keyComparator});
  return new BufferSortableGuaranteed(memory,comparator);
}
