{
  if (reducer == null) {
    throw new NullPointerException("GroupReduce function must not be null.");
  }
  TypeInformation<R> resultType=TypeExtractor.getGroupReduceReturnTypes(reducer,this.getDataSet().getType(),Utils.getCallLocationName(),true);
  return new GroupReduceOperator<>(this,resultType,dataSet.clean(reducer),Utils.getCallLocationName());
}
