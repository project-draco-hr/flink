{
  if (simpleKey) {
    return Array.get(value,keyFields[0]);
  }
 else {
    int c=0;
    for (    int pos : keyFields) {
      ((Tuple)key).setField(Array.get(value,pos),c);
      c++;
    }
    return key;
  }
}
