{
  if (!(o instanceof FloatValue))   throw new ClassCastException("Cannot compare " + o.getClass().getName() + " to FloatValue!");
  final double other=((FloatValue)o).value;
  return this.value < other ? -1 : this.value > other ? 1 : 0;
}
