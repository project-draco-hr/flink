{
  final int oldNumFields=this.numFields;
  int index=0;
  for (int i=0; i < oldNumFields; i++) {
    if ((mask & 0x1L) == 0) {
      this.removeField(index);
    }
 else {
      index++;
    }
    mask>>>=1;
  }
}
