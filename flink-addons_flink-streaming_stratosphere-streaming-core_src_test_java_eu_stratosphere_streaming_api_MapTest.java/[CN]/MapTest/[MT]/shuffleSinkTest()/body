{
  StreamExecutionEnvironment env=new StreamExecutionEnvironment();
  fillFromCollectionSet();
  DataStream<Tuple1<Integer>> dataStream=env.fromCollection(fromCollectionSet).map(new MyMap(),3).addSink(new MyShufflesSink());
  env.execute();
  assertEquals(10,shuffleResult);
}
