{
  int index=((operator_scope)operator_stack.peek()).aliasNames.indexOf(variable.getText());
  if (index == -1)   index=((operator_scope)operator_stack.peek()).inputNames.indexOf(variable.getText());
  if (index == -1)   throw new IllegalArgumentException("Unknown variable:",new RecognitionException(variable.getInputStream()));
  return index;
}
