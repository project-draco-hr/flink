{
  final List<Level<Node>> levels=GraphLevelPartitioner.getLevels(startNodes,navigator);
  for (  final Level<Node> level : levels)   for (  final Node node : level.getLevelNodes())   listener.nodeTraversed(node);
}
