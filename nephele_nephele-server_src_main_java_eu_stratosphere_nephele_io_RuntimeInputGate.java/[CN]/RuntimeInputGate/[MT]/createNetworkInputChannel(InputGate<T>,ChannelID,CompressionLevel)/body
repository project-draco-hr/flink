{
  final NetworkInputChannel<T> enic=new NetworkInputChannel<T>(inputGate,this.inputChannels.size(),this.deserializerFactory.createDeserializer(),channelID,compressionLevel);
  addInputChannel(enic);
  return enic;
}
