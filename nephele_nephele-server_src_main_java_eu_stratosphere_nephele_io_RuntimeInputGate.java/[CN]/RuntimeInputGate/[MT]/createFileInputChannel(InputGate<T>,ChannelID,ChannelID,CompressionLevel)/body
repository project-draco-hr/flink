{
  final FileInputChannel<T> efic=new FileInputChannel<T>(inputGate,this.inputChannels.size(),this.deserializer,channelID,connectedChannelID,compressionLevel);
  addInputChannel(efic);
  return efic;
}
