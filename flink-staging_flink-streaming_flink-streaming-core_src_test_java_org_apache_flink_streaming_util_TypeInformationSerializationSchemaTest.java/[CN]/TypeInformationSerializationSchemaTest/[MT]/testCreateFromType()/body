{
  try {
    TypeInformationSerializationSchema<String> schema=new TypeInformationSerializationSchema<String>("",new ExecutionConfig());
    assertEquals(BasicTypeInfo.STRING_TYPE_INFO,schema.getProducedType());
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
}
