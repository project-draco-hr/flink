{
  final EvaluationExpression[] projections={null,getAggregativeProjection()};
  final double[] thresholds={0.0,0.5,1.0};
  final boolean[] useIds={false,true};
  final ArrayList<Object[]> parameters=new ArrayList<Object[]>();
  for (  final EvaluationExpression projection : projections)   for (  final double threshold : thresholds)   for (  final String[][] combinedBlockingKeys : CombinedBlockingKeys)   for (  final boolean useId : useIds)   parameters.add(new Object[]{threshold,projection,useId,combinedBlockingKeys});
  return parameters;
}
