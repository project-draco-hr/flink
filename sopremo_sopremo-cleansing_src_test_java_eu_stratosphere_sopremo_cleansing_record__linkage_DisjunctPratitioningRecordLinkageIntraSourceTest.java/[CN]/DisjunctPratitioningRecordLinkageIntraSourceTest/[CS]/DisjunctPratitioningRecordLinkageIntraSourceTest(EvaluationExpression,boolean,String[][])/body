{
  super(projection,useId);
  this.blockingKeys=new EvaluationExpression[blockingKeys[0].length];
  for (int index=0; index < this.blockingKeys.length; index++)   this.blockingKeys[index]=new ObjectAccess(blockingKeys[0][index]);
}
