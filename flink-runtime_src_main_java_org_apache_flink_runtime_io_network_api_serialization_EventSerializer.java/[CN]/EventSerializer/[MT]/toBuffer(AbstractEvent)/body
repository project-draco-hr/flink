{
  final ByteBuffer serializedEvent=EventSerializer.toSerializedEvent(event);
  final Buffer buffer=new Buffer(new MemorySegment(serializedEvent.array()),FreeingBufferRecycler.INSTANCE,false);
  buffer.setSize(serializedEvent.remaining());
  return buffer;
}
