{
  if (!parseParameters(args)) {
    return;
  }
  StreamExecutionEnvironment env=StreamExecutionEnvironment.createLocalEnvironment().setDegreeOfParallelism(4);
  @SuppressWarnings("unused") DataStream<String> stream1=env.addSource(new SimpleKafkaSource<String>(topic,host,port,new JavaDefaultStringSchema())).setParallelism(3).print().setParallelism(3);
  env.execute();
}
