{
  MyIterator it=new MyIterator();
  if (this.isMutable) {
    do {
      reducer.reduce(it,collector);
      it.reset();
    }
 while (reuse != null);
  }
 else {
    do {
      reducer.reduce(it,collector);
      it.reset();
    }
 while (reuse != null);
  }
}
