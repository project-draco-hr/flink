{
  if (counter >= batchSize) {
    return false;
  }
 else {
    try {
      reuse=recordIterator.next(reuse);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    return (reuse != null);
  }
}
