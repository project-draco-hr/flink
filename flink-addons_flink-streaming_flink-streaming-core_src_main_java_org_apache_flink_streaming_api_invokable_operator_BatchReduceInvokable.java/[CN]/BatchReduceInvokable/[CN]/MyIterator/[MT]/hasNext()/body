{
  if (counter >= batchSize) {
    return false;
  }
 else {
    try {
      resetReuse();
      reuse=recordIterator.next(reuse);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    return (reuse != null);
  }
}
