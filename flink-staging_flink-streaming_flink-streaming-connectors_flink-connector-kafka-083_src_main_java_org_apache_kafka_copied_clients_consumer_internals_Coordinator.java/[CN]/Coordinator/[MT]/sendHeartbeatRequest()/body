{
  HeartbeatRequest req=new HeartbeatRequest(this.groupId,this.generation,this.consumerId);
  return client.send(consumerCoordinator,ApiKeys.HEARTBEAT,req).compose(new HeartbeatCompletionHandler());
}
