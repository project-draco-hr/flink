{
  while (coordinatorUnknown()) {
    RequestFuture<Void> future=sendConsumerMetadataRequest();
    client.poll(future,requestTimeoutMs);
    if (future.failed())     client.awaitMetadataUpdate();
  }
}
