{
  if (timer != null) {
    timer.cancel();
  }
  if (reporter != null) {
    try {
      reporter.close();
    }
 catch (    Throwable t) {
      LOG.warn("Metrics reporter did not shut down cleanly",t);
    }
  }
}
