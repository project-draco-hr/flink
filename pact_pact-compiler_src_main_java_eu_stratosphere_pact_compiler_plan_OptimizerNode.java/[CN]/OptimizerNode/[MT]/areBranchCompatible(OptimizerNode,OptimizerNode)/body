{
  if (subPlan1 == null || subPlan2 == null)   throw new CompilerException("SubPlans may not be null.");
  if (this.lastJoinedBranchNode == null) {
    return true;
  }
  final OptimizerNode nodeToCompare=subPlan1.branchPlan.get(this.lastJoinedBranchNode);
  if (!(nodeToCompare == subPlan2.branchPlan.get(this.lastJoinedBranchNode)))   return false;
 else   return true;
}
