{
  ois.defaultReadObject();
  this.possibleValues=new ArrayList<JsonNode>();
  ArrayNode array=SopremoUtil.deserializeNode(ois,ArrayNode.class);
  for (  JsonNode jsonNode : array)   this.possibleValues.add(jsonNode);
}
