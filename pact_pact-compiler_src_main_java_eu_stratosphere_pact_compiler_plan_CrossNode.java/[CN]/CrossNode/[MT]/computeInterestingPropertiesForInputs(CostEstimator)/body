{
  List<InterestingProperties> thisNodesIntProps=getInterestingProperties();
  List<InterestingProperties> props1=InterestingProperties.filterByKeepSet(thisNodesIntProps,getKeepSet(0));
  List<InterestingProperties> props2=InterestingProperties.filterByKeepSet(thisNodesIntProps,getKeepSet(1));
  if (props1.isEmpty() == false) {
    input1.addAllInterestingProperties(props1);
  }
 else {
    input1.setNoInterestingProperties();
  }
  if (props2.isEmpty() == false) {
    input2.addAllInterestingProperties(props2);
  }
 else {
    input2.setNoInterestingProperties();
  }
}
