{
  ObjectCreation transformation=new ObjectCreation();
  transformation.addMapping("sum",new Arithmetic(createPath("$","a"),ArithmeticOperator.PLUS,createPath("$","b")));
  assertParseResult(new Projection(transformation,recordsSource()),recordsJaql() + "recs -> transform {sum: $.a + $.b}");
}
